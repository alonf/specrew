# Known Traps

Reusable defect patterns that must be rechecked before feature closure.

| Category | Broken Pattern | Detection Method | Remediation Guidance | Discovery Date | Reapplication Result |
| --- | --- | --- | --- | --- | --- |
| path-resolution | `[System.IO.Path]::GetFullPath($ProjectPath)` or equivalent applied to user-supplied relative paths (PWD vs .NET CurrentDirectory split) | `tests/integration/project-path-resolution-regression.ps1` static scan | Replace with `Resolve-ProjectPath` from `extensions/specrew-speckit/scripts/shared-governance.ps1` | 2026-05-09 | Reapplied 2026-05-09; zero findings recorded in `specs/009-project-path-resolution/quality/trap-reapplication.md`. |
| governance | Hardening gate Approval Ref recorded as `Human developer request on 2026-05-09 to run feature 009 through the full lifecycle including implementation (current session)` when the human's intake message did not mention hardening gates or runtime-evidence deferrals. The approval was inferred by the agent on autopilot rather than explicitly granted. | Review every `hardening-gate.md` `Approval Ref` against the `.squad/decisions.md` ledger; the Approval Ref MUST trace to a recorded explicit human approval, not an inferred one. | If a hardening gate or any other governance gate cannot proceed without human approval, the agent MUST stop and ask, and MUST wait for an explicit response. Inferring approval from prior intake messages is not permitted. | 2026-05-09 | Feed this trap into spec 008 (reviewer-escalation symmetry) as a real-world example when its formal lifecycle covers agent-vs-human approval boundaries; do not modify spec 008 from this feature. |
| governance | Hardening gate Overall Verdict and pending metadata lifecycle — pre-sign-off schema convention | Scan every `hardening-gate.md` in feature directories for the presence of an Overall Verdict field (e.g., `ready`, `deferred-with-approval`) paired with pending metadata fields (`Reviewed By: *(pending ...)*`, `Reviewed At: *(pending)*`, etc.). If Overall Verdict is `ready` and the hardening gate was authored before sign-off, the gate may carry richer pending metadata (Reviewed By and Reviewed At marked pending) while Overall Verdict is set to `ready` to signal planning-level readiness. This is the intended pre-sign-off schema convention introduced in feature 008 iteration 005. | Keep richer pre-sign-off schemas where Overall Verdict: ready carries explicit pending metadata fields alongside planning-time evidence. Do not regress pending metadata when Overall Verdict changes from planning stages to signed-off stages. This convention allows hardening gates to signal planning readiness while explicitly showing which sign-off fields are still pending. | 2026-05-11 | Formalized from feature 008 iteration 005 sign-off protocol. Reapply by scanning all `hardening-gate.md` files for consistency between Overall Verdict and metadata-field pending states. This convention is the baseline for pre-implementation sign-off workflow across all features. |
| governance | Hardening gate validation-lane-completeness concern scope drift — T027 authorized command list mismatch | Every hardening gate concern for `validation-lane-completeness` must document the exact authorized list of validation commands in both the Concern Review table (in the Rationale or Expected Controls column) and the iteration plan's T027 owner-file-globs or task description. If the gate documents seven scripts but plan.md lists six, this is a drift event. Reconcile the concern evidence and the plan task definition before sign-off. | Before sign-off, verify that every hardening gate concern referencing the validation lane documents the authorized command set, and cross-check this against plan.md T027 definition. If the concern's Evidence Basis or Rationale mentions specific scripts (e.g., `gap-governance.ps1`), ensure plan.md T027 lists the same scripts. If a human approval changes the command list, update the concern table evidence and Rationale to reflect the new list. | 2026-05-11 | Seed from feature 008 iteration 005: the hardening gate validation-lane-completeness concern initially documented seven scripts (`reviewer-regression-event.ps1`, `lockout-chain-cap.ps1`, `reviewer-regression-ledger.ps1`, `reviewer-regression-withdrawal.ps1`, `carry-forward-closed-iteration.ps1`, `gap-governance.ps1`, `validate-governance.ps1`) in the concern Rationale, but human approval (Alon Fliess, 2026-05-11) authorized only six commands without `gap-governance.ps1`. The concern Rationale was not updated to reflect the new command list, so the hardening gate evidence no longer matched the authorized plan. This trap ensures future iterations update concern evidence when human approval changes the validated scope. |

| governance | Iteration 002 scaffolding without authorization — Feature 007 iteration 002 (plan.md, state.md, drift-log.md) was pre-emptively scaffolded during planning work without a fresh verbatim authorization statement from the human. The agent routed planning work and then scaffolded all iteration artifacts, but the decision record showed routing evidence only, not authorization to proceed with planning. This rule applies not only to Implementation Approval evidence (Spec 005 pre-implementation gates) but also to the act of scaffolding a new iteration's governance artifacts in the first place. Agents MUST NOT pre-emptively create plan.md, state.md, drift-log.md, or quality/hardening-gate.md for a new iteration/NNN/ directory without explicit fresh verbatim human authorization that iteration's planning should proceed. | Scan the `specs/NNN-feature/iterations/` directory structure for new iteration/NNN/ directories. For each new iteration NNN found, search `.squad/decisions.md` for an explicit decision statement containing a verbatim authorization phrase like "authorize iteration NNN planning to proceed", "iteration NNN planning approved", or equivalent fresh authorization on or before the iteration's Creation date in state.md. If a new iteration directory exists but no matching authorization decision is recorded, flag as a violation. The scanning rule: `git ls-tree --name-only specs/NNN-feature/iterations/ | grep -E '^[0-9]{3}$'` for each feature, cross-checked against `grep -i 'iteration.*[0-9]{3}.*planning.*proceed\|planning.*approved.*iteration.*[0-9]{3}'` in `.squad/decisions.md`. | When opening planning for a new iteration, the human must first issue an explicit authorization statement (recorded in `.squad/decisions.md` or as an explicit entry in the iteration plan.md Planning Approval section). Agents must then wait for or detect this authorization before scaffolding iteration governance artifacts (plan.md, state.md, drift-log.md, quality/hardening-gate.md). Prepending a Planning Approval section in plan.md with explicit Approved By, Approved Date, and Approval Evidence fields is an acceptable in-artifact authorization surface if the approval decision is also recorded in the main decisions ledger. The rule prevents silent scaffolding and ensures every iteration's governance boundary is explicitly authorized. | 2026-05-11 | Discovered during feature 007 iteration 002 planning on 2026-05-11 when iteration 002 artifacts were scaffolded during routed planning work without an explicit authorization decision recorded in `.squad/decisions.md`. The repair applies this trap to all future iterations: before a planner or agent scaffolds new iteration/NNN/ directories, verify authorization is present. The trap is preventive—it ensures governance artifacts are never silently created without human direction. |
| governance | Hardening gate for feature 008 iteration 003 initially authored with six feature-specific concerns but missing the five canonical concerns defined by spec 005 Phase 2 (security-surface, error-handling-expectations, retry-idempotency-requirements, test-integrity-targets, operational-resilience-concerns). The original planner-authored artifact omitted the canonical concerns entirely, resulting in intra-feature schema regression detected during revision cycle. | **Validator-enforced** via `extensions/specrew-speckit/scripts/validate-governance.ps1` (`category=concern-order`) and `tests/integration/validator-hardening-iteration1.ps1`. The validator now requires the first five concern rows to be exactly the canonical concerns in the required order. | Follow the structured `concern-order` FAIL output to restore the five canonical concerns first, in order, before any feature-specific rows. Re-run `validate-governance.ps1` or the iteration-1 replay harness after repairing the table. | 2026-05-10 | Validator-enforced as of feature 013 iteration 002 (FR-002, FR-007), with replay-path proof preserved in `tests/integration/validator-hardening-iteration1.ps1`. |
| governance | `specrew-start.ps1` auto-handoff bypasses the user's ability to issue first-message directives when session-loaded files are committed between restarts. Session-loaded paths include `.github/agents/*`, `.github/copilot-instructions.md`, `extensions/specrew-speckit/squad-templates/coordinator/*`, `.specify/extensions/specrew-speckit/squad-templates/coordinator/*`, and `.squad/agents/*/charter.md`. When the user commits changes to these behavioral files and restarts Copilot to reload them, the existing auto-continue directive in the regenerated `.specrew/last-start-prompt.md` immediately resumes the lifecycle, bypassing the user's chance to inject a post-restart directive (e.g., status clarifications, iteration-plan overrides, or scope reframings). | Code review and user-reported friction observed on 2026-05-11 during feature 007 dogfooding; deterministic test coverage in `tests/integration/specrew-start-pause-and-confirm.ps1` and `tests/integration/specrew-start-parameter-handling.ps1` exercises the detector, pause-and-confirm, and custom-directive prepending paths. | See feature 011 (`specs/011-specrew-start-conditional-pause`) implementation: (1) baseline commit tracking in `.specrew/last-start-prompt.md` YAML frontmatter (`baseline_commit_hash: <sha>`), (2) change detector (`Test-SessionLoadedFilesChanged`) runs `git diff --name-only` between baseline and HEAD on session-loaded globs, (3) pause-and-confirm directive injection in `Save-StartArtifacts` when detector reports changed files (with file list and user prompt), (4) optional `-PostRestartDirective` parameter prepends custom text before pause or auto-continue logic. Auto-continue behavior is preserved for routine resumes (zero changed files). | 2026-05-11 | Implemented in feature 011 iteration 002 (`specs/011-specrew-start-conditional-pause/iterations/002/`); pause-and-confirm tested via scaffold-replay-path in `tests/integration/specrew-start-pause-and-confirm.ps1`, parameter handling tested in `tests/integration/specrew-start-parameter-handling.ps1`, end-to-end scenarios tested in `tests/integration/specrew-start-end-to-end.ps1`. Detector visibility output in `.specrew/last-start-prompt.md` YAML frontmatter under `session_loaded_files_changed:`. |

| governance | Per-iteration scaffolding without explicit human authorization — agents pre-emptively created new iteration governance artifacts (plan.md, state.md, drift-log.md, quality/hardening-gate.md) without a fresh verbatim authorization decision from the human that the iteration's planning should proceed. This extends the "inferred approval" trap (row 8) to cover the act of scaffolding itself: Implementation Approval evidence reuse is dangerous, but pre-emptive iteration scaffolding without authorization is equally dangerous because it commits the project to an iteration boundary before the human has authorized planning to begin. | Scan each feature's `specs/NNN-feature/iterations/` directory for new iteration/NNN/ subdirectories created on or after 2026-05-11. For each new iteration, verify that `.squad/decisions.md` contains an explicit decision record with a verbatim authorization statement (e.g., "authorize iteration NNN planning", "iteration NNN planning approved") recorded on or before the iteration directory creation date. Additionally, verify that the iteration's `plan.md` Planning Approval section (if present) records an Approved By, Approved Date, and Approval Evidence. If a new iteration directory exists without both the decisions ledger entry and the in-artifact Planning Approval section, flag as unauthorized scaffolding. Script: For each `specs/NNN-feature/iterations/MMM/`, check `git log --format=%ai` to find creation date, then scan `.squad/decisions.md` for matching authorization within 30 minutes prior. | When a human requests iteration planning work, record an explicit decision statement in `.squad/decisions.md` authorizing the iteration's planning to proceed. Before scaffolding new iteration/NNN/ artifacts, agents must verify this decision exists OR wait for the human to provide authorization. The Planning Approval section in plan.md is a companion artifact but does not replace the decisions ledger entry. Both surfaces must record the same authorization truth. This rule prevents governance artifacts from being created on agent initiative alone. | 2026-05-11 | Concrete example: Feature 007 iteration 002 on 2026-05-11 — iteration 002 directory with plan.md, state.md, and drift-log.md was scaffolded during planning routing work. The `.squad/decisions.md` routing entry shows the planner agent was requested but does not show explicit human authorization to scaffold. No Planning Approval section was recorded in plan.md. This trap ensures future iterations capture the authorization decision before artifacts are created. Reapplication: scan all features for unauthorized iteration scaffolding; most recent scaffold violations should be surfaced immediately to prevent silent governance boundary drift. |
| governance | Iteration closeout over-claim — Feature 007 iteration 002 closeout report on 2026-05-11 claimed "iteration 002 is now closed" while: (a) `validate-governance.ps1 -ProjectPath .` returned two FAIL lines against HEAD; (b) the last commit was `28288a3` (a scribe merge) with no iteration-closeout commit yet; (c) 14 modified files plus 9 untracked closeout artifacts remained in the working tree. | **Validator-enforced** via `extensions/specrew-speckit/scripts/validate-governance.ps1` (`category=over-claim`) and `tests/integration/validator-hardening-iteration2.ps1`. The validator now treats closure-oriented `Iteration Status` text as a closeout claim, requires accepted `review.md`, `retro.md`, recorded hardening-gate verification, and a clean canonical iteration directory, while excluding repo-level evidence traces such as `.squad/decisions.md` and `.squad/identity/now.md` from the dirty-tree blocker. | Fix the missing closeout evidence or dirty canonical iteration artifacts before claiming closure. Use the structured `over-claim` FAIL output to repair the missing review/retro/hardening evidence or commit/revert the iteration-directory changes, then rerun `validate-governance.ps1`. | 2026-05-11 | Validator-enforced as of feature 013 iteration 002 (FR-004, FR-007) with runtime proof in `tests/integration/validator-hardening-iteration2.ps1`; reapply by running `pwsh -NoProfile -ExecutionPolicy Bypass -File extensions/specrew-speckit/scripts/validate-governance.ps1 -ProjectPath .` or the iteration-2 replay harness. |
| governance | Implementation approval evidence reuse — sibling iterations reuse the same implementation-authorization quote, differing only by whitespace or markdown emphasis, without an explicit blanket multi-iteration authorization scope. | **Validator-enforced** via `extensions/specrew-speckit/scripts/shared-governance.ps1`, `extensions/specrew-speckit/scripts/validate-governance.ps1` (`category=approval-reuse`), and `tests/integration/validator-hardening-iteration2.ps1`. Approval evidence is normalized by collapsing whitespace and stripping markdown emphasis before sibling-iteration comparison. | Replace the reused quote with iteration-specific human approval evidence, or record an explicit blanket multi-iteration authorization scope in the approval block when reuse is intentional. Rerun `validate-governance.ps1` after repairing the approval evidence. | 2026-05-12 | Validator-enforced as of feature 013 iteration 002 (FR-003, FR-007) with duplicate, blanket-scope, and distinct-quote replay coverage in `tests/integration/validator-hardening-iteration2.ps1`. |
| test-integrity | Iteration 003 T019 was initially marked complete because cap visibility existed in runtime state surfaces (`manage-reviewer-regression.ps1` stdout, `decisions.md` ledger, `state.md` managed block) but the actual scaffolded reviewer replay path (`specrew review` and `scaffold-reviewer-artifacts.ps1`) was not exercised in tests. Review caught the gap as G-001; rework commit `a17f6cb` closed it. | For any feature task that delivers user-facing handoff or visibility output, scan the corresponding test suite to verify the test invokes the scaffolded replay path itself (for example `scaffold-reviewer-artifacts.ps1` or `specrew-review.ps1`), not just the underlying runtime state writers. Tests that only assert state-file content without invoking the user-facing scaffold path do not satisfy handoff coverage. | Every user-facing handoff or visibility task must include representative fixture coverage that invokes the actual scaffold/replay path and asserts user-visible output (`reviewer-index.md` content, `SPECREW_REVIEW` digest tokens, `specrew-review` JSON fields). Coverage of runtime state alone is insufficient. | 2026-05-10 | This trap formalizes Iteration 003 retro lesson 1 as durable corpus knowledge. Reapply by scanning all user-facing handoff tasks across features for whether their tests exercise the scaffold path. |
| governance | Session-restart trigger over-inclusivity — Concrete example: During feature 011 iteration 001 planning on 2026-05-11, the restart-trigger interpretation mistakenly included transient handoff files like `.specrew/last-start-prompt.md` and `.specrew/start-context.json` as files whose changes require a session restart, which created an unnecessary session-restart pause. Human refinement clarified the boundary: restart triggers apply only to files Copilot loads as part of the coordinator prompt at session bootstrap—namely `.github/agents/*`, `.github/copilot-instructions.md`, `extensions/specrew-speckit/squad-templates/coordinator/*`, `squad-templates/coordinator/*` (deprecated), `.squad/agents/*/charter.md`, and optional `agent-charter.md`. Restart triggers do NOT apply to transient handoff files read as tool actions (`.specrew/last-start-prompt.md`, `.specrew/start-context.json`, and other files generated by `specrew-start.ps1`, `scaffold-*.ps1`, or Squad runtime scripts), which are simply read and acted upon by the coordinator between sessions without requiring a restart. | Scan feature iteration plans, directives, and stop-and-ask decision points for restart-trigger language that references transient runtime files (e.g., `.specrew/last-start-prompt.md`, `.specrew/start-context.json`, or files under `.specrew/` with transient semantics matching `*-context.json`, `*-prompt.md`, or marked as session state). Cross-reference against the authoritative list of Copilot-session-loaded prompt files in `scripts/specrew-bootstrap.ps1` (the function that loads prompt-file variables) or `.github/copilot-instructions.md` entrypoint logic. Flag any handoff messages or stop-and-ask requests that cite transient files as restart triggers. | Only modifications to Copilot session-loaded prompt files require a session restart. Only these six categories require restart checks: `.github/agents/*`, `.github/copilot-instructions.md`, `extensions/specrew-speckit/squad-templates/coordinator/*`, `squad-templates/coordinator/*` (deprecated), `.squad/agents/*/charter.md`, and `agent-charter.md`. Modifications to transient handoff files do not. When a feature touches both prompt files and transient handoff files, apply the session-restart trigger only to the prompt files; transient handoff file changes are reflected on the next coordinator run without requiring a session restart. | 2026-05-11 | Discovered during feature 011 iteration 001 planning on 2026-05-11 when `.specrew/last-start-prompt.md` was incorrectly included in the restart condition and caused a false-positive restart pause. The human refined the rule to distinguish permanent behavioral files (loaded at session bootstrap by Copilot) from transient runtime state (Squad-generated, session-local). Reapplication: when a feature modifies both prompt files and transient handoff files, verify the restart-trigger condition applies only to prompt files; if any reapplication finds restart logic applied to transient files, that is scope drift and must be escalated. |
| governance | Iteration state.md metadata schema inconsistency — agents introduce non-canonical iteration state.md schema during scaffolding, using custom metadata field names and layouts instead of the canonical fields (`Schema`, `Last Completed Task`, `Tasks Remaining`, `In Progress`, `Baseline Ref`, `Updated`, `Current Phase`, `Iteration Status`). Feature 012 iteration 001 initially scaffolded with custom header format lacking required canonical fields, causing validator crashes when trying to validate hardening gates or other governance artifacts that depend on standard state.md metadata. | **Validator-enforced** via `extensions/specrew-speckit/scripts/validate-governance.ps1` (`category=canonical-schema`) and `tests/integration/validator-hardening-iteration1.ps1`. The validator now requires all eight canonical fields for feature ordinal `013` and later, while preserving grandfathered historical iterations. | Follow the structured `canonical-schema` FAIL output to restore the eight canonical metadata lines in `state.md`, then rerun `validate-governance.ps1` or the iteration-1 replay harness. | 2026-05-11 | Validator-enforced as of feature 013 iteration 002 (FR-001, FR-007), with replay-path proof preserved in `tests/integration/validator-hardening-iteration1.ps1`. |
| governance | `human-handoff-id-context` — user-facing narration or stop messages mention three or more feature, iteration, task, requirement, corpus, or commit references without plain-language scope, or the replay proof checks only internal state instead of the validator's user-visible output. | Run `pwsh -NoProfile -ExecutionPolicy Bypass -File .\tests\integration\descriptive-reference-authored-prose.ps1`, `pwsh -NoProfile -ExecutionPolicy Bypass -File .\tests\integration\descriptive-reference-excluded-surfaces.ps1`, and the preserved handoff-governance regression commands; confirm warn fixtures emit `soft-warning.opaque-numeric-references`, pass fixtures emit `status: pass`, and excluded verbatim fixtures stay warning-free. | Keep the descriptive-reference rule additive and non-blocking. Seed warn/pass and excluded-surface fixtures, invoke the real `handoff-governance-validator.ps1` replay path, assert on `status`, `findings`, and `summary`, and rerun the feature 007 plus iteration 001 regression lane before review-ready handoff. | 2026-05-12 | Seeded by feature 012 iteration 002. Reapply before future closeout by running the descriptive-reference replay lane together with the existing jargon-first, plain-language, review-file-reference, readable-narration, and readable-stop-message tests so readable references never regress into opaque ID-only handoffs. |

## Exemptions

- `evaluation/scorers/process-scorer.ps1`: `$ProjectPath` is resolved with `Resolve-Path`, and `GetFullPath` is only applied to computed report paths. This does not match the user-supplied relative-path defect model, so no migration is required for feature 009.
