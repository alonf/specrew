# Review Result

- **Campaign**: `cmp-199-beta3-stabilization-i001`
- **Run**: `run-20260811-093414640-d58e787b`
- **Harness**: `codex-cli-file-primary`
- **Target digest**: `6bb5a4edfa178998ffd77b62e456fb02c706d891`
- **Completion**: `complete`
- **Verdict**: `findings`
- **Runtime outcome**: `completed`
- **Currentness**: `current`
- **Can approve current snapshot**: `false`

## Summary

Risk-based review covered the controller evidence, binding specification/design, release packaging, the campaign pause and sign-off paths, verdict capture, reparse handling, verification-plan bootstrap and diagnostics, and prerelease/version surfaces. All three controller verification records succeeded for the target digest, but the candidate has approval-blocking packaging and live pause-workflow defects plus several major contract violations.

## Findings

| ID | Severity | Relevance | Location | Finding |
| --- | --- | --- | --- | --- |
| `finding-020b7c705b956edf` | blocking | current | Specrew.psd1:258 | The beta3 package omits a required runtime policy file: Specrew.psd1's FileList does not include the newly added scripts/internal/continuous-co-review/reparse-tag-policy.ps1 (or specrew-consumer-language.ps1). The publish workflow builds the module from FileList, while _load.ps1 and review-authority-store.ps1 dot-source reparse-tag-policy.ps1 as a hard dependency. Failure scenario: a consumer installs 0.40.0-beta3 from the packaged artifact and `specrew review` fails while loading the campaign engine because the required sibling file was never shipped. |
| `finding-c3c9ccdd3f330852` | blocking | current | scripts/internal/continuous-co-review/review-campaign-orchestrator.ps1:1214 | The shipped live-review command never exposes or consumes the pause protocol: Invoke-ReviewCampaignRun returns a pause with the required decision surface, but Invoke-ReviewCampaignCommand projects it away and scripts/specrew-review.ps1 only renders the generic result. A workspace-wide caller inspection also finds no production call to Write-ReviewCampaignPauseDecisionFact, Test-ReviewCampaignContinuationAuthorized, or Invoke-ReviewCampaignStopHereLanding. Failure scenario: after a round, the consumer sees no numbered decision surface; replying 1/2/3 cannot authorize a single round or run the composed stop-here landing, and a newly authorized run is not checked against the outstanding pause or the four-round budget. This leaves the release's P1 acceptance flow implemented only as disconnected helpers/tests. |
| `finding-6c127f7e790956a3` | major | current | scripts/internal/continuous-co-review/review-campaign-orchestrator.ps1:1214 | The public command drops the restored-slot disclosure: Pre-invocation failure returns now carry slot_restored and slot_restored_note, and the CLI explicitly tries to render those fields, but Invoke-ReviewCampaignCommand's projection omits both. Failure scenario: verification or harness preflight fails before reviewer invocation, the allowance slot is restored, but the consumer is not told and may create another authorization unnecessarily—the exact F4 disclosure failure this slice claims to close. |
| `finding-b76f73b9fbadf802` | major | current | scripts/internal/continuous-co-review/reparse-tag-policy.ps1:122 | Unknown reparse tags are admitted instead of failing closed: For any reparse point whose LinkType and LinkTarget are empty and whose attributes do not match the cloud mask, Resolve-SpecrewReparseDisposition returns admit-nonlinking; Test-SpecrewReparseRefusesRead refuses only refuse-link. This directly contradicts the binding acceptance scenario requiring an allowlist where unknown tags are refused. Failure scenario: an unknown tag that redirects reads without .NET exposing LinkType/LinkTarget passes containment and is trusted based on the redirected bytes' hash. |
| `finding-735e4ac5e8765d2a` | major | current | scripts/internal/continuous-co-review/review-signoff-evidence-gate.ps1:461 | Binding workshop changes are treated as records-only: Test-ReviewCampaignPathIsFeatureProcessRecord classifies every path under workshop/ as a process record. Resolve-ReviewCampaignVerdictPacketDecision then treats a records-only delta as review-current. Failure scenario: a maintainer changes a binding workshop security, requirements, or architecture decision after review; the campaign continues to authorize sign-off from the old result even though the standard the implementation was reviewed against changed. |
| `finding-05e46b88fcd58db3` | major | current | scripts/internal/continuous-co-review/verification-plan-runner.ps1:209 | Missing-environment failures still do not identify the required edit: Get-ContinuousCoReviewVerificationFailureDiagnosis lists the variables already allowed and generically tells the consumer to add another NAME, but it neither identifies the missing variable nor shows the exact env_refs line to add. Failure scenario: a fresh project's command fails because FOO is absent; the sealed output plus this message still leaves the consumer to reverse-engineer that FOO is the cause, so acceptance scenario 5.2 remains unmet. |
| `finding-faadcc2d24a98e07` | major | current | scripts/internal/continuous-co-review/review-authority-store.ps1:336 | Pending pause selection uses run-id order instead of recency: Get-ReviewCampaignPendingPause sorts run directories lexicographically and returns the last parsed unanswered pause; it never compares observed_at or campaign run order. Explicit run IDs are supported and need not sort chronologically. Failure scenario: an older `run-z` pause and newer `run-a` pause coexist; the gate selects the old digest, fails the quiet check, and demands another review even though the current round is already waiting for the human, or re-renders the wrong decision on resume. |
| `finding-7b168830114a4656` | minor | current | Specrew.psd1:440 | Beta3 package metadata still publishes the beta2 release notes: The manifest prerelease label is beta3, but PrivateData.PSData.ReleaseNotes still describes 0.40.0-beta2 and links to the beta2 release tag. Consumers viewing the beta3 gallery/package metadata are therefore directed to the wrong release notes and limitations. |

_This Markdown is a controller-generated projection. Authority is the sibling immutable `result.json`._