updated_at: (set at first squad session)
focus_area: (no active feature yet)
active_issues: (none - this is a fresh Specrew project seed; the coordinator replaces this snapshot at the first lifecycle boundary)
---

What We're Focused On
====================

**Phase**: No feature is active yet. This file is the squad's rolling
"now" snapshot — the coordinator rewrites it as lifecycle work progresses
so a resuming session can re-enter without reconstructing state.
**Urgency**: (none)

---

Current Status
--------------

Feature Lifecycle: NOT-STARTED

- No feature directory exists yet. Once a feature is created, its
  artifacts land under `specs/<feature-id>/` (spec.md, plan.md, tasks.md)
  with per-iteration files under `specs/<feature-id>/iterations/<NNN>/`.
- This seed intentionally carries no project history: the project under
  governance is THIS project; Specrew is the tool, never the subject.

Next Valid Action

Run the Specrew lifecycle intake for the first feature (the design
workshop precedes specify), or resume whatever boundary the packet in the
latest session handover names.
