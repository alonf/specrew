# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Local backup leftovers should not be version-controlled
*.backup

# Reviewer-local validation workspace
.worf-review/
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/.scratch/

# Specrew: ignore ephemeral runtime handoff artifacts, but keep stable governance files tracked
.specrew/last-start-prompt.md
.specrew/start-context.json
.specrew/start-summary.md
.specrew/bootstrap-dry-run-*.md
.specrew/.cache/
# F-043: per-developer host selection history (lives per workspace, not shared)
.specrew/host-history.json
# F-049 i004 (Pillar 1 / FR-018): per-session handoff-evidence produced at boundary syncs (runtime state)
.specrew/handoff-evidence.json
# F-174 iter-4 (FR-009): the Stop-event rolling handover is local session-bridge context, never pushed
.specrew/handover/

# Local scratch/test leftovers
.scratch/
0.7.3

# Node.js build and dependency outputs
node_modules/
dist/
build/
*.log
.env*

# Universal editor/OS noise
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/

# Specrew atomic-write staging residue (transient)
*.pending

# Specrew per-session files (F-051 multi-session foundation) - do not commit
.specrew/last-*
.specify/feature.json
.specrew/start-context.json
.specrew/host-history.json
.specrew/.cache/
.squad/sessions/
.squad/decisions/inbox/
.specrew/last-validator-summary.json
.specrew/active-sessions.yml
.specrew/workshop-visuals/
