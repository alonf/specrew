# Implementer History

Project-specific learnings and patterns discovered during work.

## Patterns

<!-- Append entries below. Format: **Pattern:** description. **Context:** when it applies. -->

## Learnings

- 2026-05-08: When Phase 2 routing defaults change, keep `extensions/specrew-speckit\templates\iteration-config.yml`, `extensions/specrew-speckit\scripts\scaffold-governance.ps1`, and the repo's own `.specrew\iteration-config.yml` aligned so downstream scaffolds and Specrew dogfooding stay truthful together.
- 2026-05-08: Phase 2 config fixtures should mirror the contract block exactly by pairing `quality.known_traps_path` with explicit `quality.routing` defaults, so governance tests can exercise seeded defaults without implying later enforcement work is already implemented.
- 2026-05-08: When a new iteration-local evidence surface is introduced, seed it in both the initial iteration scaffold and the reviewer closeout scaffold so late-generated packets preserve the same placeholder homes without overwriting real evidence.
- 2026-05-08: The hardening-gate orchestrator should reconcile the existing `quality\hardening-gate.md` artifact instead of inventing review outcomes; preserve explicit rationale text, compute verdicts through `shared-governance.ps1`, and fail closed on unresolved `tbd` or unapproved deferred concerns.
- 2026-05-10: When adding new state tracking to reviewer-regression logic (like implementer chain and lockout caps), store runtime state in `.squad/config.json` under the feature-specific section, decision evidence in `.squad/decisions.md`, and visibility in iteration `state.md` managed blocks. This three-surface pattern (runtime, governance, visibility) ensures state is both machine-readable and human-inspectable.
- 2026-05-10: Cap-activation logic should transition check (before/after) to ensure idempotent decision recording. Record the cap-activation decision only when the cap first becomes active, not on every subsequent report call, to avoid duplicate ledger entries.
- 2026-05-10: When extending test suites with new assertions, reuse existing test harness patterns (like `reviewer-regression-event.ps1`) to minimize fixture setup and focus tests on the new acceptance criteria. This keeps tests maintainable and reduces duplication.
- 2026-05-10: For state visibility tests, prefer checking the managed block in `state.md` directly over running full command-line integration (like `specrew review`) when the fixture doesn't include all required closeout artifacts. Direct block inspection is faster and more focused on the FR requirement.
- 2026-05-10: When surfacing governance state in user-facing handoff outputs (like reviewer replay), parse the managed block from `state.md` using a dedicated helper function, include parsed fields in both the summary object and digest line, and update tests to exercise the full handoff path (scaffold → review command) rather than reading fixture state directly. Cap state fields should be added conditionally to summary lines only when active, avoiding clutter and maintaining backward compatibility with existing parsing logic.
- 2026-05-10: For Iteration 005 Polish work, keep the execution truth split across `specs\008-reviewer-escalation-symmetry\iterations\005\plan.md`, `state.md`, and `drift-log.md`: mark validation progress there immediately, then hand Reviewer concrete follow-up notes instead of implying review or closeout already happened.
- 2026-05-10: When documentation includes reviewer-facing visibility examples, verify the exact text against live `extensions\specrew-speckit\scripts\scaffold-reviewer-artifacts.ps1` and `scripts\specrew-review.ps1` output first; for lockout-cap examples, the relevant surfaces are `tests\integration\fixtures\lockout-chain-cap\project\specs\008-sample\iterations\001\reviewer-index.md`, `README.md`, and `docs\user-guide.md`.
- 2026-05-10: User preference for Iteration 005 was to keep T027 to the authorized six-command lane only (`reviewer-regression-event`, `lockout-chain-cap`, `reviewer-regression-ledger`, `reviewer-regression-withdrawal`, `carry-forward-closed-iteration`, and `validate-governance -ProjectPath .`) and to stop at the implementation boundary without review, retro, or closeout.
- 2026-05-13: When closing an iteration, do not claim closure in state.md until the canonical artifacts are staged and committed; the validator will fail the closure claim on uncommitted changes. Use a pattern that reflects the closure boundary (e.g., "iteration closeout verified ... via iteration-closeout boundary commit") rather than claiming the specific commit hash ahead of time, then commit the staged changes in one boundary commit.

- 2026-05-12: For approval-reuse hardening, parse only explicit implementation-authorization evidence lines from `Implementation Authorization` / `Implementation Approval` blocks, then normalize whitespace and markdown emphasis before sibling-iteration comparison. Blanket reuse should stay opt-in via an explicit `blanket multi-iteration authorization` line so distinct approvals do not get over-matched.
- 2026-05-12: For closeout-truthfulness hardening, scope dirty-tree enforcement to canonical files inside the iteration directory and keep repo-level evidence traces (like `.squad\decisions.md`) outside the blocker. Pair that validator rule with replay-path tests that prove both the blocked dirty-iteration case and the repo-level evidence-only exemption.
- 2026-05-13: For retroactive release bookkeeping, treat tags as documentary anchors at the intended historical ship points, keep `.specrew\config.yml` as the active-version source of truth, and handle pre-existing tags with advisory-only messaging instead of any rewrite.
- 2026-05-13: For additive validator checks, keep fixture iterations on a known-good planning state and assert warning lines separately from exit codes so new soft warnings cannot mask existing hard failures.
- 2026-05-13: For review-boundary durability, create a single dedicated commit containing iteration-level review.md/plan.md/state.md updates plus any new skill/validator artifacts, stage together, write multi-section commit message (Artifacts/Verification/Outstanding-findings/Next-action), and push to origin before claiming the review boundary is durable. This prevents the boundary-claim-without-commit anti-pattern where review verdict is narrated before the next-phase prerequisites exist.
- 2026-05-13: After review-boundary repair, final-sync tasks (Scribe logging, decision merging, cross-agent history updates, .squad/ git commit) complete the iteration's synchronization; local and origin must match before retro authorization or iteration closeout, and all administrative state must be persisted durably to git before handoff.
- 2026-05-21: When consolidating duplicate helper functions into `shared-governance.ps1`, preserve original call-site behavior semantics rather than forcing uniform normalization. For example, `Get-DeclaredCompletedTaskCount` must use `Get-NormalizedKeyword` (regex-based keyword extraction) for state.md task status and `Normalize-MarkdownCell` (whitespace/backtick stripping) for plan.md parsing to match the original `validate-governance.ps1` behavior. Mirrored script files under `.specify/extensions/specrew-speckit/` must stay byte-for-byte identical to `extensions/specrew-speckit/` sources; use SHA256 hash comparison to verify parity before claiming mirror discipline.

- 2026-05-13: Feature-closeout boundary commits (per Rule 15 execution) must include precise artifact descriptions (exact file paths modified), verification evidence (validate-governance.ps1 pass, review/retro completeness), outstanding-findings resolution (explicitly no gaps or deferrals), and next-action clarity for post-merge tag creation. The boundary commit message structure (Artifacts/Verification/Outstanding-findings/Next-action) becomes the durable record that feature closure was truthful and complete. Following merge, the annotated release tag anchors the version bump and closes the feature lifecycle.
- 2026-05-13: After Feature 015 closeout, version management per Rule 15 is now proven in real-world execution: .specrew/config.yml version update (0.14.0 → 0.15.0), CHANGELOG.md entry addition, README versioning refresh, and release tag creation all flow through a single boundary commit merged to main, then tag-and-push happens after main merge confirmation. Governance validation passes after version/changelog/tag updates, confirming no drift occurs during closeout. This pattern is now available for reference in the next feature closeout.
- 2026-05-14: Feature 015 corpus-row addition (Rule 15 first real-world test): three new passive-guidance rows captured from Feature 015 execution defects discovered on 2026-05-14 and added to known-traps.md via high-rigor commit message with full Artifacts/Verification/Outstanding-findings/Next-action sections. Rows are: (1) auto-correct-cosmetic-vs-pause-and-ask (lifecycle-discipline), (2) template-substitution-failure-on-closeout (validation-discipline), (3) stale-example-wording-on-version-bump (documentation-fidelity). All three marked passive guidance with proposed graduation to Feature N Learning Loop Closure (Phase 2 slot 2.4). Corpus-row addition preserves forward-only history; no commits rewritten. Governance validation passed clean at commit 4b2db8f.
- 2026-05-15: When Recent Shipped can show multiple closed iterations for the same feature, keep the row label per-iteration (for example `F-017 · iter-001`) instead of feature-only text so live `specrew where` never emits duplicate shipped labels after multi-iteration features land.
- 2026-05-15: For Windows-rich CLI entrypoints, do not let renderer eligibility depend on `[Console]::IsOutputRedirected`. Prime UTF-8 in the entry script when rich mode is still allowed, restore the caller encodings on exit, and keep fallback warnings limited to directly verifiable causes.
- 2026-05-15: Bounded repair R-018-V2 documented: Feature 018 review misdiagnosis (terminal reporting redirected output during direct run) fixed by moving UTF-8 state priming from shared renderer to entry point with restore-on-exit pattern. The fix keeps diagnostics truthful and preserves caller state safety. Repair is not review acceptance; human confirmation gate remains: Alon must run `.\scripts\specrew.ps1 where` in fresh terminal and confirm rich glyphs/colors/sparklines display.
- 2026-05-17: Feature 019 `specrew start` launch flags are platform-conditional in `scripts\specrew-start.ps1`: Windows non-autopilot must add `--mode interactive` (and may add `--allow-all`), while Linux/macOS non-autopilot must pass only `--agent`, `--add-dir`, and `-i` because Copilot CLI v1.0.48 already defaults that bootstrap flow into the REPL there and `--mode interactive` causes a one-shot exit. Keep `tests\integration\start-command.ps1` and `specs\019-specrew-distribution-module\test-evidence\us5-cross-platform.md` aligned whenever this launch contract changes.
- 2026-05-17: For cross-platform display-path helpers, trim both `\` and `/` on root-prefix removal and only convert URI relative paths back to `\` on Windows. Feature 019 repair R14 touched `scripts\specrew-start.ps1`, `scripts\specrew-review.ps1`, and `tests\integration\start-command.ps1`; helper-level regression coverage should explicitly block Linux-style `/.specrew/...` bootstrap paths.
- 2026-05-17: Bounded repair R-019-V2-R14 completed: Feature 019 display-path separator bug (shell path trimming) fixed by removing both `/` and `\` in `Get-DisplayPath`, not just one direction. Same bug class repaired in `specrew-review.ps1`. R11/R12/R13 flag logic preserved as deferred defense-in-depth (now clearly marked symptom-chasing in evidence, not root-cause). Integration tests validated. Pattern: when shell-path separators leak into display output, check all path-manipulation boundaries for incomplete trimming; root-cause fixes trim all separator variants uniformly.
- 2026-05-17: The current feature-closeout hook lives in `extensions\specrew-speckit\scripts\scaffold-feature-closeout-dashboard.ps1` (and the mirrored `.specify` copy). It resolves the feature from `-FeatureId` or `.specify\feature.json`, writes `specs\<feature>\closeout-dashboard.md`, and is the right place to refresh `.squad\identity\now.md` to a post-closeout "No active feature" state.
- 2026-05-18: Before rerunning `speckit.specrew-speckit.before-implement`, repair stale iteration-boundary wording across the iteration plan, decision ledger, task SC body lines, and hardening-gate sign-off evidence together; otherwise governance can stay blocked even when the task backlog itself is already present.
- 2026-05-18: For before-implement-ready iteration plans, keep the header `Status` on the validator-recognized execution boundary (`executing`) and keep the `Capacity` denominator aligned with `.specrew\iteration-config.yml` (`<slice-used>/20 story_points` here) even when the feature itself is locked to a smaller slice inside that ceiling.
- 2026-05-19: Feature 023 iteration 001 feature-closeout completed. Implementer prerelease work: `promote-prerelease` publishes stable baseline from validated prerelease tag or dispatch; prerelease suffix remains audit trail, staged manifest clears `PrivateData.PSData.Prerelease` before `Publish-Module`. `invoke-module-release.ps1` stages and signs scratch copy instead of mutating worktree—both dry-runs and live publishes leave worktree unchanged. Commits `be3c2b3` and `f119e4aa` recorded. Feature-closeout boundary merged into decisions.md with absorbed T025-T031 narrative (17 SP planned / 17 SP delivered / 0 SP variance). Version 0.23.0 shipped. PR #269 open and ready for human merge authorization pending external GitHub check resolution. Decision ledger now truthful and durable.
- 2026-05-21: Feature 028 (`028-review-evidence-integrity`) merge and cleanup completed via autonomous task execution: PR #345 merged to main using merge-commit strategy (SHA `030a5a3`), branch deleted locally and on origin, no worktree removal needed (none found). Feature 028 specification and review evidence integrity requirements now durable in main. No version bump or release performed per authorization scope. Stashed uncommitted squad state during branch-switch to avoid fast-forward abort, then cleaned after merge. Decision entry logged to `.squad/decisions/inbox/implementer-feature-028-closeout.md`.
- 2026-05-21: Proposal 067 small-fix slice (validator perf and dedupe) completed in worktree `chore-validator-perf-and-dedupe`. Implemented four pillars: (1) refined global-state pathspec list in `Get-ValidatorGlobalStatePathspecs` to include `.squad/decisions.md`, `.squad/team.md`, `.squad/config.json`, `extensions/specrew-speckit/`, and `.specify/extensions/specrew-speckit/` so changed-only validator correctly triggers full validation for true global surfaces; (2) added Squad coordinator guidance in `specrew-governance.md` rule 5 documenting when validator runs in changed-only vs full mode; (3) deduplicated `Get-DeclaredCompletedTaskCount` by consolidating into `shared-governance.ps1` and removing duplicates from `validate-governance.ps1` and `scaffold-reviewer-artifacts.ps1`; (4) added lightweight timing instrumentation via `duration_ms` field in `Write-SpecrewValidatorSummary` captured by `[System.Diagnostics.Stopwatch]` in `validate-governance.ps1`. Mirror parity preserved across `extensions/` and `.specify/extensions/` for all modified scripts. CHANGELOG updated under `## Unreleased`. Small-fix per Proposal 067 contract; no proposal entry needed per human direction. Syntax checks and functional verification passed.
- 2026-05-21: Branch `029-baseline-hygiene` created from main commit `69cae3f3c8e647d61e7d1afe3e8cca3470194f3e` with working-tree changes preserved. Feature 029 directory (`specs/029-baseline-hygiene/`) and all uncommitted changes (modified squad state, new decision archive items, feature spec artifacts) are now present on the new branch. Ready for implementation without commit.
- 2026-05-21: For session-state markdown helpers, never rely on PowerShell's ambient `$Matches` after a loop that also uses `-match`. Capture the frontmatter/body match groups into local variables before iterating the frontmatter lines, or later writes can accidentally replace the prompt body with the last matched field value.
- 2026-05-21: Feature 029 (`029-baseline-hygiene`) implementation completed on branch `029-baseline-hygiene` from main commit `69cae3f`. Scope: E1 (feature-closeout invalidation) validation-only (~3.6 SP), E2 (boundary-based baseline updates) core implementation. Implemented `Update-BaselineCommitHashInFrontmatter` helper in `sync-boundary-state.ps1` to refresh `baseline_commit_hash` to current HEAD at all seven lifecycle boundaries (specify, clarify, plan, tasks, review-signoff, iteration-closeout, feature-closeout). Integration tests added: `baseline-hygiene.tests.ps1`, `lifecycle-boundary-sync.tests.ps1`, `closeout-identity-schema-parity.tests.ps1`, `stale-state-detection.tests.ps1` covering false-positive elimination, feature-closeout sentinel validation, error handling, and baseline refresh proof. Manual lifecycle test `specrew-start-pause-and-confirm.ps1` validates end-to-end flow. CHANGELOG updated with baseline hygiene and boundary-sync improvements. Scoped governance validation passed. All integration tests green. Branch ready for pull request, review, and merge to main per feature-closeout workflow. Decision entry logged to inbox.
- 2026-05-21: Generate historical dashboard artifacts only after the iteration plan/state truth surfaces have been advanced to the closeout boundary. Otherwise the stored snapshot fossilizes stale phase text (for example `phase reviewing`) and must be regenerated to become truthful closeout evidence.
