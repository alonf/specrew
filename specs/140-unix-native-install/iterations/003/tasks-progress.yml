schema: "v1"
feature: "140-unix-native-install"
iteration: "003"
updated_at: "2026-06-03T15:23:17.4068759Z"
tasks:
  T018:
    title: "macOS Homebrew `pwsh` auto-install: replace the `install.sh` Darwin fail-closed stub with the Homebrew flow — detect `brew`; `brew install --cask powershell`; **install-only-if-absent** (prefer an existing working `pwsh`); **`brew` runs as the invoking user, never `sudo brew`**; idempotent re-run; **Homebrew absent → fail closed + manual-dependency-docs link**. Derive the exact macOS command from Microsoft's CURRENT PowerShell-on-macOS install docs (D11 discipline), not memory."
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T019:
    title: "`install.sh --prerelease` flag: parse `--prerelease` (and `sh -s -- --prerelease`); route the module install to `-AllowPrerelease` (PSResourceGet `-Prerelease` equivalent); `--help` documents it; installer output states **stable vs prerelease**; if the installed module lacks the native `specrew` wrapper command surface (version/source mismatch) → **fail closed** non-zero with an incompatibility message. Built here; exercised for real at T024."
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T020:
    title: "Extend the existing `validate-macos` CI job (`cross-platform-validation.yml`) with the **wrapper runtime suite on macOS**: arg forwarding (spaces/quotes/`--`/empty), symlink resolution, pwsh-missing-at-wrapper negative, unknown-option passthrough; `install-shell-wrappers` → PATH → `specrew version` / `start --help`. (Clean no-`pwsh` auto-install is NOT CI-reachable on macOS runners → manual, T021.)"
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T021:
    title: "macOS **manual-proof** evidence on a real macOS host (the part CI cannot reach): `install.sh` primary path incl. Homebrew `pwsh` auto-install (surfaced, install-if-absent, idempotent re-run), then `specrew version`/`init`/`start`; record the run and capture the prerequisite conditions observed (Node/`nvm` shadowing; `node -v` verified inside `pwsh`; old Spec Kit remediation). Extends `macos-smoke-evidence.md`."
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T022:
    title: "Native-first docs (`README.md`, `docs/getting-started.md`, `docs/user-guide.md`, `docs/troubleshooting.md`): lead with `install.sh` / `curl`-to-`sh` from zsh/bash; demote manual `Install-Module` to a labelled fallback with the **PSGallery default-`N`** note; pwsh as internal dependency only; **troubleshooting documents** the macOS Node/`nvm`-shadows-Homebrew prerequisite (verify `node -v` inside `pwsh`) and the Spec Kit old-version remediation (`uv tool install … @v<baseline> --force`, `<baseline>` = `supported-versions.yml` `speckit.min`)."
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T023:
    title: "Complete the parity cascade **docs arm**: extend the parity CI to check doc command examples against the canonical registry (or allowlist with rationale); failure output names the FULL cascade (registry → wrappers → installer → FileList → docs). Finishes the cascade started in Iteration 2 (docs arm was deferred)."
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T024:
    title: "**Release gate (beta-before-stable):** with explicit maintainer authorization, publish the beta, then install the **published** beta on a real macOS host via `curl`-to-`sh` with `-s -- --prerelease`; run BOTH a **greenfield** and a **brownfield** project (`specrew version`/`init`/`start`), validating the bundled **Spec Kit 0.9.0** support; record evidence. This is where FR-017's prerelease install + version/source-mismatch check are proven against a real published beta. **No beta/stable publish without explicit maintainer authorization.**"
    status: "pending"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
