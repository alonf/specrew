# Hardening Gate: Iteration 004

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/008-reviewer-escalation-symmetry/spec.md`
**Iteration Ref**: `specs/008-reviewer-escalation-symmetry/iterations/004`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: *(pending sign-off)*
**Overall Verdict**: ready
**Approval Ref**: —
**Reviewed By**: *(pending)*
**Reviewed At**: *(pending)*

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `not-applicable` | `not-applicable` | `not-needed` | — | `true` | This slice does not introduce authentication boundaries, privilege checks, trust domain crossings, or user-controlled paths. It modifies internal governance ledger handling, withdrawal state reversal, and carry-forward projection logic only, so security surface analysis is not applicable for Iteration 004. | — |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents fail-closed withdrawal reversal (only reverse still-pending state; leave completed changes as historical record), robust ledger parsing (T020 fixtures for malformed ledger state), explicit error reporting in decisions ledger and handoff messages when withdrawal targets nonexistent event or carry-forward targets already-closed iteration. Post-implementation evidence: T021 integration tests should cover malformed-event withdrawal, T022 integration tests should cover carry-forward to already-active iteration, T024 implementation should enforce fail-closed reversal with no silent state loss. | `true` | Pre-implementation review confirms planning-level controls. Post-implementation verification pending. | — |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents idempotent withdrawal detection (T024 checks for existing withdrawal state before recording new withdrawal event), idempotent carry-forward projection (T026 checks for already-projected state before recording new carry-forward event), and deterministic repeated-event consolidation (consolidation logic does not depend on execution order). Post-implementation evidence: T021 test suite should include repeated withdrawal scenario, T022 test suite should include repeated carry-forward scenario, T024 implementation should check existing state before state transitions, T026 implementation should check existing projection before carry-forward. | `true` | Pre-implementation review confirms planning-level idempotency reasoning. Post-implementation verification pending. | — |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents deterministic integration coverage for US3 acceptance scenarios 1-5 (T021: `tests/integration/reviewer-regression-withdrawal.ps1`, T022: `tests/integration/carry-forward-closed-iteration.ps1`, T023: `tests/integration/reviewer-regression-ledger.ps1` and `tests/integration/gap-governance.ps1`), explicit replay-path coverage requirement for handoff-facing behavior (invokes `specrew-review.ps1` and `scaffold-reviewer-artifacts.ps1` and asserts user-visible output), and fixture-based reproducibility (T020 baseline fixtures for withdrawal, duplicate-report, carry-forward, corpus-disabled scenarios). Post-implementation evidence: T021/T022/T023 test suites should pass covering withdrawal, carry-forward, ledger consistency, corpus degradation; replay-path assertions should confirm visibility of withdrawal state, carry-forward state, and corpus state in handoff outputs; T020 fixtures should be committed and reusable for future scenarios. | `true` | Pre-implementation review confirms planning-level test coverage design. Post-implementation verification pending. | — |
| `operational-resilience-concerns` | `operational` | `not-applicable` | `not-applicable` | `not-needed` | — | `true` | This slice modifies internal governance ledger handling, withdrawal state reversal, and carry-forward projection logic. It does not introduce production runtime dependencies, external service calls, database transactions, or long-lived processes that require operational-resilience analysis. | — |
| `withdrawal-state-reversal` | `logic` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures for withdrawn event with pending and completed state, T021 integration tests for withdrawal reversal paths, T024 implementation in `manage-reviewer-regression.ps1` to reverse only still-pending escalation or routing state caused solely by the withdrawn event. Completed ownership changes, already-merged revisions, and approved corpus entries must remain as historical record and must not be retroactively undone. Post-implementation evidence: T021 integration tests should cover withdrawal of pending escalation, withdrawal of completed escalation (no-op), withdrawal with already-merged revision (preserves revision), and unapproved-trap cleanup on withdrawal. | `true` | Withdrawal reverses only still-pending state; completed changes stay as historical record. Post-implementation verification pending. | — |
| `known-traps-approval-integrity` | `governance` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures for corpus-enabled and corpus-disabled scenarios, T023 integration tests for corpus degradation paths, T025 implementation in `manage-reviewer-regression.ps1` to offer candidate trap proposals only when corpus is enabled per spec 005 FR-034, and to clean up unapproved candidate traps on withdrawal while leaving approved traps governed by normal corpus-change workflow. Post-implementation evidence: T023 integration tests should cover corpus-enabled candidate-trap proposal, corpus-disabled no-op path, and withdrawal cleanup of unapproved trap; T025 implementation should check corpus-enabled flag before proposing traps, and should distinguish approved vs. unapproved traps during withdrawal cleanup. | `true` | Only corpus-enabled repos offer candidate traps; unapproved traps are cleaned on withdrawal; approved traps remain governed by normal corpus-change workflow. Post-implementation verification pending. | — |
| `carry-forward-projection` | `logic` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures for closed-iteration reporter-regression report, T022 integration tests for carry-forward projection paths, T026 implementation in `manage-reviewer-regression.ps1` to record event immediately in ledger and project any resulting escalation or lockout-cap state into the next active iteration of the same feature by default, without silently reopening the closed iteration unless the human explicitly requests reopening. Post-implementation evidence: T022 integration tests should cover carry-forward to next active iteration, carry-forward when next iteration is already active (idempotent), and explicit-reopen path when human requests it; T026 implementation should read closed-iteration state, project escalation/cap state into next-iteration placeholder, and not modify closed-iteration artifacts. | `true` | Closed-iteration reports project into next active iteration without reopening historical artifacts. Post-implementation verification pending. | — |
| `repeated-event-consolidation` | `logic` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures for duplicate-report scenario, T023 integration tests for repeated-event consolidation and ledger consistency, T024 implementation in `manage-reviewer-regression.ps1` to maintain single active reviewer-regression chain per feature, deduplicate duplicate reports for same slice+defect, append distinct findings to ledger and extend the same chain, and preserve only the strongest unresolved escalation or routing outcome. Post-implementation evidence: T023 integration tests should cover duplicate-report deduplication, distinct-finding consolidation, and strongest-escalation preservation; T024 implementation should detect duplicate events by slice+defect hash, append distinct events to ledger, and maintain single active chain per feature. | `true` | Duplicate reports are deduplicated; distinct findings append to ledger; single active chain per feature. Post-implementation verification pending. | — |
| `us1-integration-correctness` | `integration` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures that include active reviewer-regression state from Iteration 002 US1 (event logging, stronger-class routing, same-class fallback, maximum-strength hold), T021/T023 integration tests that verify withdrawal and consolidation respect US1 state, T024/T026 implementation that reads the reviewed-regression-state managed block in iteration state and preserves US1 escalation paths during withdrawal and carry-forward. Post-implementation evidence: T020 fixtures should include US1 escalation state in fixture state.md; T021/T023 tests should verify withdrawal preserves US1 event integrity; T024/T026 implementation should read US1 state from managed block and preserve escalation/routing paths. | `true` | Withdrawal and carry-forward correctly read and preserve the active reviewer-regression chain established by Iteration 002 US1 completion. Post-implementation verification pending. | — |
| `us2-integration-correctness` | `integration` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: T020 fixtures that include implementer lockout-cap state from Iteration 003 US2 (chain counting, cap activation, post-cap routing, cap visibility), T021/T023 integration tests that verify withdrawal and consolidation respect US2 cap state, T024/T026 implementation that reads the lockout-cap state and decisions ledger entries and preserves US2 cap enforcement during withdrawal and carry-forward. Post-implementation evidence: T020 fixtures should include US2 cap state in fixture state.md and decisions.md; T021/T023 tests should verify withdrawal preserves US2 cap integrity; T024/T026 implementation should read US2 cap state from managed block and decisions ledger and preserve cap enforcement. | `true` | Withdrawal and carry-forward correctly read and preserve the implementer lockout-cap state established by Iteration 003 US2 completion. Post-implementation verification pending. | — |
| `replay-path-visibility-coverage` | `user-experience` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Planning documents expected control: Explicit replay-path coverage requirement in plan.md — any task that delivers user-facing handoff or visibility output (including but not limited to: withdrawal-state reflection in iteration state, carry-forward-state visibility in next-iteration handoff, corpus-disabled degradation message) must be tested through the scaffolded replay path (`specrew-review.ps1`, `scaffold-reviewer-artifacts.ps1`) with assertions on user-visible output. Post-implementation evidence: T021/T022/T023 integration tests should invoke scaffold-reviewer-artifacts.ps1 or specrew-review.ps1 against fixtures and assert on structured fields or format-lines output; any T024/T025/T026 implementation that modifies handoff surfaces must have corresponding scaffold-invoked test coverage. | `true` | Handoff-facing behavior is tested through scaffolded replay path with assertions on user-visible output. This requirement carries forward the Iteration 003 lesson. Post-implementation verification pending. | — |

## Post-Implementation Evidence Notes

- This gate is in the pre-implementation draft state. All `Runtime Evidence Status` fields show `pending-post-implementation` because planning is complete but implementation has not yet been authorized.
- After implementation authorization and execution, the implementing agents should update this table with `Runtime Evidence Status: recorded` and append post-implementation evidence to the `Rationale` column.
- The canonical five concerns remain first in the required order, followed by the eight slice-specific concerns used to evaluate withdrawal state reversal, known-traps approval integrity, carry-forward projection, repeated-event consolidation, US1 integration, US2 integration, and replay-path visibility coverage.

## Deferral Note

- **Polish**: Explicitly deferred to Iteration 005. Any hardening concerns related to full validation lane execution, cross-feature integration, or user-facing documentation updates are out of scope for this iteration and will be addressed in the Iteration 005 hardening gate.

## Hardening-Gate Sign-Off

**Sign-Off Evidence**: *(pending human sign-off)*

**Signed By**: *(pending)*  
**Signed At**: *(pending)*
