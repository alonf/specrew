# Hardening Gate: Iteration 003 (Draft)

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/008-reviewer-escalation-symmetry/spec.md`
**Iteration Ref**: `specs/008-reviewer-escalation-symmetry/iterations/003`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: — *(pending before-implement assignment)*
**Overall Verdict**: ready
**Approval Ref**: —
**Reviewed By**: Alon Fliess
**Reviewed At**: 2026-05-10

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `not-applicable` | `not-applicable` | `not-needed` | — | `true` | This slice does not introduce authentication boundaries, privilege checks, trust domain crossings, or user-controlled paths. It modifies internal routing and governance logic only, so security surface analysis is not applicable for Iteration 003. | — |
| `error-handling-expectations` | `error-handling` | `addressed` | `runtime-evidence` | `verified` | Planning documents fail-closed cap activation (default to human when cap is hit and no approved alternate exists), robust implementer chain parsing (T014 fixtures for malformed chain state), and explicit error reporting in decisions ledger and handoff messages. Post-implementation evidence: T015 integration tests (6/6 pass) include scenarios for malformed chain state and missing alternate-owner approval; T017 implementation enforces fail-closed routing (no synthesis of specialists after cap); T019 handoff surfaces emit clear error messages when cap is active but no approved alternate exists. | `true` | Pre-implementation review confirmed planning-level controls. Post-implementation verification: All US2 acceptance scenarios pass; cap activation fails closed to human-owned revision when no approved alternate is recorded; chain parsing handles missing or invalid state without silent degradation. | Alon Fliess (2026-05-10) |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `runtime-evidence` | `verified` | Planning documents idempotent cap-activation detection (T017 checks for existing active-cap state before recording new cap event), idempotent decision ledger writes (T018 uses append-once semantics), and deterministic chain counting (counting logic does not depend on execution order). Post-implementation evidence: T015 test suite includes repeated cap-detection scenario (lockout-chain-cap.ps1 test 5); T017 `Get-LockoutCapStatus` checks existing state before activating cap; T018 decision ledger writes use append-once semantics via `Add-StructuredDecisionsLedgerEntry`. | `true` | Pre-implementation review confirmed planning-level idempotency reasoning. Post-implementation verification: Repeated cap detection does not record duplicate events; decision ledger entries are append-once; chain counting produces consistent results regardless of execution order. | Alon Fliess (2026-05-10) |
| `test-integrity-targets` | `test-integrity` | `addressed` | `runtime-evidence` | `verified` | Planning documents deterministic integration coverage for US2 acceptance scenarios 1-3 (T015: `tests/integration/lockout-chain-cap.ps1`), closeout and replay assertions for cap visibility (T016: `tests/integration/reviewer-closeout-governance.ps1`, `tests/integration/review-command.ps1`), and fixture-based reproducibility (T014 baseline fixtures for cap-hit, alternate-owner-approved, awaiting-human-owned-revision scenarios). Post-implementation evidence: T015 test suite 6/6 pass covering cap detection, state visibility, decisions ledger, chain deduplication, post-cap routing; T016 tests pass with scaffold-invoked cap visibility assertions; T014 fixtures committed and reusable for future scenarios. | `true` | Pre-implementation review confirmed planning-level test coverage design. Post-implementation verification: All US2 acceptance scenarios pass; lockout-chain-cap.ps1 (6/6), reviewer-closeout-governance.ps1 (pass), review-command.ps1 (5/5); fixtures are committed, deterministic, and reusable. | Alon Fliess (2026-05-10) |
| `operational-resilience-concerns` | `operational` | `not-applicable` | `not-applicable` | `not-needed` | — | `true` | This slice modifies internal governance and routing logic. It does not introduce production runtime dependencies, external service calls, database transactions, or long-lived processes that require operational-resilience analysis. | — |
| `chain-counting-integrity` | `logic` | `addressed` | `runtime-evidence` | `verified` | Planning documents expected control: T014 fixtures for distinct implementer chains, T015 integration tests for chain parsing and cap-activation threshold, T017 implementation in `manage-reviewer-regression.ps1` to count only distinct implementer owners and activate cap at exactly two rotations beyond original implementer. Post-implementation evidence: T015 lockout-chain-cap.ps1 tests 1-3 pass; T017 `Get-ImplementerChainFromConfig` correctly parses distinct implementer owners; T017 `Get-LockoutCapStatus` activates cap at exactly `chainLength >= 1 + LockoutChainCap` (default 2). | `true` | Implementer chain counting correctly identifies distinct owners and activates cap at exactly the configured threshold. Post-implementation verification confirmed via T015 test results (6/6 pass) and T017 runtime execution evidence. | Alon Fliess (2026-05-10) |
| `cap-activation-routing` | `logic` | `addressed` | `runtime-evidence` | `verified` | Planning documents expected control: T014 fixtures for alternate-owner-approved and awaiting-human-owned-revision scenarios, T015 integration tests for post-cap routing paths, T017 implementation in `manage-reviewer-regression.ps1` to enforce human or explicitly approved alternate owner routing. Post-implementation evidence: T015 lockout-chain-cap.ps1 test 4 validates post-cap routing enforcement; T017 `report` mode logic does not synthesize additional specialists after cap; routing defaults to human-owned revision when no approved alternate exists in decisions.md. | `true` | Post-cap routing enforces human or explicitly approved alternate owner, with no synthesis of additional specialists. Post-implementation verification confirmed via T015 test 4 pass and T017 runtime behavior. | Alon Fliess (2026-05-10) |
| `decision-ledger-recording` | `governance` | `addressed` | `runtime-evidence` | `verified` | Planning documents expected control: T018 implementation using `shared-governance.ps1` helpers to record cap-activation and alternate-owner approval events in `.squad/decisions.md` with affected feature, iteration, rationale, and approving human. Post-implementation evidence: T014 fixtures include correct `lockout-cap` and `alternate-owner-approval` entries; T015 lockout-chain-cap.ps1 test 3 validates decisions ledger structure; T018 implementation uses `Add-StructuredDecisionsLedgerEntry` with all required metadata fields. | `true` | Every cap activation records decision evidence in `.squad/decisions.md` with complete metadata (feature, iteration, chain, rationale, approving human). Post-implementation verification confirmed via T015 test 3 pass and T014 fixture ledger inspection. | Alon Fliess (2026-05-10) |
| `handoff-visibility` | `user-experience` | `addressed` | `runtime-evidence` | `verified` | Planning documents expected control: T019 implementation in `scaffold-reviewer-artifacts.ps1`, `specrew-review.ps1`, and `.squad/routing.md` to surface locked-out agents, cap status, and planned next-owner path. Post-implementation evidence: T016 review-command.ps1 test 5 validates cap visibility in scaffold output and `specrew review` JSON fields; T019 implementation adds `Get-ReviewerRegressionCapState`, wires cap fields into summary object and format-lines output, adds `cap=active`/`cap_chain=N/M` tokens to digest line, extends `specrew-review.ps1` to expose structured cap fields. Initial gap (G-001) closed in rework commit a17f6cb. | `true` | Locked-out agents, cap status, and planned next-owner path are visible in user-facing outputs (reviewer-index.md, specrew review JSON), iteration state (state.md managed block), and decisions ledger (decisions.md cap entries). Post-implementation verification confirmed via T016 test 5 pass after rework. | Alon Fliess (2026-05-10) |
| `us1-integration-correctness` | `integration` | `addressed` | `runtime-evidence` | `verified` | Planning documents expected control: T014 fixtures that include active reviewer-regression state from Iteration 002 US1, T015 integration tests that verify chain counting respects the US1-established state, T017 implementation that reads the reviewed-regression-state managed block in iteration state. Post-implementation evidence: T014 fixtures include US1 reviewer-regression state in fixture state.md; T015 lockout-chain-cap.ps1 test 2 validates chain counting reads existing state; T017 `Get-ReviewerRegressionReadback` correctly parses the managed block and preserves US1 escalation state during cap activation. | `true` | Chain counting and cap implementation correctly read and respect the active reviewer-regression state established by Iteration 002 US1 completion. Post-implementation verification confirmed via T015 test 2 pass and T017 runtime behavior. | Alon Fliess (2026-05-10) |
| `deferred-us3-dependencies` | `scope` | `addressed` | `planning-time-analysis` | `not-needed` | User Story 3 withdrawal/carry-forward/known-traps logic is deferred to Iteration 004. Planning explicitly documents this deferral in plan.md, state.md, and drift-log.md with dependency rationale (US3 requires stable US1 event logging and US2 cap enforcement). | `false` | No implementation of US3 behavior is in scope for this iteration. This concern is addressed by explicit deferral documentation and does not block US2 implementation. | — |

## Planning-Phase Notes

- This is a **draft hardening gate** prepared during the planning phase before implementation begins.
- The gate documents **expected quality concerns and planning-level control design** rather than post-implementation evidence.
- Each concern lists the planning artifacts or task descriptions that address it, but actual implementation evidence will be required during the hardening review that occurs after execution.
- The overall verdict and approval are intentionally blank until after before-implement review and after execution begins.

## Expected Post-Implementation Review

After Iteration 003 implementation completes, the hardening gate must be updated with:

1. **Test Results**: Full execution of `tests/integration/lockout-chain-cap.ps1`, `tests/integration/reviewer-closeout-governance.ps1`, and `tests/integration/review-command.ps1` with passing results for all US2 acceptance scenarios.
2. **Implementation Evidence**: Code diffs showing the chain-counting logic, cap-activation routing enforcement, and handoff visibility updates (T017, T018, T019 implementation).
3. **Governance Artifacts**: Actual `.squad/decisions.md` entries recorded during test execution, demonstrating cap-activation event records with required metadata.
4. **Runtime Behavior**: Iteration state (`state.md`) and runtime config (`squad/config.json`) populated with correct reviewer-regression state and lockout-chain status after test execution.
5. **Integration Verification**: Test results confirming that chain counting respects the US1 active reviewer-regression state and that cap implementation does not interfere with US1 routing behavior.

## Deferral Note

- **US3 Withdrawal/Carry-Forward/Known-Traps**: Explicitly deferred to Iteration 004. Any hardening concerns related to withdrawal semantics, closed-iteration carry-forward, or known-traps integration are out of scope for this iteration and will be addressed in the US3 hardening gate.

## Hardening-Gate Sign-Off

**Sign-Off Evidence**: I sign off on the iteration 003 pre-implementation hardening gate at specs/008-reviewer-escalation-symmetry/iterations/003/quality/hardening-gate.md. The five canonical hardening concerns are present in the required order with honest US2-specific evaluations, the six feature-specific concerns follow as additional rows, the nine-column schema is in use, validate-governance passes, and the canonical-concern-enumeration trap is seeded in the corpus.

**Signed By**: Alon Fliess  
**Signed At**: 2026-05-10
