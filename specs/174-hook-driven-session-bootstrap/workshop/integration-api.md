# Integration API Intake Notes

```text
Host event JSON -> SpecrewHookDispatcher -> Bootstrap directive -> Agent prose/menu
SessionEnd event -> Handover writer ------> Handover markdown -> SessionStart read
```

Delegated intake decision: full-depth integration workshop is required because
the feature spans hook events, file contracts, generated directives, launcher
compatibility, and host-specific menu rendering.
