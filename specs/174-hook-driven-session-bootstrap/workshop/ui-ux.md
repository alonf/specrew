# UI UX Intake Notes

```text
Orientation prose
  -> handover summary if valid
  -> Resume / New / Pick-feature menu text
  -> structured picker only after visible render, where reliable
```

Delegated intake decision: medium-depth UI/UX workshop is required to verify the
render-first menu sequence per host and define prose fallback behavior.
