# Observability Resilience Intake Notes

```text
B2 event
  -> classify state: full-bootstrap | light-welcome-back | clear-anchor
  -> journal decision + dedupe key + breaker status
  -> tests assert rendered directive and state classification
```

Delegated intake decision: medium-depth observability workshop is required so
startup classification and stale-anchor clearing are reviewable after execution.
