# Retro: Iteration 012

**Schema**: v1
**Held**: 2026-06-15

Iteration 012 reconciled the F-174 user-facing documentation to the shipped hook-driven session model
(DR-1..DR-10), then absorbed an `origin/main` merge mid-iteration (Feature 182 / 0.36.0) and folded in the
resulting reconciliation as drift (D-009 cap, D-010 forge sweep, D-011 specify.md), plus the `0.37.0-beta1`
release-prep.

## What went well

- **The adversarial doc-consistency sweep paid off.** A fan-out reader-per-doc pass returned zero residual
  defects after the four maintainer-named findings were fixed — the docs reconciliation held up to scrutiny.
- **Close review caught the wrong pressure point before it shipped.** The interim cap fix cut the reconciliation
  excerpt 300→100, spending the fix against the feature's own resume behavior. The maintainer caught it; the real
  cause was the co-resident refocus growing, not resume. Reverted to the dogfooded floor, recovered from refocus,
  and added a floor-guard so it can't recur.
- **The "what is the 10K cap" question produced a better architecture.** Establishing that 10K is the *host* hook-
  output cap (not a model context window) reframed the durable fix: stop inlining what can be a pointer. The
  in-flight disk scan — 100% machine-generated by us — became Proposal 191's lead pilot (write the digest to a
  file, inline only a tiny anti-free-run anchor; ~700–850 char reclaim).

## What to improve (the lessons)

- **Never pay a cap fix against core feature behavior.** Trim the thing that *grew* (the refocus B2 tail), not the
  resume directive. The new `DirectiveDeliveryCap` resume-floor guard (handover >= 380, reconciliation >= 300)
  encodes this so the regression cannot recur silently.
- **The masked verdict-worst breach.** The cap test threw at the 9,300 margin assertion before reaching the
  verdict-worst (3b) check, hiding that the true worst case was ~10,090 — *over* the 10K hard cap. A failing
  earlier assertion can mask a worse later one; the fix had to clear the hard cap, not just the margin.
- **A docs-only iteration that absorbs a mid-iteration main merge accrues code-reconciliation drift.** Recorded as
  drift (maintainer ruling) rather than scope-creeping the docs iteration or spinning a new one — but it means a
  "documentation only" iteration shipped code/test changes, tracked honestly in the drift-log.
- **The merge brought stricter governance.** Hand-authored `review.md` tripped the post-F-182 validator schema
  (Overall Verdict token + Task Verdicts table + Gap Ledger). Match the scaffolder schema (or run the scaffolder)
  for governance artifacts after a governance-touching merge.

## Carry-forward

- **Proposal 191** (in-flight-digest lead pilot) + **Proposal 179** (dispatcher fragment-priority drop) — the
  durable CAP-1 fix (drift D-007/D-009); the pilot supersedes this interim refocus trim. Sequence after the beta.
- **CHANGELOG `[0.36.0]` gap** — pre-existing from main's F-182 ship; reconcile at release-prep.
- **specify.md gate-stop drift (D-011) was pre-existing** — sweep the other boundary digests for the same F-165
  host-scoping gap (candidate).
- **Published-beta install validation** of the bootstrap surfacing on the actual host — the feature-closeout /
  beta gate; the dev-tree real-host PASS is necessary-not-sufficient for the shipped bytes.
