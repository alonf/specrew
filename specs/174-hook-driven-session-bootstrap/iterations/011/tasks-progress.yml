schema: "v1"
feature: "174-hook-driven-session-bootstrap"
iteration: "011"
updated_at: "2026-06-14T05:05:00Z"
tasks:
  T001:
    title: "Expose a TESTED agent-callable authoring surface around the handover writer (DF-7). Shipped as the `specrew handover author` dispatcher command (the reachable replacement for the un-exported Write-SpecrewHandoverContext — agents invoke `specrew ...`, not module functions; divergence drift D-005). Parses a markdown body (--from <file> | --stdin) into the Pillar-2 handover sections via the SAME ConvertFrom-SpecrewHandoverFile reader a resume uses (tolerant lead-phrase header matching; unrecognized headers reported + ignored), resolves feature/boundary/host from committed session state (flag-overridable), and writes via Write-SpecrewHandoverContext -> the shared atomic writer (honors the centralized clobber guard, keeps .old crash backup). Registered in specrew.ps1 dispatch + Show-Usage + FileList; the FR-022 bootstrap directive now NAMES this reachable command. Proven end-to-end (HandoverAuthorCommand.Tests: round-trip incl. interpretive sections SC-012, tolerant headers, dispatch arm, unrecognized-skip, clobber-guard preservation SC-015, --stdin)."
    status: "done"
    started_at: "2026-06-14T04:00:00Z"
    completed_at: "2026-06-14T05:00:00Z"
    blocked_reason: ""
  T002:
    title: "Mechanical Stop-hook packet capture (A2, LOAD-BEARING) — capture the VERBATIM rendered boundary packet into the body + set active_boundary; non-skippable. Reader Get-SpecrewCapturedBoundaryPacket (marker-tied, RAW -Raw read so the six '## ' headers + newlines survive; substantive-content floor, NOT six exact headers) + a THIRD section-ownership category (excluded from both mechanical and agent-owned) + the parser made captured-section-verbatim-aware (a '## ' line closes it only on an EXACT canonical title, so the packet's own '## ' do not shred it) + forward-only active_boundary from the marker FROM (never regress) + a [FROM..TO] freshness/stale guard. Captured into Update-SpecrewRollingHandover, fail-open. Proven end-to-end (HookPacketCapture.Tests, realistic six-section packet)."
    status: "done"
    started_at: "2026-06-14T00:40:00Z"
    completed_at: "2026-06-14T01:33:49Z"
    blocked_reason: ""
  T003:
    title: "Clobber guard — capture MUST NOT overwrite a richer authored body with placeholder/stale (SC-015). CENTRALIZED in the shared Write-SpecrewRollingHandoverContent so BOTH writers (hook floor-writer AND the agent body-author Write-SpecrewHandoverContext exposed in T001) honor it: a later generic/no-marker/duplicate Stop PRESERVES the authored packet within its boundary; a forward boundary change REPLACES the stale one; a packet from a boundary already passed is dropped (never regresses active_boundary). Falsification cases 5/6/8/9 green."
    status: "done"
    started_at: "2026-06-14T00:40:00Z"
    completed_at: "2026-06-14T01:33:49Z"
    blocked_reason: ""
  T004:
    title: "Verdict capture from transcript — the Stop/UPS hook IS the verdict authority: recognizer + transcript reader + evidence-source tag + Update-SpecrewRollingHandover wiring (calls Add-SpecrewBoundaryAuthorization with the captured verdict, forward-only, fail-open) + gate-stop boundary marker. Proven end-to-end (HookVerdictCapture.Tests). GATE-CONTIGUITY guard added (one-boundary-at-a-time): the captured marker FROM must equal the authorized cursor AND TO must be its immediate successor, else reject + journal marker-cursor-mismatch (fixes the maintainer-found from-skip hole); 5 falsification cases green. RESIDUAL: marker emission on non-Claude hosts (they degrade safely to awaiting-verdict until added)."
    status: "done"
    started_at: "2026-06-13T23:33:28Z"
    completed_at: "2026-06-13T23:45:31Z"
    blocked_reason: ""
  T005:
    title: "Boundary-sync integrity — STOP fabrication (sync records the mechanical crossing only; never advances last_authorized_boundary, never appends/fabricates verdict_history, never git-committer). Authorization moves to the hook (T004) + explicit re-confirm (T006), per decision f174-i011-verdict-authority-stop-hook."
    status: "done"
    started_at: "2026-06-13T22:18:31Z"
    completed_at: "2026-06-13T22:35:00Z"
    blocked_reason: ""
  T006:
    title: "Committed != authorized resume + specrew where — read last_authorized_boundary decisive; surface awaiting-verdict; never auto-advance. Shared Get-SpecrewPendingVerdictState helper + specrew-where surfacing (part 1) AND bootstrap-provider resume-directive surfacing (part 2) both DONE; 6/6 tests green."
    status: "done"
    started_at: "2026-06-13T22:45:00Z"
    completed_at: "2026-06-13T23:21:24Z"
    blocked_reason: ""
  T007:
    title: "Deterministic SC-acceptance consolidation (SC-012/013/014/015). Each SC's authoritative proof was written alongside T001-T006 (SC-012/015 = HookPacketCapture + HandoverAuthorCommand; SC-013 = HookVerdictCapture + verdict-capture-blocks + boundary-sync-atomic; SC-014 = pending-verdict-surface). T007 binds them into ONE auditable, runnable acceptance (tests/bootstrap/Sc012to015Acceptance.Tests.ps1): asserts each SC's proof FILE(s) exist + RUNS each unique proof + asserts GREEN-together, and emits the SC->proof matrix. Non-duplicative (orchestrates, does not re-assert); the deterministic floor that complements the real-host re-dogfood acceptance gate. 14/14 assertions green (6 unique proof files all pass together)."
    status: "done"
    started_at: "2026-06-14T05:10:00Z"
    completed_at: "2026-06-14T05:30:00Z"
    blocked_reason: ""
  T008:
    title: "DF-1 pointer-mode recap synthesis — push pointer-mode hosts to synthesize a decisions recap, not just lens names (first to defer if overrun)"
    status: "planned"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T009:
    title: "DF-2 version/branch carried in the bootstrap directive so the pointer-mode banner is complete (first to defer if overrun)"
    status: "planned"
    started_at: ""
    completed_at: ""
    blocked_reason: ""
  T010:
    title: "Hook-deploy Layer 1 (FR-028, SC-016) — proactive provisioning at init+update for ALL hook-capable registry hosts (claude/codex/copilot/cursor), not PATH-detected only. Get-SpecrewHookCapableHosts registry helper (manifests carrying RefocusHookBindings; capability boolean ONLY — events stay from Get-HostEventGroups) + Invoke-RefocusHookDeployment iterates it (claude gated on .claude/ existence, user-level hosts always). Preserve user entries, replace only Specrew-owned, respect opt-outs, fail open, per-machine launcher provisioned even with no host binary present. Tests folded (refocus-deploy.tests 16-18). Commit 1f9b83fb."
    status: "done"
    started_at: "2026-06-14T01:45:00Z"
    completed_at: "2026-06-14T02:00:00Z"
    blocked_reason: ""
  T011:
    title: "Hook-deploy Layer 2 (FR-028, SC-017) — `specrew hooks status|install|remove [--host]` command (dispatcher-only, NO project-setup gate, runs in a broken project) + non-mirrored Get-SpecrewHooksStatus inspector (installed/missing/stale/opted-out/failed; stale keys on stable ownership tokens — launcher / ${CLAUDE_PROJECT_DIR} dispatcher). install (no host) provisions missing + respects/reports opt-outs; install --host (or --force) clears that opt-out; remove records opt-out. Registered in specrew.ps1 dispatch + FileList + Show-Usage. Tests folded (specrew-hooks-command.tests). Commit dea3540c."
    status: "done"
    started_at: "2026-06-14T02:00:00Z"
    completed_at: "2026-06-14T02:15:00Z"
    blocked_reason: ""
  T012:
    title: "Hook-deploy Layer 3 (FR-028, SC-018) — degradation diagnostic. Get-SpecrewHookDegradationWarning warn-once gate + Test-SpecrewBootstrapDirectiveArrived (fail-open toward silence); `specrew hooks status` surfaces the note (-Peek); self-check on the managed Squad copilot-instructions template (copilot's always-loaded surface). general.md NOT used (≤600-token core is budget-locked + hook-injected). Honest residual: claude/codex/cursor get no AUTOMATIC in-context warning when hooks fully absent — Layer 1 prevents the gap; `specrew hooks status` is the manual mitigation (documented in FR-028). Tests folded (hook-degradation-diagnostic.tests). Commit 457a398d."
    status: "done"
    started_at: "2026-06-14T02:15:00Z"
    completed_at: "2026-06-14T02:30:00Z"
    blocked_reason: ""
