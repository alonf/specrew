# Skills-Loading Warning Documentation + Proposal 072 Cleanup

This docs-only chore documents the upstream Copilot CLI startup warning that reports failed skill loads when home-directory skills from other tools use colon-delimited names, adds a troubleshooting note to `docs/user-guide.md`, corrects the dead `SIGNATURE_VALIDATION_ROOT_CAUSE_ANALYSIS.md` references in Proposal 072 via Option B, and updates `docs/operations/psgallery-release-credentials.md` so the removed Authenticode-signing flow is described as historical rather than current.
