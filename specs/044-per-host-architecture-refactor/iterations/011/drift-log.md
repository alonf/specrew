# Iteration 011 Drift Log

**Feature**: F-044 | **Iteration**: 011 — Host Menu Priority Ordering (Smoke-Test Bug Fix)

## No drift events

iter-011 is a single-root-cause bug-fix with user-specified scope (Option 1: priority field + two-defaults model). Plan written, executed, closed — zero surprises.

## Note: this iteration's bug-fix shape

The bug was found in **smoke-test prep** (user investigating menu behavior before running). This is the second smoke-test-prep regression discovery in F-044 (the first was iter-009's bare-URI). Pattern: pre-test rehearsal is a high-signal moment for catching UX regressions even when no manual test will actually run.

Calibration data point for future methodology proposals: "smoke-test-prep dogfood mode" might warrant a methodology directive separate from the live test mode.
