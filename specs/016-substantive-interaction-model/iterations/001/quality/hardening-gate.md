# Hardening Gate: Iteration 001

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/016-substantive-interaction-model/spec.md`
**Iteration Ref**: `specs/016-substantive-interaction-model/iterations/001`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: `(pending hardening review)`
**Overall Verdict**: `ready`
**Approval Ref**: `—`
**Reviewed By**: Alon Fliess
**Reviewed At**: 2026-05-14

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Authorization-text capture and `.squad/decisions.md` entry integrity without unintended sensitive-information leakage into public artifacts; paired-authorization auto-generation must preserve verbatim user text but prevent accidental exposure of credentials, private decision context, or internal approval chains when artifacts are published. | `true` | FR-008 requires auto-generating authorization entries from user pastes with verbatim authorization-text capture. Risk 8 explicitly flags that substantive handoff content (which summarizes artifact decisions) must not unintentionally expose sensitive information. Implementation must verify: (1) Authorization entry schema preserves user text faithfully without redaction, (2) paired-authorization expansion from one user paste creates two distinct entries without information loss, (3) `.squad/decisions.md` entries themselves are treated as internal governance records and not accidentally shipped into public README/release artifacts, (4) substantive-content rules applied to boundary handoffs do not incentivize Squad to include sensitive data that belonged only in artifact bodies. | `—` |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Validator behavior under malformed artifacts, incomplete `.squad/decisions.md` state, corrupted exemption-list extensions, and edge-case authorization shapes; Squad behavior when auto-generation fails to parse user authorization text. | `true` | FR-006 hard-fail rule detects missing intervening authorization between boundary commits, but must define clear semantics when `.squad/decisions.md` is incomplete, corrupted, or missing. FR-008/FR-009 auto-generate authorization entries from user pastes, but must handle malformed input (unclosed quotes, unexpected structure, multi-boundary pasts that cannot be clearly paired). FR-016/FR-018 implement parameterized bare-path detection with exemption-list lookup, but must gracefully handle missing exemption-list keys in `.specrew/config.yml`, malformed YAML exemption entries, or removed exemption contexts that cause stale-entry orphans. Implementation must specify: (1) What validator FAIL message is emitted when `.squad/decisions.md` is unparseable? (2) Does auto-generation reject malformed authorization pastes or emit warnings while creating best-effort entries? (3) How does the bare-path exemption list behave if `.specrew/config.yml` contains invalid YAML or removed exemption contexts? (4) Rollback/recovery path if `.squad/decisions.md` entry generation partially succeeds (e.g., first paired entry created, second fails). | `—` |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Validator idempotency across repeated rule-execution runs and partial-state recovery; no duplicate validation findings, no rule-order dependencies, no transient state leakage between runs. | `true` | Feature 016 adds five new validator rules (two hard-fail, three soft-warning) and auto-generation of `.squad/decisions.md` entries. The validator must remain mechanical and re-runnable without state side effects. Implementation must enforce: (1) `validate-governance.ps1` runs can be executed repeatedly on the same iteration without emitting duplicate FAIL warnings or corrupting `.squad/decisions.md`, (2) individual rule execution order does not affect final verdict (rules are independent), (3) partial-state recovery: if validator execution is interrupted during a run, subsequent runs re-parse the full state and do not assume cached partial results, (4) authorization-entry auto-generation is idempotent or explicitly deduplicates paired entries if the same authorization paste is processed twice. | `—` |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Integration test fixtures must exercise validator rules through real scaffold-replay-path execution, not only helper function mocks; test coverage must include both Iteration 1 soft-warning severity of `bare-path-in-boundary-handoff` (FR-016 part 1) and the deferred Iteration 2 hard-fail promotion (FR-016 part 2, configuration flip). | `true` | Feature 016 defines two hard-fail rules and three soft-warning rules that must be observable through `validate-governance.ps1` command output. FR-021 defers integration test implementation to Iteration 2, but Iteration 1 planning must establish the fixture scaffolding. Implementation must commit to: (1) scaffold-replay-path fixtures (compliant and violating) for each new rule, stored in `tests/unit/fixtures/` or `tests/integration/` as appropriate, (2) explicit test cases exercising both Iteration 1 soft-warning severity of `bare-path-in-boundary-handoff` and the placeholder for Iteration 2 hard-fail severity (using parameterized severity config), (3) fixtures must invoke real `validate-governance.ps1` or the governance-validator helper script, not only internal parsing functions, (4) exemption-list fixtures must verify that approved exemption contexts (shell commands, inline code, JSON/YAML, regex) do NOT trigger warnings while bare paths outside those contexts DO trigger, (5) test fixtures must be replayable deterministically from committed fixture files without requiring live commit history or external services. | `—` |
| `operational-resilience-concerns` | `operational` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Validator performance budget (hard-fail rules <200ms, soft-warning rules <50ms each) and coordinator-prompt line budget (≤150 additional lines) must be met without regression to existing validator runtime or prompt clarity. | `true` | NFR-001 commits hard-fail rules to <200ms completion and soft-warning rules to <50ms each, with no noticeable impact on total `validate-governance.ps1` runtime. NFR-002 commits coordinator-prompt updates to ≤150 new lines. Implementation must verify: (1) Baseline runtime measurement of existing `validate-governance.ps1` on a representative Specrew repository state (e.g., this repository at spec-finalization commit), (2) post-implementation re-measurement showing new hard-fail rules complete within 200ms budget and soft-warning rules within 50ms each, with total validator runtime not exceeding baseline + 15% tolerance, (3) existing test runs (e.g., `tests/integration/` suite) remain green and do not timeout, (4) coordinator-prompt diff against current baseline counts new lines only, not modification or deletion of existing guidance, and confirms the addition stays within 150-line envelope, (5) any latency-critical path (e.g., decision-entry auto-generation during boundary inspection) is profiled and validated against user-experience expectations (sub-second blocking time for entry review/override flow). | `—` |

## Notes

- Human hardening-gate sign-off was granted on 2026-05-14 for Feature 016 Iteration 001.
- The concern rows remain planning-time commitments with `pending-post-implementation` runtime evidence; Expected Controls are verified at review, not at this authorization boundary.
