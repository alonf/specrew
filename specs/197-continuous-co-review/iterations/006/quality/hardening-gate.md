# Hardening Gate: Iteration 006

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/197-continuous-co-review/spec.md`
**Iteration Ref**: `specs/197-continuous-co-review/iterations/006`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: `strongest-available`
**Overall Verdict**: `ready`
**Approval Ref**: `—`
**Reviewed By**: Planner
**Reviewed At**: 2026-06-24T00:00:00Z
**Post-Implementation Verification**: pending (runtime evidence recorded per concern at iteration-closeout)
**Verified At**: `—`

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | THE SEC TRUST BOUNDARY. The navigator reviewer is a TRUSTED in-boundary component: it runs a real host (codex/claude) with repo READ access and the INHERITED process environment, inside an isolated READ-ONLY worktree. The PRIMARY control is the worktree itself: a `git archive`+`tar` content export OUTSIDE the repo with NO `.git` and NO real `.specrew/` (`New-SpecrewIsolatedTaskWorktree`), so the reviewer physically cannot mutate real repo/gate state. Secondary controls: the host's native read-only mode (codex `exec --sandbox read-only`); and the execution-engine mutation guard — but its scoping on the detached-worktree path is a T082 EMPIRICAL DECISION, not a settled control: the guard hashes fixed Specrew-OWN roots and reads `git status` (`workspace-mutation-guard.ps1`), which are absent in the reviewed-project worktree, so re-aiming it at the worktree makes it INERT and leaving it at the repo root could false-trip on a concurrent edit; the likely call is to SKIP it on this path since the read-only export is the guarantee. Surfacing the FULL findings must NOT surface raw transcripts or environment capture: the blackboard's redacted-evidence record persists findings/thread but marks `raw_provider_transcripts`, `raw_prompts`, and `secret_or_environment_capture` as not-stored (review-blackboard-writer.ps1:192-218). No new runtime dependency. | `true` | T082 settles the mutation-guard scoping against the guard body (skip-on-worktree vs. re-aim) and keeps codex `--sandbox read-only`; T083's blackboard write reuses the redacted-evidence not-stored fields; T085 proves the worktree carries no `.git`/`.specrew/` (the primary control), the chosen guard posture does not false-invalidate a real run under a concurrent repo edit, and the redacted-evidence record confirms no transcript/env capture. | `—` |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The navigator MUST NEVER throw to the F-185 dispatcher (the iteration-005 fail-open contract): a malformed/absent FindingsResult, a launcher-unavailable path, a selection miss (no authorized independent host), or a blackboard-write failure must each degrade to a clean no-op or the existing one-line summary note, never a thrown exception that perturbs the merged Stop result. Timeout, nonzero exit, empty output, and invalid JSON from a real LIVE HOST INVOCATION are handled as structured non-verdict reap outcomes (NFR-001), surfaced advisory-only and never promoted to gate evidence. The raised timeout config must clear a real codex run (iter-002: ~300s; 120s timed out) so the reap parses a real verdict rather than `timed-out`. | `true` | T082 fails open to a no-op when no authorized independent candidate exists (never the stub) and raises the timeout config; T083 wraps the blackboard write fail-open (degrade to the summary note); T085 covers the malformed-FindingsResult, no-candidate, write-failure, and timeout paths and asserts a no-op decision (and the existing structured non-verdict notes), not a throw. | `—` |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The durable record must satisfy DS-002 (`.specrew/review/inline/<run-id>/...` is the system of record) and stay idempotent across re-reaps. The reaped `pending/<run-id>/` is SEPARATE from the durable `inline/<run-id>/` (proven: `Clear-...Entry` `Remove-Item` targets only `Get-ContinuousCoReviewNavigatorRunDir` = `pending/`), so routing findings to `inline/` needs NO reap-ordering change. The blackboard writer is idempotent (re-write with identical content is a no-op; divergent content throws — `Write-ContinuousCoReviewIdempotentJson`/`Assert-ContinuousCoReviewBlackboardNoDrift`), so a double-reap of the same run does not corrupt or duplicate; run_id normalized to the registry run-id co-locates findings with the gate record under one `inline/<run-id>/`. The stub never produces a durable record (the `is_stub` guard is retained). | `true` | T083 reuses the idempotent blackboard writer, normalizes run_id, and keeps the `is_stub` exclusion; T085 asserts the `inline/` record survives the reap, a second reap is a no-op (no duplication/corruption), and the stub writes no record. | `—` |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Tests prove BEHAVIOR, not file presence, and the acceptance E2E fires through the LIVE DISPATCHER on a real host — NOT a direct function call (the iteration-005 catalog-drift / green-but-inert lesson, where fixtures stayed green while the navigator was inert on live dispatch). The E2E must stage a real MULTI-severity reviewer result (>=1 blocking + >=1 advisory/nit) so the "all severities land AND surface" claim is exercised, not asserted on a no-findings pass. Deterministic coverage asserts: candidate selection threaded into the fired `-Command`; module-base resolution; run_id normalization (findings co-located with the gate record); the worktree-scoped mutation guard; the inject note points at the thread; fail-open on malformed FindingsResult. | `true` | T085 delivers the live-dispatcher multi-severity E2E plus the deterministic behavior coverage; no file-presence-only assertions; the E2E is recorded with the real-host codex run (the raised ~300s timeout). | `—` |
| `operational-resilience-concerns` | `operational` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Reviewer-host selection stays HOST-NEUTRAL: the navigator selects through `Select-ContinuousCoReviewReviewerCandidate` (catalog-driven, code-writer-independent: claude->codex, codex->claude) with NO host-name branch in the navigator's own logic; codex is the concrete E2E host only because it is live-validated (iter-002) and confirmed installed. Per-host Stop-hook behavior is verified empirically (FR-030), not trusted from stale capability comments. The LIVE HOST INVOCATION must not regress the Stop budget or orphan a process: the fire hop stays cheap (selection in-repo, host call detached, fire-and-return — no `Start-Process -Wait`), and the iteration-005 reaper/supervisor remains the orphan backstop. THE FINDINGS-SURFACING payoff (the developer sees ALL findings via the blackboard thread, not the one-line summary) is the operational deliverable. Capacity is 14/20 with deliberate slack; the manual defer strategy is the safety valve for a first-live-run spill. | `true` | T082 routes selection through the policy with no host literal and keeps the fire hop detached/cheap; T084 surfaces the full thread at the inject note/STOP-BLOCK; T085 asserts the selected candidate is code-writer-independent, the navigator carries no host-name branch, no orphaned process/worktree remains, and the developer sees the full finding set. | `—` |

## Before-Implement Conditions

| Condition | Status | Evidence | Decision |
| --- | --- | --- | --- |
| `condition-a-human-authorization` | `pending` | The `tasks -> before-implement` boundary stop is not yet presented (iteration 006 is at the plan boundary). Implementation MUST NOT start until the human authorizes `tasks -> before-implement`. | Implementation MUST NOT start on the plan-boundary approval alone; the `tasks -> before-implement` verdict is required. |
| `condition-b-mutation-guard-posture` | `planned` | The execution-engine mutation-guard POSTURE on the detached-worktree path MUST be settled against the guard body (`workspace-mutation-guard.ps1`): leaving it at the live Specrew repo root risks a false-trip on a concurrent human edit during the ~300s review; re-aiming it at the reviewed-project worktree makes it inert (its fixed Specrew-own roots and `git status` are absent there). The likely correct call is to SKIP the in-repo guard on this path (the isolated read-only export is the guarantee). | If T082 cannot land a posture that neither false-invalidates a real run nor ships an inert-but-claimed control, STOP and escalate; do not ship a real-repo-scoped guard that trips on concurrent edits. |
| `condition-c-timeout-sufficiency` | `planned` | The co-review timeout config MUST be raised above a real codex run's duration (iter-002: ~300s; 120s timed out) before the live E2E, or the reap parses `timed-out` and no findings land. | T085's E2E MUST use the raised timeout; a timed-out E2E is not acceptance evidence. |
| `condition-d-live-dispatcher-e2e` | `planned` | The meaningful E2E MUST fire through the LIVE DISPATCHER on a real host, not a direct function call (the iteration-005 catalog-drift / green-but-inert lesson). | A direct-call dogfood does not satisfy acceptance; the live-dispatcher multi-severity run is required. |
| `condition-e-capacity-discipline` | `met` | Capacity is 14.00/20 with deliberate slack (the first live reviewer run is where surprises live; T082/T085 are not estimated to consume the cap). Defer strategy is manual. | If a task expands beyond its estimate, STOP for a human split/defer decision; do not raise the cap. The 20 SP cap is restored (iteration 005's 30 was a one-off authorized override). |

## Notes

- **Before-implement posture.** Every concern is `addressed` at `planning-time-analysis` with
  `Runtime Evidence Status: pending-post-implementation`; the `Overall Verdict: ready` reflects that the
  controls are designed and traced to tasks, not that runtime proof exists yet. At iteration-closeout
  each `addressed` concern moves to `runtime-evidence` / `recorded` with the per-concern proof captured
  here (mirroring the iteration-184 closure pattern).
- **No `tbd`.** Every concern carries a concrete Expected Control grounded in an existing, primary-
  sourced control (the isolated read-only worktree as the primary guarantee, codex `--sandbox
  read-only`, the idempotent blackboard writer, the redacted-evidence not-stored fields, the fail-open
  contract, the host-neutral selection policy). The execution-engine mutation-guard POSTURE on the
  detached-worktree path is deliberately framed as a T082 empirical decision (condition-b), not asserted
  as a settled control — the guard hashes Specrew-own roots and reads `git status`, both absent in the
  reviewed-project worktree.
- **F-184 footprint: NONE.** All controls live in the non-protected `continuous-co-review/` scripts and
  tests; no protected hook/dispatcher/registry/refocus/shared-governance surface is touched by
  iteration 006.
