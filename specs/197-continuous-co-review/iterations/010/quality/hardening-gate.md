# Hardening Gate: Iteration 010

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/197-continuous-co-review/spec.md`
**Iteration Ref**: `specs/197-continuous-co-review/iterations/010`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: `strongest-available`
**Overall Verdict**: `ready`
**Approval Ref**: `—`
**Reviewed By**: Planner
**Reviewed At**: 2026-07-02T00:00:00Z
**Post-Implementation Verification**: pending (runtime evidence recorded per concern at iteration-closeout)
**Verified At**: `—`

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The OS-native process containment (T100: Windows Job Object `KILL_ON_JOB_CLOSE` / Unix `setsid`+PGID group kill) runs the reviewer in the SAME ephemeral read-only `git archive` worktree the feature already uses — containment strengthens the kill, it MUST NOT weaken the read-only-to-source guarantee (the worktree is discarded; the real repo is never touched). Codex runs with `--dangerously-bypass-approvals-and-sandbox` ONLY inside that disposable worktree (authorized D-197-I009-009); the containment gives the atomic kill the bypass removed. The T107 reviewer-instruction fold MUST DROP the stale `read-only-composed-prompt` visibility text (it contradicts browse-the-repo) while KEEPING the secret-non-exfiltration bindings (no secrets/tokens/env/transcripts persisted or echoed). No new runtime dependency. | `true` | T100 keeps the read-only worktree as the source-mutation guarantee and adds atomic containment (no source write path); T107 preserves the secret-non-exfiltration manifest rows and drops only the stale visibility text; the fold test asserts the outbound prompt carries no secret-exfiltration relaxation. |  `—` |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The never-deadlock invariant is the iteration's spine: the tiered degraded-evidence gate (T094) NEVER blocks on "no parseable verdict" — worst case is a recorded human ack; the hard timeout (T091 + T100) MUST fail-SAFE — the watchdog kills the whole tree, records `terminal_reason`, and never hangs (the 1h12m field class); the remediation menu (T096) never blocks the Stop; the codex retry-once (T108) on a still-empty result FAILS LOUDLY (`status=failed`), never a silent false-green. All degrade to a surfaced human choice, never a dead end. | `true` | T091/T100 fail-safe kill + `terminal_reason`, WSL-validated; T094 never-deadlocks (ack floor); T096 fires a menu, not a block; T108 fails loud on a still-empty retry; tests cover the timeout, no-independent-host, degraded, and empty-retry paths as surfaced non-deadlock outcomes. | `—` |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Round/remediation state must be idempotent and bounded. The remediation choice (T096) is carried in `co-review-round-state.json` and applied to the re-run without duplication; the escalation-latch (T106) surfaces a ceiling escalation ONCE then latches quiet (a re-fire is idempotent — no re-nag) and resets on a converged checkpoint; the codex retry (T108) is bounded to ONE retry on a 0-byte result (not a loop); the session-scoped pidfile registry + SessionStart reaper (T100) reap orphans idempotently (a double-reap is a no-op). | `true` | T096 round-state carries the choice idempotently; T106 latch is surface-once + convergence-reset (idempotent re-fire), integration-tested on real transcript data; T108 retry is single-shot; T100 reaper is idempotent. | `—` |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Tests prove BEHAVIOR on the real OS, not file presence. The R5/R7 hard gate (T091/T100) is **WSL-validated** — a configured timeout kills the reviewer process TREE with NO orphaned children on WSL, not Windows-only (the standing spawn/detach rule). The escalation-latch (T106) integration test runs on REAL transcript data (surface-once-then-latch), not a fixture stub. The T107 fold test asserts the ACTUAL outbound slim prompt content (re-pointed from the retired `code-review-agent.md`), verifying every TO-FOLD manifest row present + every DROP row absent. Cross-host validation (T110) records which harnesses were ACTUALLY exercised vs unavailable — no all-five claim without all-five evidence. | `true` | T091/T100 WSL orphan-kill proof (hard acceptance gate); T106 real-transcript integration test; T107 outbound-prompt content test vs the preservation manifest; T110 honest per-harness evidence. | `—` |
| `operational-resilience-concerns` | `operational` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Host-neutrality + Stop-budget discipline hold. Reviewer selection stays host-neutral: the independence fallback (T093) fires a LABELLED same-host review immediately (never blocks) and upgrades the next run; no host-name branch in core logic. The Stop-hook stays cheap: the material-turn gate (T099) runs the conformance parse ONLY after a material turn, so trivial stops don't pay it. The atomic kill (T100) leaves NO orphan and records `terminal_reason` for observability. The fold (T107) keeps the reviewer SHARP (falsification/adversarial stance) so review quality does not regress. Capacity 24.00/26 with 2.0 slack; manual defer (T110 then T108 drop first). | `true` | T093 labelled same-host fallback (host-neutral, never-block); T099 material-turn-gated parse (cheap stops); T100 no-orphan + `terminal_reason`; T107 preserves the adversarial stance; capacity discipline with a named defer order. | `—` |

## Before-Implement Conditions

| Condition | Status | Evidence | Decision |
| --- | --- | --- | --- |
| `condition-a-human-authorization` | `pending` | The `tasks -> before-implement` boundary stop is being presented now; implementation MUST NOT start until the human authorizes it. The plan-boundary approval (Option A, `ab1b516b`) does NOT authorize implementation. | Implementation starts only on the explicit `tasks -> before-implement` verdict. |
| `condition-b-wsl-hard-gate` | `planned` | R5/R7 (T091/T100): a configured timeout MUST kill the reviewer process TREE with no orphaned children, proven on WSL (not Windows-only). This is the standing hard acceptance gate carried from iter-009 D-197-I009-006. | If the WSL orphan-kill cannot be evidenced, STOP — do not mark T091/T100 done on Windows-only evidence (the exact iter-009 over-claim). |
| `condition-c-preservation-manifest` | `planned` | T107 fold MUST satisfy every `TO-FOLD` row of the preservation manifest (`requirement-reconciliation.md` §D) — present in the slim prompt AND asserted by the re-pointed test — and confirm every `DROP` row absent. | If any TO-FOLD binding (esp. the falsification stance) would be lost, STOP; the fold's whole point is losing no critical reviewer instruction. |
| `condition-d-never-false-green` | `planned` | T108 (codex retry) MUST fail loudly (`status=failed`) on a still-empty retry — never counted as gate evidence; T094 MUST never deadlock. | A silent false-green or a hard-deadlock is an automatic STOP; correctness outranks convenience. |
| `condition-e-capacity-discipline` | `met` | Capacity is 24.00/26 with 2.0 slack under the maintainer-raised cap (OVERRIDE 4, applied after iter-009 closed). Defer strategy manual; drop order T110 then T108 if a slice spills. | If a task expands beyond estimate past the 26 cap, STOP for a human split/defer decision; do not silently raise the cap again. |

## Notes

- **Before-implement posture.** Every concern is `addressed` at `planning-time-analysis` with `Runtime Evidence Status: pending-post-implementation`; `Overall Verdict: ready` means the controls are designed and traced to tasks, not that runtime proof exists yet. Each `addressed` concern moves to `runtime-evidence` / `recorded` at iteration-closeout with the per-concern proof captured here.
- **No `tbd`.** Every concern carries a concrete Expected Control grounded in the approved design (design-analysis N1–N8): the OS-native containment on the read-only worktree, the never-deadlock tiered gate, the idempotent round-state + surface-once latch, the WSL hard gate, and the host-neutral fallback + material-turn budget gate.
- **F-184 footprint: NONE.** All controls live in the non-protected `continuous-co-review/` + `agent-tasks/` scripts and tests; the navigator provider edited by T096 is F-197-owned (confirm via the protected-surface guard before committing).
