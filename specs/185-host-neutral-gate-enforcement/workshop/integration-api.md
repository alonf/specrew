# Integration & API - Feature 185

Three contracts:
1. a host-appropriate **refocus-digest render** (harness-free);
2. a **gate-provider decision** (`PreToolUse permissionDecision` where available, the host's strongest lever otherwise);
3. the **`SPECREW-VERDICT-BOUNDARY` marker** as the host-neutral verdict-capture contract.

Confirmation: human-confirmed (lens-question).