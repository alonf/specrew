# Architecture - Feature 185

Capability-matrix **enforce-or-halt** built on the EXISTING dormant `kind=='gate'` dispatcher seat plus Proposal-065 authorization (`start-context.json`/`verdict_history`). Harness-free cleaning of the all-host refocus digests. Each host uses its strongest available lever: Claude `PreToolUse`; Antigravity `PreInvocation`/`Stop`; truly hookless degrades to cooperative instruction plus a mandatory halt.

**Split guard**: stop for a maintainer split/defer verdict if real enforcement requires a broad host-model rewrite rather than a bounded gate-provider + digest + fallback change.

Confirmation: human-confirmed (lens-question).