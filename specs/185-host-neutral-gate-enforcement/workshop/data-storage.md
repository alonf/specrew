# Data & Storage - Feature 185

Reuse `.specrew/start-context.json` (`boundary_enforcement` + `verdict_history`) via `SessionStateAccessor`. The `SPECREW-VERDICT-BOUNDARY` marker is the verdict-capture mechanism. Per-host capability is a static field in `host.psd1`. No new persistent store.

Confirmation: human-confirmed (lens-question).