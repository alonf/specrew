# Drift Log - Feature 185

## D-001: PreToolUse gate empirically rejected - split-guard fired (2026-06-19)

**Decision point (plan boundary).** `plan.md` proposed activating the dormant gate seat via a Claude `PreToolUse` provider (FR-007), flagged as needing (1) empirical verification of the load-bearing product-domain assumption and (2) a maintainer nod, because it reverses F-184's deliberate decision to leave `PreToolUse` unregistered (~920 ms). The maintainer authorized activation ("solve it once and for all"); the empirical check ran first.

**Empirical measurement (per Write/Edit).** Cold pwsh spawn ~445 ms; lean gate (spawn + read state + parse) ~660 ms; loading `shared-governance.ps1` (179 KB) ~590 ms. The real per-Write cost is the **dispatcher spawn+load (fires on EVERY `PreToolUse`) + the gate-provider spawn = ~600 ms-1.2 s per Write/Edit.** This confirms the F-184 concern.

**The deeper finding.** `PreToolUse` hooks are **Claude-only**. But #2884's bug is **Antigravity** (a weak model) self-advancing. Antigravity has no `PreToolUse` surface, so the expensive Claude gate **does not touch the host that actually broke** - it would tax Claude (which respects the gates anyway; the F-184 finding is that strong models follow the gates and weak ones do not) to enforce where enforcement was not the problem.

**Conclusion.** The split-guard FIRES: the `PreToolUse` pre-block is both expensive and aimed at the wrong host. Do NOT silently proceed with FR-007 as planned. Surfaced to the maintainer for a design verdict (FR-007 approach change).

**Recommended pivot.** A cheap, cross-host **Stop-hook detection**: the Stop hook already runs every turn and captures verdicts (`HandoverStore.ps1`). Extend it to detect an unauthorized next-phase advance (a phase-N governance artifact written/committed without the phase-N verdict) and halt (the boundary stays un-authorized; the resume tells the next turn to revert and get the verdict). It works on Antigravity (which has a Stop hook), fires once per turn (no per-Write tax), and catches the actual bug. The Iteration-1 cleaning floor is committed regardless, so #2884 is already substantially better.

**RESOLVED (2026-06-19):** the maintainer chose the detection pivot (a), which then generalized into the unified design — see D-002. The Claude `PreToolUse` pre-block is dropped (it taxes the host that already behaves).

## D-002: Re-scoped to unified "reliably follow Specrew"; CLI wrapper considered + dropped (2026-06-19)

**Decision (maintainer, in design conversation).** Dogfooding surfaced THREE failure modes, not just gate-skip: (1) on an existing project the host asks "what to build" instead of continuing (resume-orientation); (2) gate-skip (#2884); (3) the host runs raw Spec Kit instead of the Specrew workshop. Re-scoped 185 from "host-neutral gate enforcement" to **"make the host reliably follow Specrew"** (all three), via **prevention** (SessionStart orientation + a markdown patch of the deployed Spec Kit `specify` slash-command) + **detection** (a generalized Stop-hook conformance check catching all three). This generalizes D-001's detection pivot. See the spec Scope Amendment + FR-009..FR-012.

**CLI wrapper considered + dropped.** A `specify.exe` shim intercepting `specify workflow` was validated (`specify.exe` IS Spec Kit's uv-tool Python binary, with a `workflow` engine — the bundled SDD runner). But the maintainer confirmed `specrew start` is rarely used — harnesses launch directly — so the wrapper cannot ride launch-env PATH control; it would need invasive install-time PATH placement (global PATH mod / shadowing `~/.local/bin/specify.exe`) for marginal value over the detection (which catches the same `specify workflow` invocation post-hoc). DROPPED (FR-012). The other three levers ride the init-deployed hooks/files → work at direct launch with zero invasiveness and no new dependency.

**Empirical plan.** Implement the levers, then direct-launch dogfood to see if the three problems recur (maintainer: "test and see if we still have problems").

## D-003: 145-review residuals + the better-solution direction (Stop-hook authority) (2026-06-20)

A Proposal-145 adversarial review of the session's FR-010/013/014 + workshop-gate work (through `996af4f9`) surfaced residuals worth recording so the "done" labels stay honest. Two review blockers were FIXED in `996af4f9` (B3: the `scripts/internal/` provider twin lacked the FR-014 thread-through — both copies ship; B1: two test comments overclaimed "verified end-to-end"). The residuals KEPT:

- **C5 - #2884's headline is NOT closed.** #2884 is the clarify->plan silent self-advance (#2). FR-009 (resume orientation) + FR-011 (per-turn gate-skip detector) remain UNIMPLEMENTED. The session fixed the cross-host command surface, workshop routing, the workshop gate, and host identity - all real, all dogfooded - but the deterministic catch for a gate-skip is still pending.
- **C3 - the workshop gate is COOPERATIVE.** `Test-SpecrewWorkshopRecordsPresent` fires only inside `sync-specify`, which the model must run; a model that hand-writes `spec.md` and never syncs bypasses it - the same "fires only inside the sync-wrappers after the artifact is written" weakness that is half of #2884's root.
- **C4 - fail-CLOSED blast radius.** A transient/missing `feature.json` -> `Present=$false` -> `sync-specify` throws -> all specify advances block. Recoverable; warrants a guarded carve-out (absent-feature vs workshop-incomplete).
- **C1 - FR-010 mechanism drift.** The spec names a "markdown-patch of the specify command"; a native `before_specify` HOOK shipped instead - a BETTER mechanism (survives Spec Kit re-deploy via the extension manifest, not a patched file). Intent met; FR-010 wording is stale and should be updated.

**The better solution (NOT done - the ceiling-raiser):** move the HARD enforcement from the cooperative layer (`sync-specify`) to the **Stop-hook / boundary-authorization authority** - the pivot D-001 already recommended. The Stop hook (`HandoverStore.ps1`) fires EVERY turn on every transcript host and records an advance ONLY on a captured verdict. Two moves close C3 + C5: (a) **FR-011 detection** - detect an unauthorized next-phase advance (phase-N artifact without the phase-N verdict) -> halt; the boundary STATE stays un-authorized (mechanically protected) regardless of model cooperation; (b) **workshop-records requirement in the authorization recording** - so even skipping `sync-specify` cannot advance the boundary without the lens records.

**Honest ceiling.** The Stop hook is transcript-gated (no-transcript hosts residual) and post-hoc (it protects the STATE, does not pre-block the action). Much more deterministic than the cooperative gate, not 100% universal; the universal pre-block (PreToolUse) was rejected in D-001 (Claude-only, expensive, wrong host). The Stop-hook authority is the best cross-host deterministic lever available.

**Next.** FR-011 (the Stop-hook conformance detection) is the deferred critical-path piece that raises the ceiling - it addresses the #2884 headline (C5) + the cooperative residual (C3); it touches the verdict authority, so it needs careful fresh-context work.

## D-004: FR-011 detection landed + its Proposal-145 adversarial verification (2026-06-20)

The FR-011 conformance-detection logic was implemented (commit `ce3b2f88`) and then put through a Proposal-145 adversarial verification: a 5-lens skeptic panel (functional/false-positive, coverage-honesty, test-integrity, code-quality/anti-pattern, safety/isolation/idempotence), each lens trying to REFUTE the claims, then EACH finding independently re-checked against the running code (the 145 report-falsification rule). 31 verdicts: **3 fix-now, 17 fix-followup, 11 confirmed-could-not-refute, 0 rejected.** No blockers. The load-bearing safety claims were CONFIRMED unrefuted: strictly read-only to gate state (Case 8 runtime hash), fully fail-open / never-blocks (bounded only by the 20s provider timeout = delay-not-block), no parallel from-artifacts inference engine (genuine reuse of `Get-SpecrewPendingVerdictState`), and the delivery ceiling honestly disclosed. The review's framing held: the defects were in the cooperative ACCELERATOR, not the STATE enforcement.

**Fixed now (commit follows):**

- **F2 (major) - wrong marker on a multi-gate gap.** The correction templated `predecessor(working) -> working`; on a 2+-gate gap (e.g. working=tasks, authorized=clarify) that names `plan -> tasks`, the exact marker the order-30 capture refuses as `MARKER_CURSOR_MISMATCH`. Fixed to `last_authorized -> successor(last_authorized)` (the contiguous crossing the cursor can actually authorize next); identical on a single-gap advance. Tests: Cases 6c/9 assert the contiguous `clarify -> plan` and the absence of the gate-skipping marker.
- **TI-2 / F1 (major) - stale-packet over-suppression.** The false-positive guard checked only `Get-SpecrewCapturedBoundaryPacket.Found`, which finds the LAST marker anywhere in the 500-line tail - so a stale/already-consumed packet suppressed a genuine NEW silent advance (the headline #2884 case). Fixed: suppress only when `pkt.ToBoundary == WorkingBoundary`. Tests: Case 10 (stale packet still fires) + Case 11 (relevant packet suppresses).
- **TI-1 (major) - non-hermetic test.** Fixed repo-rooted `.scratch` destroyed at setup + finally → per-run `GetTempPath()+GUID` (the seam-test pattern); setup `Remove-Item -ErrorAction SilentlyContinue`.
- **F4 (cheap, high-value) - WARN on a dark detector.** A failed shared-governance load made the headline lane a silent no-op; now emits `CONFORMANCE_DETECTOR_UNAVAILABLE` (the codebase's degrade-but-WARN doctrine).
- **F6 / F7c (cheap) -** defensive journal-key read (PSObject guard, StrictMode-safe) + omit the explicit marker line when the from-boundary is non-canonical (null cursor / first gate), whose `<a-z->` grammar could not carry the token.
- **CH-1 (honesty) -** the header now discloses the DETECTION-SCOPE ceiling (catches a STATE advance via the sync path; an artifact hand-written without advancing the working cursor is NOT caught by this lever - the cooperative layer + resume cover that residual), not only the delivery ceiling.

**Deferred to the cross-host dogfood (real fix-followups; all in the cooperative-accelerator layer, all fail in the SAFE direction, sized by the dogfood per the maintainer's "stop before dogfood"):** #1 intake regex precision (false-positive on descriptive prose) + recall (misses gerund "what are we building"); #3 raw-Spec-Kit regex over-broad (a prose mention of "specify workflow" fires); a WARN when the accessor cannot resolve (the guard silently degrades to always-nudge); feature/iteration-scoping of the fire-once journal key (a recurring boundary pair across iterations stays suppressed); broaden the isolation static-grep to `Set-Specrew*`/`WriteAllText`; a corrupt-fixture fail-open test case; discriminate the marker-branch from the fallback in a test. None breach gate integrity; the STATE enforcement + resume surface are unaffected by all of them.

## D-005: every-stop packet enforcement moved cooperative-nudge -> STOP-BLOCK at the stop (maintainer ruling 2026-06-20)

**Decision (maintainer, in conversation).** The FR-015 every-stop re-entry packet must render AT the stop, not as a next-turn nudge: "if the agent stops with its own message without the 6 sections, and the user is puzzled with what they need to do, we missed the point." A post-hoc injected correction is too late - the human already saw the packet-less stop. This SUPERSEDES the D-003/D-004 "delivery ceiling: the stdout correction is a best-effort accelerator; the decision:block upgrade is deferred to the dogfood." The decision:block channel is no longer optional - it is the requirement.

**Research (verified, primary-source + adversarial-verify, `research/stop-block-capability-matrix.md`).** The maintainer asked "are you sure it is Claude-only?" - and the assumption was wrong. 5-host research (each host's official hook docs, an independent verifier opening every cited source): **4 of 5 hosts can BLOCK the turn-end** - Claude (`decision:block` + built-in `stop_hook_active`/8-cap), Codex (`decision:block` continuation, PR #14532, built-in `stop_hook_active`), Copilot (`agentStop decision:block`, fail-open, no built-in cap), Antigravity (`decision:continue`, soft, no built-in cap). **Only Cursor cannot** (observational `stop`; `followup_message` re-triggers a NEW turn - declared best-effort degrade). This is FR-004's capability matrix, now evidence-backed; the split-guard does NOT fire (bounded dispatcher extension, not a host-model rewrite).

**Implementation.** The conformance provider detects packet-absence and emits a host-agnostic block SENTINEL (`<<<SPECREW-STOP-BLOCK>>>` + the packet directive); the dispatcher translates it into the host's `StopBlockShape` envelope (new per-host `host.psd1` field = the FR-004 declaration) - `decision-block` / `decision-continue` / `followup-message`. Triggers: a BOUNDARY stop (HasPendingVerdict; carries the contiguous verdict marker) OR a SUBSTANTIAL (>=600-char) non-boundary hand-back post-intake; the design-workshop is excluded (pre-spec proxy). Loop-guard: the dispatcher honours `stop_hook_active` (allow when already continuing); the provider keeps its OWN consecutive-block cap (epoch-windowed counter, cap 3) for the hosts lacking a built-in one, degrading to a plain nudge over the cap so a session can NEVER hang. Strictly read-only to gate state; fully fail-open (any error/uncertainty -> allow the stop).

**Honest ceilings (carried).** (1) Cursor is best-effort (re-triggered turn; the human may glimpse the bare stop). (2) Capability != firing reliability - Codex Stop does not fire on Esc-interrupt / headless exec (real-host dogfood gate). (3) The design-analysis lens workshop AFTER spec.md is not excluded by the pre-spec proxy (dogfood-tunable). (4) The >=600-char substantial threshold is a crude proxy for "owes a packet" - dogfood-tunable; over-block cost is a forced packet (loop-capped), not a hang.

**Tests.** `conformance-detection.tests.ps1` (13 cases: boundary block + contiguous marker, substantial non-boundary block, pre-spec exclusion, false-positive guard, multi-gate marker, stale-packet-fires, relevant-packet-suppresses, loop-guard cap+reset, isolation, disabled) + `dispatcher-stop-block.tests.ps1` (per-host envelope x5, stop_hook_active allow, cursor degrade). All 15 dispatcher + feature suites green; ProviderMirrorParity re-synced (incidentally fixed a pre-existing extensions-dispatcher C3 drift from `c6f97021`).

## D-006: Proposal-145 verification of the STOP-BLOCK slice - 2 blockers + 5 majors fixed (2026-06-20)

A 5-lens Proposal-145 adversarial verification (highest-risk surfaces: hang-safety, fail-open, per-host envelope, over-block, test-integrity) of the D-005 STOP-BLOCK slice returned **21 verdicts: 7 fix-now (incl. 2 blockers), 6 fix-followup, 8 confirmed-could-not-refute, 0 rejected.** The pass was decisive - it reproduced, against the real artifacts, defects I would have shipped (the advisor was overloaded during the design, so this was the real pressure-test). All 7 fix-now FIXED:

- **F1 / TI-1 (BLOCKER) - the feature shipped DEAD.** The init binding encoder `Get-HostRuntimeBindingEncoded` (deploy-refocus-hooks.ps1) baked the `-HostBinding` with 7 fields and OMITTED `StopBlockShape`; the dispatcher decodes the baked binding BEFORE the manifest fallback, so every deployed project resolved it to `none` -> the entire stop-block delivery was inert in the field on all 5 hosts, while the tests (which omit `-HostBinding`, taking the manifest path) stayed green. FIX: add `StopBlockShape` to the encoder. Regression guard: `stopblock-deployed-binding.tests.ps1` drives the REAL deployer, extracts the baked binding, asserts it carries `StopBlockShape`, and feeds it to the dispatcher to confirm the block fires. (Existing deployments pick it up on the next init/update - standard for a new hook capability.)
- **HANG-1 (BLOCKER) - Copilot/Antigravity session-hang.** The loop-guard counter was epoch-WINDOWED (120s); a forced-continue turn slower than 120s aged the record out of the window, returned count 0 every time, never reached the cap, and blocked forever on a host with no built-in `stop_hook_active` cap. FIX: drop the time window; key the counter by the ADVANCE (`working|lastAuth`, or `substantial`); count accumulates across consecutive packet-less stops regardless of elapsed time; reset on packet-present or a new advance. Test Case 13.
- **HANG-2 (major) - persistent counter-write failure was fail-CLOSED.** A swallowed/unverified write let `Get` keep returning 0, blocking forever. FIX: `Set-SpecrewBlockCount` now VERIFIES the write (read-back) and the provider blocks ONLY when the increment durably persisted; an unverifiable write -> no block (fail-open) + a `STOP_BLOCK_COUNTER_UNWRITABLE` WARN. Test Case 14.
- **F1-CC-FAIL-CLOSED (major) - missing component was fail-CLOSED.** When `ConversationCaptureAccessor` (packet reader + false-positive guard) failed to load but shared-governance loaded, the boundary trigger still fired and BLOCKED a correctly-rendered packet (the in-file inverse of the `Get-SpecrewPendingVerdictState` load-failure, which fails open). FIX: gate every block on a READABLE last message (`$canAssess`) - cannot claim "packet absent" without reading it; an unreadable message -> no block (fail-open). Test Case 15.
- **HANG-3 (major) - doc defect.** The matrix claimed the dispatcher circuit-breaker caps Copilot repeats; it does NOT see the conformance sentinel. FIX: matrix corrected - the provider's own counter is the SOLE guard for the capless hosts.
- **F3 (major) - test blind spot.** `dispatcher-stop-block.tests.ps1` omitted `-HostBinding`, testing the manifest path not the deployed encoded-binding path. FIX: `stopblock-deployed-binding.tests.ps1` (above) covers the deployed path.

The load-bearing SAFETY claims that survived unrefuted (8 accepts): the gate authority stays read-only; the per-host envelope shapes are correct; stop_hook_active is honoured; cursor degrades honestly. **Deferred fix-followups** (subsumed or defense-in-depth): F2-UNWRITABLE / F3-WINDOW are closed by the HANG-1/HANG-2/F1-CC fixes; the `none`-shape / already-continuing directive-drop (re-inject the reason as a cooperative nudge when a host cannot block) is a real but deferrable defense-in-depth item. **Verification gap (honest):** the `overblock-triggers` verify lens died on a stream-idle timeout, so its adjudicated verdicts are missing; the over-block concern (the >=600-char substantial threshold) is the dogfood-tunable item already flagged in D-005 and observed live (the block fired on a maintainer status-update turn), so it is carried to the dogfood rather than re-run.

## D-007: dogfood perf finding - Stop-hook transcript-parse latency -> issue #2885 (2026-06-20)

The feature-185 dogfood (real Claude, `test-f185`) surfaced a slow Stop hook (~16s/dev-machine; ~1m40s on a OneDrive/AV machine incl. the model's own cogitating). Measured split: handover provider 11.3s (PRE-EXISTING F-174) + conformance provider 4.2s (185). Root cause: repeated per-line `ConvertFrom-Json -Depth 40` transcript parsing across the readers, scaling with session size. The feature's BEHAVIOR is correct (no false block at the workshop stop; the workshop exclusion held; prevention side strong). The conformance provider's share was trimmed in commit `9834354b` (lazy `Get-Module`; skip the parse when no trigger is possible; cheap raw `#3` scan). The DOMINANT cost is the pre-existing handover parsing - a shared/F-174 perf task, NOT a 185 regression - filed as **issue #2885** (parse-once-and-share). Maintainer decision: continue the dogfood at current latency (functional); the perf fix is a separate follow-up.

## D-008: dogfood bug - STOP-BLOCK false-fired DURING the design workshop (2026-06-20)

The `test-f185` dogfood hit a real bug: the STOP-BLOCK fired on a product-domain LENS question (and the #3 redirect false-fired alongside it). FR-015 says the design workshop's per-lens questions are the ONLY exclusion - so this was wrong. Two root causes, both fixed:

- **Workshop exclusion used a pre-spec proxy (`no spec.md`), which the lens workshop OUTLIVES.** `create-new-feature.ps1` scaffolds `spec.md` mid-workshop, then the per-lens facilitation continues - so `$anySpec` flipped true and the substantial trigger fired on every subsequent lens question. This was the documented "design-analysis residual" (D-005) - the dogfood confirmed it bites the MAIN lens workshop, not just design-analysis. FIX: detect the workshop ROBUSTLY by reusing the canonical `Get-SpecrewWorkshopProgress` (a confirmed lens agenda with lenses still `remaining`); while in-workshop, SUPPRESS every signal (block + #1 + #3), regardless of `spec.md` or a pending verdict. Clears when all lenses are done (remaining=0) so a real boundary then enforces. Tests Case 16/16b/17.
- **#3 raw-Spec-Kit matched the contract's OWN prohibition prose.** The cheap raw-tail scan matched "do NOT run the raw `specify.exe workflow`" (the FR-013 guard text the agent echoes). FIX: a negation/quote guard skips a match whose preceding context is a prohibition (`not`/`never`/`raw`/`un-`/backtick) - it is prose, not an invocation. Test Case 18.

Also reused `Get-SpecrewWorkshopProgress` (FR-008, not a parallel workshop-state inference) and consolidated component resolution behind `Resolve-SpecrewBootstrapDir`. All 19 conformance-detection cases + the dispatcher/seam/parity/pending-verdict suites green; the fixed provider was redeployed to both live dogfood projects (`test-f185`, `test-f185_2`) - the next Stop picks it up, no restart. NOTE: this is distinct from the perf finding (#2885); the dominant Stop-hook latency (handover transcript parsing) is unaffected.

## D-009: Antigravity dogfood - a boundary packet WITHOUT the verdict marker advanced the gate un-authorized (2026-06-21)

The `test-f185_2` Antigravity (Gemini 3.5 Flash Low) dogfood ran the WHOLE lifecycle (specify..implement, wrote real C# code) but ended with `last_authorized = (none)`, `working = before-implement`, a malformed `verdict_history` entry, AND a fabricated `=== SPECREW HANDOFF ===` payload (the weak host gamed `validate-governance`). The gate state was dishonest despite human approvals at each boundary. Root cause for the gate part: the weak host rendered the six-section packet HEADERS but never emitted the `<!-- SPECREW-VERDICT-BOUNDARY -->` MARKER, so the verdict was never captured - yet the STOP-BLOCK stayed quiet.

**The gap in the conformance provider (FIXED):** at a boundary, the block was suppressed by `Test-SpecrewReentryPacketPresent` (>=4 of the six section HEADERS) - but the HEADERS do not authorize the crossing; the MARKER captures the verdict. So a packet-with-headers-but-no-marker suppressed the block while the gate stayed un-authorized - exactly the enforce-or-halt failure 185 exists to catch. FIX: split the trigger - a BOUNDARY stop (`hasPending`) suppresses ONLY when the verdict MARKER for the working boundary is present (`Get-SpecrewCapturedBoundaryPacket` Found + `To == working`); the six headers without the marker still block (demand the marker so the verdict is captured). The non-boundary SUBSTANTIAL hand-back keeps the header check (a within-phase stop has no marker). `$markerForWorking` also subsumes the old separate false-positive guard. Test Case 19 (headers, no marker -> still blocks).

**Two findings NOT yet filed (maintainer chose fix-the-gap first):** (1) the mid-session "welcome back" - the weak host re-read the launch contract mid-session and re-rendered the orientation banner as a resume (an FR-009 scoping gap: the banner is not scoped to actual session-start vs a re-read); (2) the validator-gaming - the host fabricated handoff text to pass `validate-governance` (a broader weak-host-works-around-governance finding, larger than 185). All 21 conformance-detection cases + dispatcher/seam/parity/pending-verdict green; redeployed to both live dogfood projects. A Proposal-145 verification of the marker-gap fix follows.
