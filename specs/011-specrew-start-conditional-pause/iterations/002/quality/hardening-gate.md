# Hardening Gate: Iteration 002

**Schema**: v1
**Gate ID**: `pre-implementation-hardening`
**Feature Ref**: `specs/011-specrew-start-conditional-pause/spec.md`
**Iteration Ref**: `specs/011-specrew-start-conditional-pause/iterations/002`
**Requested Review Class**: `strongest-available`
**Effective Review Class**: strongest-available
**Overall Verdict**: ready
**Approval Ref**: —
**Reviewed By**: Alon Fliess
**Reviewed At**: 2026-05-11
**Post-Implementation Verification**: *(pending post-implementation evidence)*
**Verified At**: *(pending verification)*

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `not-applicable` | `not-applicable` | `not-needed` | — | `false` | Iteration 002 Phase 4 + Phase 5 + Iteration 002 share of Phase 6 work introduces no new authentication boundaries, privilege checks, trust domain crossings, or user-controlled input paths beyond the optional `-PostRestartDirective` parameter. The parameter is prepended as user-supplied text with no sanitization or interpretation, following the existing handoff-regeneration model from Iteration 001. No new privilege escalation surfaces are introduced. | *(pending)* |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Pause-and-confirm injection must fail safely: missing detector result, malformed changed-files list, empty parameter string, and combined scenarios (parameter + pause, parameter + auto-continue) must not corrupt `.specrew/last-start-prompt.md` or duplicate directives. | `false` | Post-implementation verification will record: the pause-and-confirm injection handles missing or empty detector results cleanly, parameter prepending handles empty/null strings gracefully, existing failure paths remain intact, and the integration test lane (`T056`) completes without pause-and-confirm-related or parameter-related failures. | *(pending)* |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Re-running pause-and-confirm injection, parameter prepending, and visibility output generation must not corrupt `.specrew/last-start-prompt.md` or duplicate directives. | `false` | Post-implementation verification will record: `tests\integration\specrew-start-pause-and-confirm.ps1` confirms pause messages are idempotent across repeated runs, `tests\integration\specrew-start-parameter-handling.ps1` confirms parameter prepending does not duplicate or reorder directives, and integration test lane revalidation introduces no state churn. | *(pending)* |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Iteration 002 must ship deterministic coverage for pause-and-confirm injection, parameter prepending, detector visibility, and scaffold-replay-path assertions per test-integrity corpus row 16, then hold closure to the comprehensive six-test integration lane. | `false` | Post-implementation verification will record: `specrew-start-pause-and-confirm.ps1`, `specrew-start-parameter-handling.ps1`, and `specrew-start-end-to-end.ps1` all passed during implementation and review, scaffold-replay-path assertions for visibility output are present in T045, and the full six-test integration lane (`T056`) passed on the committed state. | *(pending)* |
| `operational-resilience-concerns` | `operational-resilience` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Pause-and-confirm and parameter features must preserve existing operational behavior: durable YAML frontmatter, unchanged script signature (except optional `-PostRestartDirective` parameter), unchanged legacy error paths, auto-continue preservation for routine resumes, and no performance regression in handoff regeneration. | `false` | Post-implementation verification will record: baseline YAML round-trip remained stable with new detector visibility field, no new package or manifest surfaces were introduced, the public `specrew-start.ps1` entrypoint added one optional parameter with default empty string (FR-006 compliance), legacy error messages remained unchanged (FR-007 compliance), and both iteration-scoped and project-wide governance validation passed on the implementation tree. | *(pending)* |
| `pause-and-confirm-correctness` | `core-functionality` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The pause-and-confirm directive must inject when detector reports changed session-loaded files, list files clearly, and allow user input before Squad's coordinator continues. False positives would over-pause and add friction; false negatives would miss the case where the user needs to inject directives. This concern is a direct closure criterion for FR-003 and User Story 2. | `true` | Post-implementation verification will record: `tests\integration\specrew-start-pause-and-confirm.ps1` passed all pause-and-confirm injection, message format, and file-list assertions; scaffold-replay-path visibility tests (T045) confirmed pause messages render correctly in handoff output; review inspection confirmed pause-and-confirm conditional logic triggers only when detector reports changes (not on every resume); and no false-positive or false-negative pause behavior was introduced in routine resumes or changed-files scenarios. | *(pending)* |
| `directive-injection-fidelity` | `core-functionality` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | The `-PostRestartDirective` parameter must prepend custom text verbatim as the first instruction before any pause-and-confirm or auto-continue logic. Prepending order is critical for user directive priority. This concern is a direct closure criterion for FR-005 and User Story 3. | `false` | Post-implementation verification will record: `tests\integration\specrew-start-parameter-handling.ps1` passed all parameter acceptance, verbatim prepending, and optional-behavior assertions; `tests\integration\specrew-start-end-to-end.ps1` passed combined scenarios (parameter + no changes → auto-continue, parameter + changes → pause-and-confirm); review inspection confirmed prepending happens before pause-and-confirm or auto-continue logic; and parameter handling gracefully manages empty/null strings without corrupting the handoff. | *(pending)* |
| `handoff-visibility-coverage` | `requirements-compliance` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Detector result, changed-files list, and custom directives must be visible in regenerated `.specrew/last-start-prompt.md` and testable via scaffold-replay-path assertions per test-integrity corpus row 16 from specs/005. Visibility output that exists only in runtime state but not in user-facing handoff prompt is insufficient. This concern is a direct closure criterion for FR-009, FR-010, and TG-006, and explicitly carries the scaffold-replay-path requirement from known-traps row 16. | `true` | Post-implementation verification will record: T048 implemented detector visibility output in `.specrew/last-start-prompt.md` as a structured field showing changed-files list (e.g., `## Session-Loaded Files Changed: .github/agents/squad.agent.md`); T045 scaffold-replay-path visibility tests invoked `scaffold-reviewer-artifacts.ps1` and `specrew-review.ps1` to assert pause messages render correctly in handoff output; review inspection confirmed visibility field is present in regenerated handoff prompt; and all visibility assertions in T044, T045, T051, and T052 passed without requiring runtime state inspection fallbacks. | *(pending)* |
| `corpus-seeding-completeness` | `requirements-compliance` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Known-traps corpus entry for auto-handoff-bypass pattern must be seeded in iteration 002 per FR-008 closure criterion; T055 seeds the corpus entry with category `governance`, broken pattern documented as "specrew-start.ps1 auto-handoff bypasses the user's ability to issue first-message directives in the new session when session-loaded files are committed between restarts", detection method via code review and user-reported friction observed on 2026-05-11, remediation guidance pointing to feature 011 implementation and FR-001 through FR-005, discovery date 2026-05-11. | `true` | Post-implementation verification will record: T055 completed corpus seeding with all required fields (category, broken pattern, detection method, remediation guidance, discovery date) present in `.specrew/quality/known-traps.md` on the committed tree, review inspection confirmed corpus row format matches existing entries, and the corpus entry is visible in the committed state at iteration 002 closeout. FR-008 makes corpus seeding a feature closure criterion; this concern blocks iteration 002 until T055 completes and corpus entry is verified on disk. | *(pending)* |
| `us1-integration-with-iter-001` | `requirements-compliance` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Iteration 002 user-facing behavior must not regress Iteration 001 auto-continue preservation for routine resumes. User Story 1 acceptance (FR-004, FR-006, FR-007) remains the baseline; Iteration 002 extends that baseline with pause-and-confirm and parameter features without breaking the routine-resume path. This concern ensures Iteration 001 and Iteration 002 integrate correctly. | `false` | Post-implementation verification will record: Iteration 001 tests (`specrew-start-change-detector.ps1`, `specrew-start-baseline-tracking.ps1`, `specrew-start-auto-continue-preservation.ps1`) still pass after Iteration 002 implementation lands; review inspection confirmed auto-continue behavior for routine resumes (no changes) is preserved; and the full six-test integration lane (`T056`) includes Iteration 001 tests alongside Iteration 002 tests to confirm no regression. | *(pending)* |

## Post-Implementation Evidence Notes

- This gate is in the pre-sign-off planning state. All `Runtime Evidence Status` fields show `pending-post-implementation`.
- Planning-level evidence is preserved in `plan.md`, while runtime evidence will be recorded during implementation, review, and closeout.
- The blocking concerns for iteration 002 (`pause-and-confirm-correctness`, `handoff-visibility-coverage`, `corpus-seeding-completeness`) must carry runtime evidence for complete-state validation after implementation.
- The `handoff-visibility-coverage` concern explicitly carries the scaffold-replay-path requirement from known-traps row 16, ensuring T045 visibility tests invoke the actual scaffold path, not just runtime state inspection.

## Deferral Note

- **Deferred work**: Comprehensive documentation updates (T057, 1 story_point) are deferred to feature closeout. Visibility output in handoff prompt and scaffold-replay-path coverage are sufficient for user comprehension in early rollout.
- **Explicitly not in scope for Iteration 002**: Additional polish work beyond the authorized 20-point slice (T043-T056).

## Hardening-Gate Status

**Overall Verdict**: ✅ **READY FOR SIGN-OFF** — Planning artifacts are complete and ready for human sign-off; implementation authorization will follow sign-off.

**Scope**: Iteration 002 Phase 4 + Phase 5 + Iteration 002 share of Phase 6 (pause-and-confirm directive injection, optional `-PostRestartDirective` parameter, detector visibility in handoff, known-traps corpus seeding per FR-008 closure criterion, scaffold-replay-path coverage for visibility output; tasks T043-T056, 20 story points); T057 comprehensive documentation updates deferred to feature closeout.

**Pre-Sign-Off Readiness Summary**: The five canonical concerns are present in the required order with honest pre-implementation evaluations for the pause-and-confirm + parameter + corpus-seeding + visibility slice. Five feature-specific concerns follow (pause-and-confirm-correctness, directive-injection-fidelity, handoff-visibility-coverage, corpus-seeding-completeness, us1-integration-with-iter-001). The nine-column schema is in use. The richer pre-sign-off convention is applied: Overall Verdict is `ready`, Reviewed By and Reviewed At are marked pending, Evidence Basis is `planning-time-analysis`, and Runtime Evidence Status is `pending-post-implementation`. Three blocking concerns are marked: `pause-and-confirm-correctness` (FR-003 closure criterion), `handoff-visibility-coverage` (FR-009, FR-010, TG-006, and scaffold-replay-path requirement from known-traps row 16), and `corpus-seeding-completeness` (FR-008 makes corpus seeding a closure criterion). The Iteration 002 slice builds on Iteration 001 detector and baseline tracking infrastructure, which is available after Iteration 001 closeout commit `fb926fe`.

**Next Action**: Await explicit human sign-off on this hardening-gate artifact before implementation authorization.

## Sign-Off Evidence

**Authority**: Alon Fliess
**Reviewed By**: Alon Fliess
**Reviewed At**: 2026-05-11
**Evidence Statement**: I sign off on the iteration 002 pre-implementation hardening gate at file:///C:/Dev/Specrew/specs/011-specrew-start-conditional-pause/iterations/002/quality/hardening-gate.md. The five canonical concerns are present in the required order with honest pre-implementation evaluations for the pause-and-confirm + directive-injection + visibility + corpus-seeding slice. The five feature-specific concerns follow (pause-and-confirm-correctness, directive-injection-fidelity, handoff-visibility-coverage, corpus-seeding-completeness, us1-integration-with-iter-001), the nine-column schema is in use, the iter-005-of-008 richer pre-sign-off convention is applied, the blocking flags correctly identify the three highest-risk concerns (pause-and-confirm-correctness, handoff-visibility-coverage, corpus-seeding-completeness) as Blocking: true, and the validator passes. Your self-correction in commit 6124e09 (restoring T055/FR-008 corpus seeding to iteration 002 scope after detecting it had been dropped in the initial planning) is exactly the corpus-driven behavior change the dogfooding is meant to produce; it does not affect this sign-off and should be noted as a positive signal in the iteration 002 retrospective.

---

**Hardening-Gate Planning Status**: ✅ **READY FOR SIGN-OFF** — Planning artifacts complete; awaiting human sign-off and implementation authorization. Post-implementation evidence will be recorded during review and closeout.
