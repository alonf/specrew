# v0.24.0-beta.1 Smoke Checklist

**Schema**: v1
**Feature**: `024-slash-command-multi-host-correctness`
**Version Under Test**: `v0.24.0-beta.1`
**Status**: `partial`
**Scope**: Verify the prerelease restores truthful slash-command discoverability for Claude Code and GitHub Copilot CLI while preserving host-neutral `.agents/skills/` deployment and safe legacy migration.

## Preconditions

- [ ] Install the `v0.24.0-beta.1` prerelease into a clean PowerShell 7 session.
- [ ] Start from a clean repository that does not already contain `.claude/skills/`, `.github/skills/`, `.agents/skills/`, or seeded legacy `.copilot/skills/specrew-*` content.
- [ ] Confirm the active validation claim remains limited to Claude Code + GitHub Copilot CLI discoverability.

## Automated Checks

- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-distribution.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-discovery.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-compatibility.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-coexistence.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-multi-path.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-frontmatter.tests.ps1`.
- [X] Run `pwsh -NoProfile -File tests/integration/slash-command-legacy-migration.tests.ps1`.

## Manual Smoke Flow

- [ ] Run `specrew init` in a clean project.
- [ ] Confirm `.claude/skills/`, `.github/skills/`, and `.agents/skills/` each contain the seven `specrew-*` command directories.
- [ ] Confirm each deployed `SKILL.md` begins with valid YAML frontmatter containing `name` and `description`.
- [ ] Confirm the deployed `SKILL.md` files are byte-identical across all three active roots for each command.
- [ ] Seed both Specrew-managed and unmanaged legacy `.copilot/skills/specrew-*` directories, run `specrew update`, and confirm only the managed content is removed.
- [ ] Validate that `/specrew-where` is the published command form in active runtime messaging and deployed skill content.
- [ ] Perform a manual discoverability smoke test in Claude Code or GitHub Copilot CLI and confirm `/specrew-where` appears.

## Evidence Capture

| Check | Evidence Ref | Status | Notes |
| --- | --- | --- | --- |
| Automated regression suite | `specs/024-slash-command-multi-host-correctness/iterations/001/quality/quality-evidence.md` | `complete` | Command lines and exit codes are now recorded in the iteration evidence artifact. |
| Three-root deployment verification | `specs/024-slash-command-multi-host-correctness/iterations/001/quality/quality-evidence.md` | `pending` | Include copied-path list and byte-identity confirmation. |
| Legacy migration safety | `specs/024-slash-command-multi-host-correctness/iterations/001/quality/quality-evidence.md` | `pending` | Record managed/unmanaged fixture outcomes. |
| Manual host discoverability | `specs/024-slash-command-multi-host-correctness/iterations/001/quality/quality-evidence.md` | `pending` | Limit claims to Claude Code + GitHub Copilot CLI. |

## Release Stop Condition

- [ ] Do not promote `v0.24.0` stable until every checklist item above is complete and the evidence is recorded in `quality-evidence.md`.
