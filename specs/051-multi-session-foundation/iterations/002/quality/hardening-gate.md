# Hardening Gate: Iteration 002 (Iteration 2a)

**Schema**: v1  
**Gate ID**: `pre-implementation-hardening`  
**Feature Ref**: `specs/051-multi-session-foundation/spec.md`  
**Iteration Ref**: `specs/051-multi-session-foundation/iterations/002`  
**Requested Review Class**: `phase-1-custom-composition` + required security/concurrency lens  
**Effective Review Class**: pending — security/concurrency lens fires at this before-implement gate  
**Overall Verdict**: ready  
**Approval Ref**: —  
**Reviewed By**: Specrew Crew Coordinator (security/concurrency lens to run at before-implement)  
**Reviewed At**: 2026-05-31  
**Post-Implementation Verification**: pending — collision/claim acceptance suites (T025/T026/T026b/T032/T033) + the deterministic race test + `git rm --cached` data-loss guard analogue + validator green; the per-concern Runtime Evidence Status rows below stay `pending-post-implementation` because Evidence Basis is planning-time-analysis (validator contract).  
**Verified At**: —

## Concern Review

| Concern | Category | Status | Evidence Basis | Runtime Evidence Status | Expected Controls | Blocking | Rationale | Approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `security-surface` | `security` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | `Get-MachineFingerprint` computes from local identifiers only (hostname+username; no hardware UUID/MAC, no network/telemetry — FR-043); the full fingerprint is written ONLY to gitignored `.specrew/active-sessions.yml`; committed `.squad/active-features.yml` `claimed_by` carries only the coarse `user@machine` token (no localhash). Leak-guard analogue: no fingerprint/localhash token may appear in any git-tracked file. | `false` | 2a introduces an identity-bearing committed/gitignored split; defensible only if the rich fingerprint never leaves the gitignored lock and the committed claim is no richer than the git author identity the repo already records. | — |
| `error-handling-expectations` | `error-handling` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Corrupt/invalid-YAML `active-sessions.yml` / `active-features.yml` → treated as empty + logged warning + recreated (Edge Case); missing file → empty (zero entries), no error; invalid entries (missing required data-model fields) skipped with a warning, never aborting `specrew start`; no failure path leaves a partial write. | `false` | These files are read on every `specrew start`; a parse failure must degrade safely, never crash the launch path (contract Safe-degradation invariant). | — |
| `retry-idempotency-requirements` | `retry-idempotency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | `Register-SessionLock` for an existing (feature_id+machine) entry updates last_heartbeat_time, not a duplicate; `Add-FeatureClaim` is upsert-by-feature_id (one claim per feature); `Update-FeatureClaim` advances last_refresh_time monotonically (never backward); `Remove-*` on an absent entry is a no-op success; `Clear-StaleSessionLocks` run twice clears nothing the second time. | `false` | FR-014 refresh fires at six boundaries and `specrew start` may re-run; all mutations must be safely repeatable without duplicate accumulation (SC-008 without drift). | — |
| `test-integrity-targets` | `test-integrity` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Acceptance tests assert observed reality (real YAML bytes, real git state, real timestamps) against real temp projects, no filesystem/git mocking (Shape-6). T025 collision-within-2s, T026 stale-clear+corrupt-YAML, T026b deterministic controlled-interleave race, T032 claim-lifecycle, T033 claim-warning. Coverage-evidence (T033b) records every new 2a test file as executed (closes the F-049 iter-5 / F-050 iter-2 coverage-drift trap). | `false` | The race edge (Edge Case "within milliseconds") was uncovered by the prior decomposition; T026b closes it with a deterministic single-process interleave, not flaky spawned processes. | — |
| `operational-resilience-concerns` | `operational-resilience` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | With `session_mode: single` (default) lock/claim registration is inert — existing single-session installs unaffected (Backward-compatibility invariant). Stale-lock auto-clear uses the 24h `last_heartbeat_time` threshold (configurable `stale_lock_threshold_hours`, default 24, R4) preserving multi-day/overnight branches; before threshold a warning is shown but work continues (non-destructive). Claim removal is bounded to feature-closeout-to-main (FR-016). Validator (T033b) confirms no regression from Iter 1/2a. | `false` | The 24h auto-clear is the one auto-destructive timer; it must be bounded, justified, and non-destructive before threshold. | — |
| `maintainability` | `maintainability` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | `Write-SpecrewFileAtomic` is EXTRACTED from `session-config.ps1` into a shared `scripts/internal/atomic-write.ps1` and dot-sourced by session-management + feature-claims (and reused by 2b JSON-Lines + iter-4) — no copy-paste of the temp-rename body across three call sites. session-management.ps1 + feature-claims.ps1 stay single-responsibility per the iter-1 idiom; both new modules registered in the alphabetical `Specrew.psd1` FileList as created (the v0.27.3/v0.28.0-beta.1 omission crash-class guard). | `false` | The atomic-write primitive is load-bearing and race-safety-critical; three drift-prone copies would be a real hazard. | — |
| `concurrency-correctness` | `concurrency` | `addressed` | `planning-time-analysis` | `pending-post-implementation` | Two distinct guarantees, stated honestly: (1) **torn-write safety** — every mutation routes through the shared `Write-SpecrewFileAtomic` (write-temp + `Move-Item -Force`, atomic on same volume, R3), so no reader ever observes a partial file; the file is always valid YAML. (2) **no-permanent-loss** — whole-file read-modify-write is LAST-WRITE-WINS, so a true simultaneous claim can clobber one entry in that instant; atomicity does NOT prevent this. The spec reconciles via eventual consistency: FR-014 boundary refresh re-adds the clobbered-but-active claim (Edge Case) and the resulting duplicate is surfaced (FR-015 / Edge Case). `Clear-StaleSessionLocks` is itself a RMW and MUST use the same atomic primitive. The race test (T026b) asserts this exact post-condition. | `false` | Concurrency flips from iter-1 not-applicable to load-bearing (first concurrent-write surface, FR-007/FR-012). The gate, the code, and the test must assert the SAME guarantee — NOT a false "both claims atomically recorded in one write." | deferred to before-implement security/concurrency lens for sign-off |

## Planning Evidence Notes

- **Scope**: Iteration 2a — collision detection (US3, FR-007–011) + feature claims (US4, FR-012–016) + FR-043 fingerprint privacy (population-side), 12 SP (T020–T033b). See iterations/002/plan.md.
- **Security/concurrency lens (required, fires here):** plan.md L117-132 marks the security + concurrency lens `required`; iter-1 was N/A. The lens reviews three load-bearing boundaries at before-implement: (1) identity split (rich fingerprint gitignored-only; committed claim coarse-only; local-only fingerprinting); (2) the honest concurrency guarantee (torn-write-safe + last-write-wins + FR-014 reconciliation, NOT a false atomic-both-recorded); (3) shared atomic-write reuse across all mutations incl. stale-clear. Advisory on code execution; no boundary-advance authority.
- **Blessed spec-fidelity decision (drift D-003):** the gitignored lock is local (same-machine/worktree); cross-machine collision is the committed claims file's job (FR-015). FR-005 gap fixed (T020c adds active-sessions.yml to gitignore patterns).
- **Deterministic race test** (T026b): single-process controlled interleave (read A → read B → write A → write B clobber), asserting always-valid YAML + real last-write-wins + FR-014 re-add + duplicate surfaced. NOT spawned-process timing (the anti-pattern).
- Capacity precondition: 12 SP, within the ≤20 cap; honest plan-time re-sum (workflow 2026-05-31).

## Hardening-Gate Status

**Overall Verdict**: ready — all 2a concerns addressed at planning-time with genuine controls; concurrency is now load-bearing and the honest (torn-write-safe + last-write-wins + refresh-reconciliation) guarantee is encoded for the code, the gate, and the race test to share. Final sign-off is the required security/concurrency **lens** at the before-implement gate; per-concern Runtime Evidence Status stays `pending-post-implementation` (planning-time evidence basis) until the acceptance suites + race test run post-implementation.

**Scope**: Iteration 2a — collision detection + feature claims, ~12 story_points (T020–T033b).
