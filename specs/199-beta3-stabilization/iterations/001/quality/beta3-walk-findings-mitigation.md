# Beta3 two-host walk findings — validation and mitigation

**Evidence baseline**: `ada7c793` plus the `de11559e` Copilot CLI 1.0.80 / Sonnet 4.6 walk.

**Preserved evidence**: `C:\Dev\specrew-beta3-manual-ada7c793\copilot-lifecycle` and
`C:\Dev\specrew-beta3-manual-ada7c793\claude-workshop`. These projects are read-only and
were not used as repair targets.

The failed `de11559e` Copilot project at
`C:\Dev\specrew-beta3-manual-de11559e\copilot-lifecycle` is also preserved read-only; it was
inspected for controller and receipt evidence, never repaired in place.

| Finding | Validation | Mitigation and implementation | Acceptance proof |
| --- | --- | --- | --- |
| W1 — agenda confirmation cannot converge | **True.** The writer and provider hashed different inputs; the transcript accessor collapses all whitespace, so CRLF normalization was not a fix. | Bind the ordered agenda content to `agenda_digest`; require the complete canonical block to be visible without making prose identity the authority. | Real provider path passes LF and CRLF with preamble/suffix; agenda A cannot authorize changed agenda B and the refusal names the lens. |
| W2 — confirmed controller is hand-writable | **True.** The Copilot evidence carries nested skipped objects no governed writer emits. | Require flat skipped strings, exact agenda entries, matching typed receipt, and the receipt-bound digest at every controller reader. Route every new invalid reason to a targeted terminal provider refusal. | Nested controller and flat content-substitution fixtures both return named invalid states. |
| W9 — refusal remedy cannot converge | **True, with a second defect found in the first repair.** The prior remedy could never produce the provider-side hash and prompted repeated forensics/bypass. Its first replacement converged but told the human that Specrew's “controller plumbing is broken,” exposing machinery and assigning fault without evidence. | One canonical refusal contract permits one named retry, forbids hand-editing workshop records, then calmly states what could not be completed, confirms that answers are safe, proposes one concrete next action, and asks for human approval. User-facing text never asserts that Specrew is broken; diagnosis belongs in the drift record. | AST discovery covers actionable agenda refusals and every shipped shared-contract caller with floors; mutation of one helper call makes the guard fail. A second property-derived guard discovers the canonical skill, project mirror, and four host-materialized instruction surfaces and requires the same calm contract in all six. The provider matrix automatically language-checks every exercised output carrying the shared recovery contract. |
| W3/W4/W8 — product-domain receipt covers technical decisions | **True at the instruction layer.** Copilot harvested runtime/reachability/output under one product receipt; Claude kept the boundary. | Product-domain may discover an externally fixed technology constraint only. Runtime/stack, output, timeout/reachability, concurrency, and dependencies remain in their own selected technical lens and typed turn. | Deployed workshop and lens contracts are pinned structurally; behavioral acceptance is the fresh two-host walk. Authority-layer persistence enforcement remains the recorded beta4 extension. |
| W5/W6/W8 — intake menu is impossible/decorative | **True.** Pick-feature was offered with no feature and the instruction never required a wait. | Show choices only with at least two executable actions and end the turn. With one action, announce and proceed. A concrete request in a fresh project deterministically starts New. | Bootstrap-provider test exercises the generated directive consumed by every host adapter. |
| W7 — depth selected silently | **True; ambiguous contract.** Both hosts selected different depths and merely reported them. | Crew proposes risk-based depth; the human confirms or adjusts it in the typed product-domain reply; do not persist it first. | Product lens plus deployed workshop contract carry the ruling; fresh two-host walk checks behavior. |
| W10 — product discussion and agenda outran their durable prerequisites | **True.** Copilot asked the first product question before feature setup, then showed an agenda while both product-domain records were absent. All four typed confirmations therefore remained product-domain receipts and the agenda retry could not converge. | The Stop hook refuses the first question until feature setup exists; `confirm-workshop-agenda -RenderOnly` refuses until both product records exist; a prose agenda shown early gets a targeted Stop correction to record product grounding first. Refusal language distinguishes preserved answers from “no answer recorded yet.” | RED-first production fixtures prove the original silence and wrong late error. The full conformance suite, agenda writer suite, refusal contract, typed-turn authority, initialization, consumer-language, and mirror-parity checks pass after repair. |
| W16 — reformatted agenda binds no identity and stays silent | **True.** The walk used `•` for the canonical `-`. Visibility correctly refused the digest; the Stop said nothing, so the next turn looked like a missing receipt. | Keep the exact-match visibility check. When phase is agenda, a valid proposal exists, and the shown block is not the canonical text, Stop names the rewrite immediately and asks for the command output unchanged. | The live RenderOnly path refuses a bullet rewrite at that Stop and still binds the exact canonical block. The visibility helper itself rejects the substitution. |
| W17 — picker-consumed answers mint nothing and the guard stays silent | **True.** The Sonnet 5 Copilot walk answered eleven workshop questions through `ask_user`; picker responses are tool results, never typed turns, so zero receipts were minted. The PostToolUse picker guard is unreachable on Copilot CLI (no per-tool-call hook), and the Stop classified the state as an intermediate workshop turn. The agent then misdiagnosed the silence as unwired hooks and proposed hand-writing records. | Keep the receipt requirement. At Stop, product records persisted without any typed pre-agenda receipt block with `workshop-product-records-unreceipted`: name the selection channel, keep the answers safe, route the record set-aside through human approval, and require each question re-asked as visible prose with a typed reply. Whether a record may ever be persisted before its receipt exists is an open maintainer ruling. | The shipped provider refuses the walk state (records, no receipt) at Stop with the selection-channel correction and no machinery vocabulary; the identical state behind a genuine typed receipt stays quiet. |
| W18 — a correct material block reads as an engineering interrupt mid-workshop | **True, and the enforcement is right.** The retro-calculator walk edited the feature `spec.md` during the product-domain turn, which correctly costs the turn its workshop-record-only exemption. The directive then said only "render the packet": no file named, no acknowledgement that the workshop was open, no requirement that the pending question survive. The human lost their place mid-conversation. | Keep the exemption and enforcement byte-for-byte. When a material stop fires with a durable workshop active, name the open topic and the outside work that cost the exemption, require the packet to END by re-asking the same question in full, forbid opening the next topic, and name premature spec authoring as premature. Do not widen the exemption: that would drop enforcement for genuine spec authoring during intake. | Git-backed fixture reproducing the walk (confirmed agenda, genuine receipts, edited `spec.md`): the shipped provider still blocks and names topic, path, and re-ask with no machinery vocabulary; the same stop with no workshop open keeps the ordinary directive unchanged. |
| W19 — approving a round throws away the configured reviewer | **True.** `--live --approve-round` failed after 449 s with `preflight-failed:harness` and told the human no reviewer had been chosen, at a project where copilot was configured, authorized, and had run a clean round twenty minutes earlier. The same command with an explicit `--host copilot` passed and completed clean. One guard gated two jobs: protecting a fresh approval from a spent reference (right) and, as collateral, the only host resolution from project config (wrong). | Split them. Resolve the host unconditionally; take the recorded reference only when no approval is being performed now, so the protected property is unchanged. | AST guard over the enclosing conditions of the host-resolution assignment: fails against the pre-fix file at HEAD, passes after, mutation reintroducing the coupling is caught, and the reference assignment is still required to stay gated. |
| W20 — the recorded countermeasure for that class had never been written | **True.** `scripts/specrew.ps1` states `tests/unit/review-flag-whitelist-parity.tests.ps1` derives the expected flag set from the review script's aliases. The file did not exist in the tree or anywhere in git history, so a comment asserted a closed class that was open. | Write the invariant the comment describes: derive the flag set from the review script's own parameter aliases via AST and require every one to be reachable through the front door, with a mutation proof. | 20 aliases derived, all present in the 37-entry whitelist; an added un-whitelisted alias fails the guard. |
| W21 — the pause menu offers a round it cannot grant, and consumes the answer | **True, three times in one session.** Option 1 "Fix these and run another review round" passes the round-BUDGET check (1 of 4 used), records the immutable pause decision, then dies 0.2 s later on a spent per-grant ALLOWANCE with the bare token `allowance-exhausted` and no next action. The third occurrence left the campaign with no pending pause and no round run — the wedge the file's stop-here ordering exists to prevent, reached through the other option. | Choosing "run another round" IS approving one, per the file's own stated philosophy. Option 1 now carries its own round approval and mints the derived one-slot reference. The round budget still caps it and is still checked before any answer is consumed; options 2 and 3 keep spending nothing. | Structural guard: the approval predicate must accept both spellings of option 1 and neither spend-nothing answer, the mint must stay approval-gated, and the budget refusal must precede the immutable decision. Fails against pre-fix HEAD, passes after, mutation caught. |
| W22 — FR-016 banner gloss, raised a second time and fixed | **True.** The consumer-language layer shipped complete and wired to nothing; the orientation banner still emitted bare `FR-004`, `FR-020`, `FR-022`, `FR-023`, `FR-025`, `FR-027`. Deferred to beta4 on 2026-08-11, then raised again by round `run-20260817-220959812-f183b4d8` against the frozen UI/UX design context (reviewer said blocking; the gate demoted it to minor for stating no failure scenario). | Fix rather than defer twice, on the maintainer directive to fix all issues. Gloss every ID the banner shows with what the requirement means, sourced from the owning spec, across all three shipped copies; supersede the deferral in the decisions ledger rather than deleting it. | The project's own detector runs over the banner's 381 emitted string literals and requires zero bare IDs in all three copies; fails against pre-fix HEAD naming each ID, mutation caught, and a properly described ID still passes. |
| R1 — `review.md` absent | **True and unchanged.** This repair must not manufacture independent evidence. | A real review of the fixed committed tree must produce the artifact. | Lifecycle boundary remains fail-closed until independent review exists. |
| R2 — campaign evidence stale | **True and unchanged.** The last campaign predates the candidate. | After the tree is committed and deterministic validation is green, request a fresh authorized provider round. | Current-tree campaign evidence must be valid and complete before review signoff. |
| R3 — employer in shipped comment | **True.** `review-engine-resolution.ps1` named the maintainer employer. | Preserve the OneDrive provenance using `OneDrive - <Org>`. | Review-engine test rejects the employer literal and requires the generic example. |
| W23 — a session never showed the human where the project stands | **True.** A fresh Claude session in `C:\Dev\casiocalculator` received the full orientation from the session hook, used it to orient itself, and rendered none of it. The human learned this project's rules later, at a boundary they did not know was coming, and could never correct a user-profile adaptation they were never shown. | State the obligation where it is always in context rather than adding a runtime gate: the coordinator template says a session opens by orienting the human, that reading the orientation is not rendering it, and that a concrete first request does not replace it. Materialized to `CLAUDE.md`, `AGENTS.md` and `.github/copilot-instructions.md`. | Structural guard over all four copies: each must carry the obligation, the read-is-not-render distinction, and the concrete-request clause. Fails against pre-fix HEAD. |
| W24 — a human could not close a clean review | **True, but the wrong defect.** `--pause-choice 2` records an `accept-current` disposition, and that writer refused any verdict but `findings`, so a clean round could not be disposed of. W24 widened it to accept `pass`. | Superseded by W27 and reverted. Recorded here because the reversal is the finding: W24 made a ceremony work instead of asking why the ceremony existed. | The guard W24 added now asserts the opposite, and carries the reasoning in the file. |
| W25 — instruction alone did not prove the orientation reached anyone | **True.** W23 put the obligation in both always-in-context channels, and nothing could tell whether a session had honoured it. A Stop-hook check is heavy: it runs on every stop, not only the first. | A receipt-gated lane. The first turn of a session with no orientation receipt can block; once the orientation has demonstrably reached the human the receipt is written, and every later stop costs one lookup. Lowest block priority, so it never displaces a boundary or material-work stop. | Integration guard drives a real session through first-turn block, render, receipt, and silence thereafter; a duplicate fire is idempotent. |
| W26 — a README the human asked for triggered a workshop stop | **True.** During a `C:\Dev\KeyContextAI` workshop the human asked for a README update; the material-work directive treated the write as unrecorded product work and stopped the session. From the human's side, the agent was interrupted for doing what was asked. | Exempt incidental project documentation - README, LICENSE, CHANGELOG, CONTRIBUTING, dotfiles, `docs/` - from the workshop material directive, while anything under `specs/` stays governed. A surprise is what the stop is for; a requested README is not a surprise. | Packet-language guard: a workshop-phase README write raises no material block, a `specs/` write still does. |
| W27 — the clean-review menu asked a question nothing consulted | **True.** The signoff gate already releases the boundary on a complete, current, approvable `pass`, with no disposition anywhere in the path - yet a clean round still rendered a numbered menu. Because the answer is stored as `authorized_by: human`, an unnecessary question was also a forgeable authority record, and a 2026-08-19 walk produced exactly that record without the human. | Remove the question rather than authenticate it. A clean round renders no menu, accepting a clean result is refused as deciding nothing, and a human who answers one out of habit is told it was unnecessary. Reverses W24. | The W24 fixture inverted: accepting a clean `pass` must throw, a `findings` verdict must still be acceptable, and no widening reopens the incomplete, invalid or stale cases. |
| W28 — a pause answer governed a tree it was never about | **True.** A recorded pause decision kept conferring authority after the tree it described had moved on, so an answer about one snapshot silently governed later, different code. | Supersede a pause decision once the reviewed tree has moved, and re-ask against the new snapshot. The round budget is still consulted in the superseded path: the first attempt returned authorized without it. | The existing public-command fixture ("refuses a fifth round") caught that bypass and still holds the cap across supersession. |
| W29 — an explicit design-context ref replaced the reviewer's whole frame | **True, and it destroyed a review.** The documented contract for `--design-context-ref` is "artifact to INCLUDE in the review request context"; the resolver returned only the named files. Measured 2026-08-19 (`C:\Dev\KeyContextAI`): an agent correctly diagnosed that the reviewer lacked iteration scope and passed the iteration plan through the flag. Spec, design analysis and every contract vanished. The reviewer returned "no review-blocking issues found in the frozen iteration 001 plan" in 57 seconds - the duration of a review with no code in the tree - and that run was recorded as the independent review of the implementation. The obvious, correct use of the flag was the failure. | Merge instead of replace: explicit refs lead, then everything the resolver found, de-duplicated on the repo-relative path using the volume's own comparer rather than a hard-coded case fold. | Fails against pre-fix HEAD on the walk's exact command (frame collapses to one file); passes after with spec, design analysis and contracts intact, and naming an already-resolved artifact does not duplicate it. The class-guard lane caught the first version of this fix using a hard-coded case fold, and it was corrected before landing. |
| W30 — a slice was reviewed against the finished-feature spec | **True, and it is why W29 was reachable.** Auto-resolution knew the feature spec, the design analysis and the contracts, but not the iteration's own plan - so a reviewer judging one slice was handed the whole feature's intent and nothing about the scope actually built. Reaching for `--design-context-ref` was the correct response to a real gap. | Resolve the latest iteration `plan.md` as part of the design context, so the frame is right without the flag. | Fails against pre-fix HEAD (plan absent from the auto-resolved frame), passes after. |
| W31 — a review record claimed more than its cited run supports | **True.** `review.md` asserted the independent review ran and passed against the exact tree, and closed its verification-independence gap, citing a run id. The store said that run examined one planning document; the only run that read the implementation came back `incomplete`/`partial` with 14 findings and was discarded. Existing gates check that `review.md` exists and that its gaps are classified - nothing checked whether the run it names could support the claim. | Check the citation against the run. A cited run that is not complete, current, valid and a reviewed outcome makes the record state what that run actually established. Scoped and fail-open by construction: an uncited record, a pruned store or an unreadable fact leaves it silent, so it catches an overstated claim without turning citation into paperwork. | Fires on the real KeyContextAI record naming `run-20260819-210747148-9bd5980b` and its `partial`/`incomplete` status. Silent on a clean `pass`, on `findings` (a reviewed outcome), on an uncited record, and on a run absent from the store. Mutation caught. |
| W32 — a fault field carried a success token | **True.** A completed run whose candidate declared partial coverage landed `failure_reason: "completed"` in the immutable store, because the classifier's verdict word was routed into a fault field. Read back later, that is worse than empty. | Suppress a derived failure that merely echoes the outcome. Partiality is already carried honestly by `completion` and `verdict`, which stay untouched. | A completed-but-partial ingest records no `failure_reason` while remaining `partial`/`incomplete`; an actually faulty ingest still records one. Mutation caught. |
| W33 — a review that read nothing recorded a clean pass | **True, twice in one walk.** `run-...211204294` (57s) read the iteration plan and `run-...083412478` (67s) read governance artifacts; both recorded `pass`/`complete`/`current` with zero findings against a target holding the whole implementation, against 186s for the one run that walked the source. Both said so in their own summaries — "the frozen iteration 001 plan", "the frozen iteration artifacts" — and nothing consumed it. Because the next round's baseline advances to the last accepted tree, the hollow pass then became the baseline and every later round reviewed governance files and passed again. | The candidate declares `examined_paths` (what it READ), and the controller checks that against the tree it froze: a `complete` result declaring no source, on a target that has source, is degraded through the existing controller-degrade path — cannot approve, cannot become a baseline. `Test-ReviewCitedRunEvidence` applies the same rule to a cited run. Fail-open in three places: no declaration, empty declaration, and docs-only targets all behave as before. | 9 cases plus 3 in the cited-run guard, in the slice lane. Two mutation proofs. Catches the honest-but-misframed reviewer — the case that occurred — and is not built to catch a lying one; duration heuristics were rejected as a guess dressed as a measurement. |
| W34 — nothing recorded who wrote the verdict | **True, and a class on its third appearance.** W31 checks the cited run, W33 checks it examined code, and neither asks who wrote the record. This project (2026-08-17) and KeyContextAI (2026-08-19) both have an implementer-authored `review.md`; DRIFT-199-I001-037 recorded the same signature once already and only the campaign-evidence half was fixed. The KeyContextAI case is caught by W31 because its cited run was partial; this project's is not, because its evidence is genuine. | Three parts. **B**: the hook mints an authorship fact from what it WATCHED the session write - never a field the agent supplies, which would be the same class one level up. **A**: the independence sentence is derived from the store into a block the validator recomputes, while the per-task verdicts stay authored, because deriving those from a run that never saw the tasks would trade a false independence claim for a false evidence table. **C**: a disposition whose rationale cites findings against a zero-finding run is refused at write time. | 22 cases in the slice lane, each mutation-proven - which for this slice was not a formality: the W34-C guard was INERT on landing (a sed-inserted regex whose \b became a backspace byte), parsed cleanly, read correctly, and was caught only by an assertion that drives it. See the method note in the drift log: every guard needs at least one assertion that fails when the guard is removed. This project's own record now reports `review-authorship-unobserved` - labelled, not laundered; backfilling it was rejected because commit `066c1419` carries no session identity and the label would have been an assertion. |

## Release sequence

1. Keep both old manual projects untouched.
2. Finish deterministic source, mirror, controller, instruction, refusal, privacy, and registry validation.
3. Commit the exact candidate and run the named F-198 registry, then the complete discovered-test census, sequentially on that same commit.
4. Package and install that exact commit; verify with a newest-work marker, not only the version string.
5. Only then scaffold two new build-specific projects, one for Copilot and one for Claude.
6. Run the fresh walks. The provider round at review requires explicit maintainer authorization.
