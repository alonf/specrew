# Iteration State: 007

**Schema**: v1
**Current Phase**: review-signoff
**Iteration Status**: executing
**Last Completed Task**: T060 four-harness/three-OS live proof and campaign cutover; T061 run 10 is clean evidence for its exact snapshot but signoff remains open
**Tasks Remaining**: T061 campaign review-evidence finalization, retrospective, and iteration closeout; clean Cursor proof remains unavailable until free-credit reset; FR-048/FR-049/SC-015 remains a separate Beta2 slice
**In Progress**: DRIFT-198-I007-026 controller-owned finalization-envelope correction; all 57 F-198 suites and scoped governance pass, while commit/hosted CI and one fresh independent review remain
**Baseline Ref**: 9fd802b78c9a977fcbbe5651772af800d62fb45f
**Execution Contract Ref**: d9cdd16457e322628957ea74de959a5457358852
**Updated**: 2026-07-18

## Scope

Iteration 007 completes the five-harness/three-operating-system production code-review architecture defined by file:///C:/Dev/specrew-beta2-hardening/specs/198-beta2-hardening/iterations/007/plan.md. It also carries T030–T033 and the T034b residual under file:///C:/Dev/specrew-beta2-hardening/specs/198-beta2-hardening/iterations/007/iteration-003-reconciliation.md.

FR-048/FR-049/SC-015 is not in this iteration. That command-plan supplier/injection dependency remains an explicit open Beta2 item requiring its own replanned slice before T029 or feature closeout.

## Fresh Tasks Verdict

- **Verdict**: approved for tasks
- **Evidence**: on 2026-07-16 the maintainer explicitly wrote `approved for tasks — authorize task authoring from plan commit 9fd802b7`, followed by three binding instructions.
- **Authorized plan**: commit `9fd802b78c9a977fcbbe5651772af800d62fb45f`
- **Scope**: author task/readiness artifacts, record the gate-episode drift addendum, and run traceability. Implementation remains unauthorized until a separate `approved for before-implement` verdict.
- **Instruction 1**: add the stale-session pending-verdict fabrication, divergent packet numbering, and unsafe bare-number alias evidence to `DRIFT-198-I006-001` and T033.
- **Instruction 2**: five paid slots are the best-case floor, not expected cost; every base or correction invocation requires separate human authorization.
- **Instruction 3**: keep FR-048/FR-049/SC-015 open outside Iteration 007 and block feature closeout from treating it as covered.
- **Ledger note**: the global matcher remains unfit as scoped authority under `DRIFT-198-I006-001`. This explicit verdict and its plan commit—not stale `session_state`, option numbering, or a numeric alias—authorize task authoring.

## Readiness Summary

- **Plan/capacity**: 20.25/26 story_points; 16 tasks; 5.75 SP headroom.
- **Traceability**: PASS; 16/16 tasks have valid refs and metadata, 25/25 scoped requirements have task coverage, and there are no scoped orphans/uncovered requirements.
- **Hardening**: `Overall Verdict: ready`; the scoped before-implement verdict authorizes execution. Security, failure semantics, retry/spend, three-OS runtime control, strict ingress, capture/ledger integrity, currentness, recovery, and truthful proof have named controls/tasks.
- **Provider budget**: two Codex/local-Mac attempts were invoked: immutable partial run 1 plus clean correction run 2. Cursor attempts 01/02 stopped before invocation; attempts 03–07 each retain unique evidence, with 05/06 producing findings and 07 proving the free-account limit is exhausted. Antigravity runs 01–04 remain immutable correction evidence and run 05 is clean. Copilot runs 01–02 are immutable zero-finding partials, run 03 is an immutable verified timeout, and run 04 is clean. T061 used ten unique attempts, of which eight invoked Claude and spent one authorized slot each; final run 10 is clean. The maintainer granted exactly one additional fresh independent invocation for the DRIFT-026 authority-code correction, with a new run ID and stop-on-findings semantics. It is not yet spent. Cursor receives no paid or unchanged retry, and no harness used a hidden retry.
- **Open Beta2 item**: FR-048/FR-049/SC-015 requires a separate future iteration plan/tasks/review and blocks T029/feature closeout.
- **Team**: one serial Implementer plus the completed independent Claude T061 Reviewer result.
- **Authorization**: tasks and Iteration 007 implementation are authorized by the fresh verdict below. Provider invocations were initially separate-slot only; the later minimum-budget standing authority now covers exactly one required base proof per remaining harness, with unique run/reference evidence and stop-on-failure semantics.

## Fresh Before-Implement Verdict

- **Verdict**: approved for before-implement
- **Evidence**: on 2026-07-16 the maintainer explicitly wrote `approved for before-implement` in direct response to the Iteration 007 tasks-boundary packet.
- **Authorized execution contract**: task-boundary commit `d9cdd16457e322628957ea74de959a5457358852`, produced from approved plan commit `9fd802b78c9a977fcbbe5651772af800d62fb45f`.
- **Scope**: execute T030–T034b and T051–T061 within the approved 20.25 SP Iteration 007 contract. FR-048/FR-049/SC-015 remains outside this iteration and still blocks T029 and feature closeout.
- **Provider limit**: this verdict grants zero provider invocations. Each of the five best-case base slots and every correction/rerun slot requires its own explicit human authorization.
- **Ledger note**: this explicit scoped verdict and task-boundary commit are the authority. The stale global matcher, stale `session_state`, option numbering, and numeric aliases are not used; the known-unsafe boundary synchronizer was not invoked. T033 owns the durable append-only correction.

## Fresh Review-Signoff Verdict

- **Verdict**: approved for review-signoff.
- **Human instruction**: `approved for review-signoff — include the complete T061 attempt-and-slot ledger (all runs, provider invocations, slot spend, outcomes) in the Iteration 007 review evidence, matching the Iteration 006 precedent.`
- **Authorized reviewed tree**: commit `fc1054b54badcfe2abded0203a1d785eeec0c59b`, canonical digest `5fc6318a300afc654bb09d986d82c8c925506ed3`.
- **Durable authorization**: `auth-b3798462c3b2dd39a2ea71ec52f6b1b1ef14030a5976e91fa39b1d975a5b4317`, recorded through the documented second-chance authority writer with evidence source `human-confirmed-at-resume`; the authorization check passes for the current `implement -> review-signoff` crossing.
- **Instruction fulfillment**: `review.md` records all ten unique T061 attempts, all eight provider invocations/spend facts, every authorization reference, target commit/digest, duration, finding count, and terminal outcome. No hidden retry or unrecorded slot exists.
- **Capture follow-up**: the initial hook capture selected a later injected environment-context turn and did not accept the instruction-bearing verdict. `DRIFT-198-I007-025` records this as a deferred stop/capture-mechanism repair item; the documented second-chance writer restored durable authority without altering runtime code or review evidence.
- **Boundary result**: review-signoff is not complete. Canonical sync after commit `b094e69b` failed closed with `latest-result-not-current`; direct external-store evaluation confirmed the run-10 snapshot moved after committing the required ledger. DRIFT-198-I007-026 is open and retrospective remains unauthorized.
- **DRIFT-026 architecture verdict**: the maintainer approved the controller-owned carry-forward envelope with five mandatory bindings: direct parent is the reviewed commit; a deny-by-default file allowlist permits only enumerated review evidence and never scripts/tests/specification/contract files; one campaign fact is written through `CreateNew` with no second envelope or chain; the fact remains in the authority store outside the reviewed digest; and the gate records/displays `reviewed at X, finalized as F`. Paired positive/negative tests are required. The implementation must remain approximately one validator, one fact writer, and gate wiring plus tests.
- **Fresh-review grant**: exactly one independent provider invocation is authorized after the correction is committed and all deterministic gates pass. It must use a new run ID, has no hidden retry, and stops on any finding.

## Execution Progress

- **T030 done**: parsed user-role turns now retain `human` versus `machinery` verdict-evidence provenance. Claude `isMeta=true` feedback and complete injected envelopes are ineligible in both marker-bound and future fallback selection; a genuine prompt-submit turn remains eligible even when its text is identical to prior machinery.
- **T030 evidence**: the paired genuine/isMeta text regression, synthetic-envelope regression, shared parse-once suite, both handover suites, and all 45 F-198 honesty-registry suites pass.
- **T031 done**: the tokenizer accepts only a leading explicit approval utterance; approval mentions, quotes, teaching text, questions, ambiguous acknowledgements, and bare numeric labels remain non-authoritative. The pure disabled-fallback evaluator requires a genuine human verdict after a packet that names the exact pending cursor.
- **T031 evidence**: paired tokenizer/cursor/order tests, marker-bound numeric rejection, pending-stop teaching, hook capture, transcript parse-once, canonical-state, and atomic-sync tests pass. All 45 F-198 honesty-registry suites pass in 447.7 seconds. The separate `boundary-sync-atomicity` fixture's missing-ledger failure reproduces unchanged at clean pre-T031 commit `1e027875` and is not used as T031 evidence.
- **T032 done**: immutable DEC-198-GOV-001 and DEC-198-GOV-003 transcript fixtures replay a rendered markerless packet, hook feedback persisted as a user-role turn, and no human reply through the real verdict-authority writer.
- **T032 evidence**: both fixtures leave the context/authorization ledger and pending-verdict artifact byte-identical, with zero capture and zero authorization. The focused suite and all 45 F-198 honesty-registry suites pass in 404.0 seconds.
- **T033 done**: authorization entries now have stable derived IDs; corrections append exact original-entry and crossing identities without deleting raw verdicts; pending crossings bind the from/to/working boundaries to a boundary commit and Git tree; effective gate, ratchet, status, handover, and governance readers honor scoped corrections. A cleared scoped state never falls back to stale `session_state`, repeat packets retain one phrase/marker/crossing identity, and numeric replies remain non-authoritative.
- **T033 evidence**: two real `DRIFT-198-I006-001` misuse episodes were appended as corrections `correction-73ccb3f6407aabe32dadc7781e2acd3513ce4f466cad2f0def1a05c2b124eca9` and `correction-6283109f289f3491db9baa23a5e9b8cb9619adfb9c490b753d70e98d9824fcde`; raw history remains intact and the scoped authority readers honor the corrections. The dedicated paired suite, affected verdict/ratchet/stop suites, deployed mirror test, Iteration 007 governance validation, and all 46 F-198 registry suites pass in 442.5 seconds. Final T061 run 10 independently reviewed the current tree with zero findings; broader matcher/capture repair remains explicit backlog rather than a quiet point-fix.
- **T051 done**: `specrew review --live` now resolves one checked authority mode and delegates campaign execution through the synchronous campaign application service without legacy fallback. The transition was persisted as legacy at `81a03cd2`, disabled at `233de034`, then campaign in the current cutover change. The transition door derives campaign-fact presence from the repository and refuses legacy reactivation after facts exist.
- **T051 evidence**: campaign results and exact human dispositions are immutable, identity/path bound, and selected strictly by latest claimed run so a later partial/failure cannot fall back to an older clean result. Missing active-run state, malformed/disabled mode, unclaimed legacy evidence, stale/moved targets, partials, timeouts, actionable/advisory findings, and unverified termination all fail closed without a boundary marker. Exact current clean or human-dispositioned findings alone release the boundary packet. Public-command, project-path, whitelist, parser/diff, focused 132-test authority group, and all 47 F-198 suites pass; the aggregate registry completed in 484.6 seconds. Repository-wide governance reports `PASS` for Iteration 007 and the pre-existing unrelated Iteration 005 missing-`plan.md` failure remains untouched.
- **T052 done**: a Stop is workshop-intermediate only when one exact marker binds the active feature and active iteration to its first remaining selected lens, the iteration artifact is readable, and the visible lens content ends in a pending human question. The provider records bounded non-authoritative re-entry context, suppresses duplicate delivery, and never lets workshop state override a lifecycle boundary or let fabricated/stale prose bypass the ordinary material packet. The live DRIFT-198-I007-002 follow-up also canonicalizes the material surface across the transient `N new commit(s)` annotation: losing that annotation on a later discussion cannot fabricate a second delta, while the retained `HEAD` identity means a genuine later commit still requires the packet.
- **T052 evidence**: canonical and `.specify` providers/refocus guidance are byte-identical. Exact workshop question, outside-state, fabricated-prose, boundary-precedence, interrupted-handover, stale-iteration, same-HEAD annotation-loss, and different-HEAD fixtures pass. The main conformance suite passes all paired cases in 72.7 seconds; stop-intent, provider-seam, every-stop, dispatcher, and deployed-binding suites pass in 37.9 seconds; all 55 F-198 suites pass in 630.5 seconds. Routine discussion is direct after the prior obligation is discharged, substantial work still demands the five-section packet, and lifecycle boundaries retain the six-section verdict packet.
- **T053 done**: one shared bounded prompt/process/file contract now owns exact placeholder rendering, external `candidate.json` delivery, minimal Specrew-suppression environment propagation, stdout non-authority, preflight, and one-call execution. The canonical catalog freezes all five production vectors while unimplemented adapters fail truthfully; Claude is a thin catalog adapter. Campaign staging defaults outside the origin and an origin-owned staging override fails before grant creation. Strict ingress additionally rejects invalid UTF-8 and duplicate fields without salvage.
- **T053 evidence**: the 20-case shared conformance suite covers bounded templates/rendering, catalog dispatch, raw zero/findings candidates, prose/fence/trailing/malformed/oversize/unknown/missing/wrong-run/wrong-digest/version/duplicate/invalid-UTF8 rejection, executable/candidate preflight, a valid stdout object remaining non-authoritative, and no hidden retry. Authority, ingress, orchestration, host-catalog, public-command, packaged-deploy, module-load, manifest, and self-leak checks pass. The first aggregate run exposed an explicit Cursor timeout compatibility regression; production now derives the same 600-second fallback without changing the tolerant catalog reader. The post-correction F-198 registry passes all 48 suites in 438.8 seconds, and scoped Iteration 007 governance passes with historical warnings only.
- **T054 done**: Codex and Copilot now have separate thin production constructors over the T053 contract. Each consumes only its cataloged executable/vector/transport, appends the rendered prompt once as the final argument, propagates only the two suppression controls, writes authority through `candidate.json`, and never retries or parses stdout.
- **T054 evidence**: eight paired adapter cases prove generic factory dispatch, exact command ordering, argument-primary prompt transport, bounded identity/path rendering, unavailable-command preflight, raw candidate acceptance, stdout non-authority, and exactly one injected invocation for both hosts. The shared contract, host catalog, module load, and packaged artifact stay green; all 49 F-198 suites pass in 445.6 seconds. Provider spend remains zero.
- **T055 done**: Cursor and Antigravity now use the same thin production adapter pattern. A no-model local help probe from the OS temp directory confirmed Cursor's `--print`, `--trust`, and `--force` meanings and Antigravity's print/timeout surface; no reviewer/provider invocation occurred. The catalog carries the verified Cursor vector and preserves Antigravity's order-sensitive `--print`-then-prompt contract.
- **T055 evidence**: eight paired cases prove generic factory dispatch, exact headless vectors and final prompt placement, bounded identity/path rendering, minimal environment, unavailable preflight, raw candidate acceptance, stdout non-authority, and exactly one injected invocation. Shared, prior-adapter, catalog, package, and all 50 F-198 registry suites pass; the aggregate completed in 451.9 seconds. All five adapter implementations now exist, but support remains unproven until the separately authorized live smokes.
- **T056 done**: the production Windows runtime validates the closed process contract and real Job Object capability before spend, starts native executables or a narrowly verified PowerShell shim without `cmd.exe`, drains stdout/stderr as non-authoritative streams, and assigns the process to OS containment before publishing the start callback. Timeout, nonzero exit, clean root exit, and callback failure all reap descendants and fail closed when tree-death or stream-closure proof is unavailable.
- **T056 evidence**: seven real Windows cases prove capability preflight, native/shim resolution, pre-spend malformed-spec rejection, post-start Job Object degradation rejection before the spend callback, clean-exit descendant reap, timeout root/descendant kill, closed inherited streams, and timeout authority publication only after verified death. The shared containment suite remains green; the live-finding correction's adjacent seven-suite group passes 105/105 and all 56 F-198 suites pass in 529.8 seconds without provider spend. CI evidence follows under T060.
- **T057 done**: one controller-owned POSIX host now enters containment and writes a bounded readiness fact before it can receive the in-memory reviewer launch payload. Linux requires a delegated cgroup v2 subtree with `cgroup.procs`, `cgroup.events`, and `cgroup.kill`; it proves real membership, uses graceful TERM plus atomic cgroup kill, verifies empty, and removes the run cgroup. macOS uses native `setsid`/`killpg`, verifies `pgid == host pid`, scans the group fail closed, and applies the same stream/death/cleanup terminal ordering. One runtime factory selects Windows, Linux, or macOS without harness conditionals.
- **T057 evidence**: Windows contract/dispatch and package checks pass; an ordinary WSL session reports the missing cgroup delegation before spend while a privileged WSL cgroup v2 run passes all six cases, including clean descendant reap, timeout root/descendant kill, stream closure, candidate ingress, and residue cleanup. The native process-group mechanism also passes on Unix without being misreported as macOS proof. All 52 F-198 registry suites pass in 532.7 seconds. macOS-specific conformance and the required three-OS matrix remain T059; provider spend remains zero.
- **T058 done**: the public campaign command collects controller-owned, non-authoritative progress snapshots and renders bounded lifecycle stages, elapsed/conservative remaining time, process-tree liveness, candidate-file activity, complete-valid checkpoint finding counts, and safe optional usage. Runtime callbacks are exception-isolated and sampled in bounded wait slices; renderer mutation cannot rewrite collected diagnostics. Duplicate target/harness/contract combinations warn before spend but remain informational. Retrospective projection reads strict immutable `ReviewResult` JSON only, deduplicates by lineage deterministically, and preserves complete/partial, currentness/relevance/resolution, harness/target, containment/termination, and failure provenance.
- **T058 evidence**: five dedicated projection cases, campaign duplicate/partial/complete cases, public-command diagnostics, real Windows Job Object heartbeat/timeout cases, six privileged-WSL POSIX cases, and packaged-artifact deployment pass. Windows termination grace is aligned to the shared 10-second duration derivation. All 53 explicit F-198 registry suites pass in 484.5 seconds; scoped Iteration 007 governance passes with historical warnings only. Usage remains honestly `unavailable` when a harness supplies no safe numeric metrics, and provider spend remains zero.
- **T034b done**: the already-landed auto-resolution and component-wise physical containment code is extracted into one shared module consumed by both legacy worktree review and the production campaign command. The CLI now carries parsed design-context refs into that selector. Every invalid explicit ref fails before production-port selection, grant persistence, reservation, snapshot, or spend; valid refs enter the frozen invocation as repo-relative JSON only. An omitted unresolved context adds a bounded `DESIGN_CONTEXT_EMPTY` prompt block and the controller forces the terminal result to partial/incomplete/non-approvable even if a candidate says pass.
- **T034b evidence**: mixed and all-invalid campaign fixtures, valid exact-target/ref propagation, optimistic-pass empty-context degradation, legacy traversal/root/junction behavior, packaged deployment, and controller ingress pass. The focused Windows group passes 94/94 with one POSIX-only skip; focused WSL case-sensitive containment passes 2/2; all 53 explicit F-198 suites pass in 563.5 seconds; fresh scoped governance and the T034b drift check pass with historical warnings only. A broad WSL run also exposed three pre-existing T016 test-portability issues (Windows-path basename expectation and two unsupported `Start-Process -WindowStyle` uses); they do not affect the focused T034b cases and are explicit T059 inputs rather than hidden green claims. T060 still owns the later live-smoke current-target-digest evidence. Provider spend remains zero.
- **T059 done**: one explicitly fake-provider CI job runs the bounded adapter/runtime/authority suite on `windows-latest`, `ubuntu-latest`, and `macos-latest`. Every production adapter builds its normal vector before a test-only seam substitutes the unique fake executable; all five accept raw file-primary JSON, reject prose-wrapped output without hidden retry, and preserve one invocation. The timeout fixture requires descendant death before partial publication. The matrix also carries store/allowance concurrency, recovery, currentness, target containment, public routing, and the native runtime suite.
- **T059 local evidence**: Windows matrix 13/13, exact CI group 113/113 plus the added authority-store 9/9 and spend 13/13, containment 18/18, package 2/2, and all 54 F-198 suites green in 520.4 seconds. Privileged WSL cgroup-v2 evidence passes matrix 13/13, POSIX runtime 6/6, containment 11 pass with 7 Windows-only skips, authority-store 9/9, and spend 13/13. The earlier cross-host basename and `WindowStyle` portability failures are corrected. The scoped drift check is PASS with no new event. Provider spend remains zero.
- **T059 hosted run 1**: GitHub Actions run `29536313910` executed exact commit `662dfc4d310795aaddec587d94e6505bed1376e6`. Windows deterministic proof passed. macOS executed the all-adapter matrix 13/13 and all preceding shared suites, then exposed physical `/var -> /private/var` expectation drift and a Linux-`/proc` assumption in the read-only sampler. Ubuntu exposed root-owned child cgroup control files after parent-only chown. The scoped correction uses physical POSIX `realpath`, adds read-only macOS `Get-Process`/BSD-`ps` sampling, and runs the fake-process Linux CI lane under bounded root privilege with pinned all-user Pester and a root Git safe-directory entry. A 1-second virtualized-runner fixture race was also identified; the fake timeout case alone now uses 5 seconds and passes three consecutive WSL cgroup runs plus Windows. Run 1 remains immutable failed evidence and promotes no support.
- **T059 hosted run 2**: GitHub Actions run `29537492190` and check suite `79984185680` completed `success` against exact correction commit `27015b9e060e9f9696132ff3ed58631d9c538e38`. The deterministic jobs were terminal-success on Ubuntu (`87752152915`), macOS (`87752152937`), and Windows (`87752152924`). Ubuntu proved the privileged cgroup-v2 lane, macOS proved the production process-group path, and Windows proved the production Job Object path. No provider was invoked and fake evidence promotes no live harness support.
- **T060 in progress**: no-spend discovery against commit `55cf338a6565ce4a2a846473da5f8f51b29e31fa` used only production preflight, version/status/help/model-list commands, and GitHub runner/secret metadata, then was committed as `9f96c77f`. Windows remains ready for all five cataloged harnesses and the Job Object runtime. Ubuntu 24.04 now has Microsoft PowerShell `7.6.3`; a transient `systemd-run` service with `Delegate=yes`, `Type=exec`, and ordinary user `alon` provides the bounded cgroup subtree. The real production Copilot file-primary harness and Linux cgroup-v2 runtime preflights both pass inside it, and the unit is collected afterward. Provider spend remains zero.
- **T060 local-macOS package**: the hosted-macOS provider workflow and GitHub Actions credential secret are dropped. `scripts/t060-local-macos-smoke.ps1` requires a clean detached clone at the supplied exact commit, verifies Codex authentication plus the production file-primary/process-group ports without spend, and exposes one separately acknowledged synchronous invoke call with no retry. The returned-package validator independently binds the pinned checkout's origin, exact `HEAD`, canonical digest, package hashes, authority facts, and result contract. The exact install/clone/preflight/invoke/return steps remain in `docs/operations/t060-local-macos-smoke.md`.
- **T060 allocation**: Codex/local macOS is complete after one preserved partial run and one clean correction run. Cursor/Windows Free/Auto produced two complete valid findings reviews, while run 07 truthfully hit the exhausted free-account limit before a candidate; its live contract/runtime proof is valid but no clean-current claim is made. Antigravity/Windows run 05 is a clean exact-digest proof. Copilot/Linux runs 01–02 are valid zero-finding partials, run 03 is a verified timeout, and run 04 is a clean exact-digest proof that closes DRIFT-198-I007-011. Claude/Windows completed the T061 exact-digest signoff on run 10.
- **T060 local-Mac preflight**: direct native-Terminal execution on macOS 15.5 x64 at commit `6708bf058b708df1c6b6f7492f46bb856154434a` returned Codex `0.144.5` authenticated/file-primary ready and `macos-process-group-runtime-ready`, with `provider_invoked=false` and canonical digest `f668677ff652e84f7a05c81964d1a14721a39131`. An earlier preflight launched inside Codex's own command sandbox failed the process-group membership probe; the paired native pass shows why operator docs require direct Terminal execution.
- **T060 local-Mac run 1**: `run-t060-codex-macos-6708bf05-01`, authorization `human-grant-t060-codex-macos-20260717-slot-1`, invoked exactly once and published a schema-valid terminal result after 101491 ms with verified containment/termination, currentness `current`, and unchanged clean commit/digest. It is immutable partial/incomplete evidence: major `finding-d36b4c66cb3c1e56` found that package validation did not independently recompute the pinned checkout digest; note `finding-8d4f2649e461481c` recorded `DESIGN_CONTEXT_EMPTY`. Returned ZIP SHA-256: `6bcacd8ebd51253821132af718f6165cb650ec4b5be246e3089e3deaeb8e903f`.
- **T060 Mac correction**: validator input now includes the clean pinned repository, verifies its origin/exact `HEAD`, and compares an independently recomputed canonical digest with manifest/preflight/result authority. Campaign design-context auto-resolution now uses the validated `FeatureId`, so ignored session files and multiple feature directories cannot force the wrong feature or an empty context. The paired focused suites pass 21/21, the context suite passes 18/18 with one expected case-sensitive-platform skip, and all 55 registered F-198 suites pass in 569.4 seconds. Fresh scoped governance passes Iteration 007; the unrelated historical Iteration 005 missing-`plan.md` failure and warnings remain visible. Commit `b1ae8b47` and clean correction run 2 close both findings.
- **T060 Mac authority amendment**: after run 1, the maintainer wrote `I grant you all you need to finish the Mac tests`. This is recorded as standing authority for the bounded Mac correction sequence only. Each actual attempt remains visible, uniquely identified, single-invocation, and non-retrying; it does not grant Cursor, Antigravity, Copilot, or T061 Claude slots.
- **T060 local-Mac run 2 clean**: `run-t060-codex-macos-b1ae8b47-02`, authorization `standing-mac-grant-20260717-attempt-02`, invoked exactly once against commit `b1ae8b47aece4e0f4a017dc1e8896708fc2c8700` and canonical digest `7dcc6b4da0bf006f24b7c8fa5ed08c56fa42704c`. The authoritative result is complete/pass/current/valid with verified containment/termination, duration 210898 ms, `can_approve_current=true`, and zero findings. Independent validation returned `package_valid=true`, `smoke_clean=true`, no errors, and exit `0`; invoke also exited `0`. Returned ZIP SHA-256: `9e359c121ffae46bb24ed8761749a11492a7d58adad4591f43a3e703d6d09351`. The Mac correction sequence is complete and DRIFT-198-I007-001 is resolved.
- **T060 Cursor/Windows no-spend failures**: attempt 01, `run-t060-cursor-windows-f1e69d0a-01`, failed before target identity resolution because the default temp worktree path crossed Windows' legacy boundary. After the short-root correction passed the full local/CI gates and exact-commit preflights, attempt 02, `run-t060-cursor-windows-7089edcf-02`, proved the disposable target leaf still grew with the full run ID and reached 265 characters at the tree's actual longest tracked path. Both attempts published terminal infrastructure evidence, released their reservations, recorded zero spend facts, invoked no provider, and consumed no Cursor free credit.
- **T060 Windows path correction**: `DRIFT-198-I007-003` uses short sibling roots plus a stable fixed-length token for the disposable Git target leaf; the full run ID remains in immutable authority metadata. The deterministic boundary test derives the actual longest tracked path from `HEAD`; all 93 focused target/runtime/harness tests and all 56 registered F-198 suites, scoped governance, CI run `29585675046`, and fresh Cursor/Antigravity Windows plus Copilot/Linux preflights passed at commit `c3b48c00382500e937ef370aaff8f0ee67efa768` and digest `ca94546d9dcd86b61af25597f887c776125fe268`.
- **T060 Cursor attempts 03–06**: attempt 03 invoked once and hit the inherited Composer/Fast usage limit without a candidate. Commit `11a0129e` made model selection explicit and evidence-bearing. Attempt 04 proved Free permits only Auto. Attempt 05 returned three corrected findings. Attempt 06, `run-t060-cursor-windows-09a89e45-06`, invoked once against exact digest `df6cb180e89f79545886ca2439c8cb1341dac3dd` and published three new current findings: restart reconciliation had a plan but no executor/public operation, claim contention falsely recorded verified containment without a process, and the single-reviewer prompt rule was not mechanically enforced. `DRIFT-198-I007-006` adds the immutable pre-spend recovery receipt, OS recovery operations, one-call reconciliation/public command, truthful containment, and enforced prompt rule. Focused gates, packaged deployment, all 56 F-198 suites in 572.9 seconds, scoped governance, CI run `29595902917`, and exact-commit preflight pass. Run 07's separate free-credit exhaustion is recorded below.
- **T060 Cursor run 07**: CI run `29595902917` and exact-commit preflight passed at commit `c711799fdec8f0e995b4e84e1c525b800360efe8`, digest `773f6c9603ca73995bfedcf4a41fbd8309bdc0cb`, Cursor CLI `2026.07.16-899851b`, and model `auto`. `run-t060-cursor-windows-c711799f-07` invoked once under verified Job Object containment and preserved a clean/current target, but Cursor's transcript ended with the exhausted account usage limit and no candidate. Authority published terminated/incomplete/not-produced evidence with zero findings. No paid or unchanged retry is allowed.
- **T060 Antigravity runs 01–02 and corrections**: exact-commit run 01 reached the 900-second bound and preserved two major defects. `DRIFT-198-I007-007` clears both reviewer suppressions from the supported bounded verification child and isolates deliberate dispatcher children in the production-path fixture; the exact failing environment, 27 adjacent tests, all 56 F-198 suites, governance, and CI pass. Run 02 at commit `d9d4ee9e141f56add723ea20d1d43a31011eb948` and digest `5206249669fcfa89b8007c48e29d1ce060b32c40` completed in 798000 ms and did not repeat either defect. Its five findings were all scheduled downstream status items. `DRIFT-198-I007-008` gives both T060 packages an explicit phase-aware code-review scope while retaining grounded defect reporting; paired package/evidence suites pass 13/13 and all 56 F-198 suites pass in 610.7 seconds. Commit, CI, exact-commit preflight, and one verification run remain.
- **T060 Antigravity runs 03–04 and governance correction**: CI and exact-commit preflight passed at commit `ac731737fc6be92ea9248019c429bdc8e66eaccc`, digest `a30f1aa1511c8c3c9c9cbf148b9658ff973ae4d3`. Run 03 invoked once and returned after 450343 ms under verified containment/termination, but strict ingress rejected both finding locations as non-strings; no salvage or hidden retry occurred. `DRIFT-198-I007-009` requires and mechanically enforces a plain-string optional location. Commit `ba639e84589e3a89fc8440616e8a1112b6ea4c51`, CI `29604077312`, and exact preflight passed. Run 04 invoked once, completed in 790453 ms with a valid/current/contained candidate, did not repeat the location defect, and found only that the stale host-neutral governance test rejected approved host-bound seams while remaining outside the 56-suite registry. `DRIFT-198-I007-010` uses a closed named host-bound seam, adds Cursor to the forbidden-token set, and registers the guard as suite 57; its focused test passes 3/3, all 57 registered suites pass in 605.4 seconds, and scoped governance passes with historical warnings only. Commit/CI/preflight and one verification run remain.
- **T060 Antigravity run 05 clean**: commit `f39178e616f58df9badbdcf09f62282ec843153c`, CI run `29606585387`, and exact no-spend preflight passed at canonical digest `d3314ee2566887e5148e02ca33c04128b87d7bdf`, Antigravity CLI `1.1.3`, and verified Job Object readiness. `run-t060-antigravity-windows-f39178e6-05`, authorization `human-grant-t060-antigravity-windows-20260717-slot-5`, invoked exactly once and completed in 777391 ms. The authoritative result is complete/pass/current/valid with verified containment/termination, clean unchanged target, `can_approve_current=true`, and zero findings. DRIFT-198-I007-010 is independently closed.
- **T060 Copilot/Linux runs 01–04**: evidence commit `e539f82ccdeffbc2108e9a6b7881ad1a9ffa0e86`, CI run `29607731270`, and digest `86a075e3f3fd61c011fe2933ab945d69afc89982` produced the two immutable valid zero-finding partials that exposed exhaustive-inventory ambiguity. DRIFT-198-I007-011 then passed 32/32 paired contract/package tests, 16/16 custom orchestration cases, all 57 F-198 suites in 616.1 seconds, scoped governance, correction commit `81a03cd2974be3f529f0cd8ca9f3037d9415ea04`, CI `29609289916`, and exact preflight at digest `828c47e367dc60edd95015014ba7f6c11ac6d883`. Run 03 preserves a verified 300-second timeout with no candidate; run 04 completed in 226465 ms with complete/pass/current/valid zero-finding evidence, clean unchanged target identity, and verified delegated cgroup-v2 containment/termination. No older result is promoted.
- **T060 campaign cutover verification**: the pushed disabled barrier is commit `233de034ddd13382dc1f58ba7323ff7b89a1c0b0`. The first campaign-mode aggregate exposed DRIFT-198-I007-012: four retained legacy characterization suites inherited production campaign authority and stopped before their intended assertions. Explicit legacy fixtures preserve those tests without weakening production suppression. The four affected files pass 32/32, all 57 F-198 suites pass in 610.5 seconds against campaign mode, and scoped Iteration 007 governance passes with historical warnings only.
- **T060 done**: campaign cutover commit `fc52b6d1d9c6a4c17489f87dbfd9e4534300c0a7` exposed only a hosted Windows cold-start test bound. DRIFT-198-I007-013 keeps the dedicated five-second timeout proof unchanged and raises successful fake cases to a bounded 30 seconds. Focused matrix 13/13, all 57 suites in 616.7 seconds, scoped governance, and replacement CI `29612794643` are green. T060 support truth remains digest-scoped: Cursor live execution is proved but no clean-current claim is made after free-credit exhaustion.
- **T060 minimum-budget standing authority**: after the no-spend Cursor failure, the maintainer authorized all remaining requirements subject to minimum required budget, later supplied run/reference 04 and authorized two-to-three serialized Cursor attempts until satisfied, and then explicitly authorized the three run-05 corrections plus the remaining run 06. Each attempt records a fresh run ID and authorization reference, invokes at most once, and the sequence stops on findings, invalid output, timeout, or infrastructure failure rather than spending an unchanged retry.
- **T052 dogfood follow-up resolved**: `DRIFT-198-I007-002` traced the repeated discussion packet to a transient new-commit suffix inside the material-surface hash. The canonical key now ignores only that suffix, retains `HEAD` and file identity, and passes paired same-HEAD/different-HEAD proof plus the full registry.
- **Reconciled boundary**: T030 closes the FR-041 machinery-exclusion obligation only; T051 now separately delivers the FR-045 packet/current-review gate assigned by the approved Iteration 003 reconciliation.
- **Provider invocation ledger**: two Codex/local-Mac invocations; Cursor attempts 01/02 stopped before invocation, while attempts 03–07 each launched once and remain separately preserved. Attempts 05/06 completed semantic reviews; run 07 exhausted the free-account allowance without a candidate. Antigravity runs 01–05 each launched once and remain immutable; run 03 was strictly rejected without salvage, run 04 returned one valid governance finding, and run 05 is clean. Copilot runs 01–04 each launched once: two valid partials, one verified timeout, and one clean complete result. T061 contains ten unique attempts and authorization references: attempts 1–2 stopped before provider invocation, attempts 3–9 each spent one slot and returned 16 total validated findings, and run 10 spent the eighth slot and returned a clean pass. The eight T061 provider durations total 2,662,001 ms. Extra paid Cursor usage and hidden retries are excluded; `review.md` is the complete T061 attempt-and-slot ledger.

## Current Production Truth

- Checked-in review authority mode remains `legacy`.
- Iteration 006 foundation and Claude file-primary slice are delivered and independently reviewed.
- T019 mutable lease/navigator/stamping/pruning mechanisms are not executable Iteration 007 work.
- Machinery-turn exclusion, tokenizer/temporal/cursor capture hardening, exact fabrication fixtures, and the append-only scoped correction door are delivered. The underlying legacy matcher/backlog question remains visible; no quiet global matcher rewrite was made.
- Public campaign command and packet-gate wiring are delivered, and the checked-in authority mode is `campaign`. The disabled barrier and one-way transition history remain preserved. Workshop-intermediate and ordinary Stop materiality, the shared strict candidate contract/matrix, all five thin production harness adapters, all three production runtime ports, progress/retro, and the hosted three-OS deterministic workflow are implemented. Codex/local macOS, Antigravity/Windows, and Copilot/Linux are clean pinned-digest proofs. Cursor Free/Auto completed two valid finding reviews, then exhausted free credit on run 07; live execution is proved but clean-current support is not claimed. Claude/Windows final run 10 supplies the clean exact-digest signoff for commit `fc1054b5` / digest `5fc6318a300afc654bb09d986d82c8c925506ed3`.

## Notes

- Update this file and tasks-progress.yml after each task completes.
- Do not edit Iteration 003 state/progress to simulate the ownership move.
- T033 local verification and T061 independent final-tree verification are complete. Continue to require explicit scoped verdict phrases and never treat a bare number as authorization. DRIFT-198-I007-025 carries the newly observed capture-selection defect to the later stop/capture mechanism repair.

<!-- >>> specrew-managed escalation-state >>> -->
## Repair Escalation

- **Status**: inactive
- **Artifact**: (none)
- **Gate**: (none)
- **Failure Count**: 0
- **Current Tier**: efficiency
- **Current Owner**: (none)
- **Locked Out Agents**: (none)
- **Last Escalated**: (none)
- **Resolved At**: (none)
- **Notes**: (none)
<!-- <<< specrew-managed escalation-state >>> -->
